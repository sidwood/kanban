[
  {
    "description": "Return current core health for boot and diagnostics.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `health.get` query.",
      "title": "HealthQuery",
      "type": "object"
    },
    "name": "health_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Response payload for the `health.get` query.",
      "properties": {
        "connected": {
          "type": "boolean"
        },
        "service_version": {
          "type": "string"
        }
      },
      "required": [
        "connected",
        "service_version"
      ],
      "title": "HealthResponse",
      "type": "object"
    }
  },
  {
    "description": "Create an Initiative folder with a validated name.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `initiative.create` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The intended name; blank names are refused.",
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "name"
      ],
      "title": "InitiativeCreateRequest",
      "type": "object"
    },
    "name": "initiative_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Initiative record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The trimmed name.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "id",
        "name",
        "version"
      ],
      "title": "InitiativeRecord",
      "type": "object"
    }
  },
  {
    "description": "Rename an active Initiative.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `initiative.rename` command.",
      "properties": {
        "initiative_id": {
          "description": "The Initiative being renamed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The intended name; blank names are refused.",
          "type": "string"
        }
      },
      "required": [
        "initiative_id",
        "mutation",
        "name"
      ],
      "title": "InitiativeRenameRequest",
      "type": "object"
    },
    "name": "initiative_rename",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Initiative record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The trimmed name.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "id",
        "name",
        "version"
      ],
      "title": "InitiativeRecord",
      "type": "object"
    }
  },
  {
    "description": "Archive an Initiative. Archiving is terminal and preserves every recorded fact.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `initiative.archive` command.",
      "properties": {
        "initiative_id": {
          "description": "The Initiative being archived.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        }
      },
      "required": [
        "initiative_id",
        "mutation"
      ],
      "title": "InitiativeArchiveRequest",
      "type": "object"
    },
    "name": "initiative_archive",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Initiative record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The trimmed name.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "id",
        "name",
        "version"
      ],
      "title": "InitiativeRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Initiative, archived ones included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `initiative.list` query.",
      "title": "InitiativeListQuery",
      "type": "object"
    },
    "name": "initiative_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "InitiativeRecord": {
          "additionalProperties": false,
          "description": "The Initiative record as every client sees it.",
          "properties": {
            "archived": {
              "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
              "type": "boolean"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "name": {
              "description": "The trimmed name.",
              "type": "string"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "archived",
            "id",
            "name",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `initiative.list` query.",
      "properties": {
        "initiatives": {
          "description": "Every Initiative, archived included, newest last.",
          "items": {
            "$ref": "#/definitions/InitiativeRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "initiatives"
      ],
      "title": "InitiativeListResponse",
      "type": "object"
    }
  },
  {
    "description": "Register a Project with one Git repository, Seed Workspace, default branch, and exclusive Herdr session.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `project.register` command.",
      "properties": {
        "code": {
          "description": "The intended code; it must match `[A-Z][A-Z0-9]{1,7}` in full and `KAN` is reserved.",
          "type": "string"
        },
        "default_branch": {
          "description": "The default branch.",
          "type": "string"
        },
        "herdr_session": {
          "description": "The exclusive Herdr session name; duplicate session names are refused.",
          "type": "string"
        },
        "initiative_id": {
          "description": "The Initiative the Project sits under, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The intended name; blank names are refused.",
          "type": "string"
        },
        "repository": {
          "description": "The target Git repository; non-Git targets are refused.",
          "type": "string"
        },
        "seed_workspace": {
          "description": "The Seed Workspace.",
          "type": "string"
        }
      },
      "required": [
        "code",
        "default_branch",
        "herdr_session",
        "mutation",
        "name",
        "repository",
        "seed_workspace"
      ],
      "title": "ProjectRegisterRequest",
      "type": "object"
    },
    "name": "project_register",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ProjectCounters": {
          "additionalProperties": false,
          "description": "The Plan, Spec, and Ticket counters of one Project: the last number minted per kind. Zero means nothing has been minted yet, and the counters survive archiving.",
          "properties": {
            "plan": {
              "description": "The last Plan number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec": {
              "description": "The last Spec number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket": {
              "description": "The last Ticket number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "plan",
            "spec",
            "ticket"
          ],
          "type": "object"
        }
      },
      "description": "The Project record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Project is archived. Archived Projects stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "code": {
          "description": "The immutable, globally unique Project code.",
          "type": "string"
        },
        "counters": {
          "allOf": [
            {
              "$ref": "#/definitions/ProjectCounters"
            }
          ],
          "description": "The Project's counters, preserved through every state."
        },
        "default_branch": {
          "description": "The one default branch.",
          "type": "string"
        },
        "herdr_session": {
          "description": "The one exclusive named Herdr session.",
          "type": "string"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "initiative_id": {
          "description": "The Initiative the Project sits under, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "name": {
          "description": "The display name.",
          "type": "string"
        },
        "repository": {
          "description": "The one target Git repository.",
          "type": "string"
        },
        "seed_workspace": {
          "description": "The one Seed Workspace.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "code",
        "counters",
        "default_branch",
        "herdr_session",
        "id",
        "name",
        "repository",
        "seed_workspace",
        "version"
      ],
      "title": "ProjectRecord",
      "type": "object"
    }
  },
  {
    "description": "Archive a Project. Archiving is terminal and preserves every recorded fact.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `project.archive` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "description": "The Project being archived.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "project_id"
      ],
      "title": "ProjectArchiveRequest",
      "type": "object"
    },
    "name": "project_archive",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ProjectCounters": {
          "additionalProperties": false,
          "description": "The Plan, Spec, and Ticket counters of one Project: the last number minted per kind. Zero means nothing has been minted yet, and the counters survive archiving.",
          "properties": {
            "plan": {
              "description": "The last Plan number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec": {
              "description": "The last Spec number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket": {
              "description": "The last Ticket number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "plan",
            "spec",
            "ticket"
          ],
          "type": "object"
        }
      },
      "description": "The Project record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Project is archived. Archived Projects stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "code": {
          "description": "The immutable, globally unique Project code.",
          "type": "string"
        },
        "counters": {
          "allOf": [
            {
              "$ref": "#/definitions/ProjectCounters"
            }
          ],
          "description": "The Project's counters, preserved through every state."
        },
        "default_branch": {
          "description": "The one default branch.",
          "type": "string"
        },
        "herdr_session": {
          "description": "The one exclusive named Herdr session.",
          "type": "string"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "initiative_id": {
          "description": "The Initiative the Project sits under, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "name": {
          "description": "The display name.",
          "type": "string"
        },
        "repository": {
          "description": "The one target Git repository.",
          "type": "string"
        },
        "seed_workspace": {
          "description": "The one Seed Workspace.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "code",
        "counters",
        "default_branch",
        "herdr_session",
        "id",
        "name",
        "repository",
        "seed_workspace",
        "version"
      ],
      "title": "ProjectRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Project, archived ones included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `project.list` query.",
      "title": "ProjectListQuery",
      "type": "object"
    },
    "name": "project_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ProjectCounters": {
          "additionalProperties": false,
          "description": "The Plan, Spec, and Ticket counters of one Project: the last number minted per kind. Zero means nothing has been minted yet, and the counters survive archiving.",
          "properties": {
            "plan": {
              "description": "The last Plan number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec": {
              "description": "The last Spec number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket": {
              "description": "The last Ticket number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "plan",
            "spec",
            "ticket"
          ],
          "type": "object"
        },
        "ProjectRecord": {
          "additionalProperties": false,
          "description": "The Project record as every client sees it.",
          "properties": {
            "archived": {
              "description": "Whether the Project is archived. Archived Projects stay listed; no fact is ever removed.",
              "type": "boolean"
            },
            "code": {
              "description": "The immutable, globally unique Project code.",
              "type": "string"
            },
            "counters": {
              "allOf": [
                {
                  "$ref": "#/definitions/ProjectCounters"
                }
              ],
              "description": "The Project's counters, preserved through every state."
            },
            "default_branch": {
              "description": "The one default branch.",
              "type": "string"
            },
            "herdr_session": {
              "description": "The one exclusive named Herdr session.",
              "type": "string"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "initiative_id": {
              "description": "The Initiative the Project sits under, if any.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "name": {
              "description": "The display name.",
              "type": "string"
            },
            "repository": {
              "description": "The one target Git repository.",
              "type": "string"
            },
            "seed_workspace": {
              "description": "The one Seed Workspace.",
              "type": "string"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "archived",
            "code",
            "counters",
            "default_branch",
            "herdr_session",
            "id",
            "name",
            "repository",
            "seed_workspace",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `project.list` query.",
      "properties": {
        "projects": {
          "description": "Every Project, archived included, newest last.",
          "items": {
            "$ref": "#/definitions/ProjectRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "projects"
      ],
      "title": "ProjectListResponse",
      "type": "object"
    }
  },
  {
    "description": "Create a draft Plan, minting its number from the Project counter.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.create` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "description": "The Project the new Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "project_id"
      ],
      "title": "PlanCreateRequest",
      "type": "object"
    },
    "name": "plan_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Add a Spec to a draft Plan's membership and display order.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.spec.add` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan gaining a member.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_number": {
          "description": "The Spec number joining the Plan.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id",
        "spec_number"
      ],
      "title": "PlanSpecAddRequest",
      "type": "object"
    },
    "name": "plan_spec_add",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Remove a Spec from a draft Plan; a Spec carrying edges is refused.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.spec.remove` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan losing a member.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_number": {
          "description": "The Spec number leaving the Plan.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id",
        "spec_number"
      ],
      "title": "PlanSpecRemoveRequest",
      "type": "object"
    },
    "name": "plan_spec_remove",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Move a Spec within a draft Plan's display order.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.spec.move` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose display order changes.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "position": {
          "description": "The position in the display order the Spec moves to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_number": {
          "description": "The Spec number moving.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id",
        "position",
        "spec_number"
      ],
      "title": "PlanSpecMoveRequest",
      "type": "object"
    },
    "name": "plan_spec_move",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Add a dependency edge inside one draft Plan; an edge leaving the Plan is refused.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.edge.add` command.",
      "properties": {
        "from_spec": {
          "description": "The Spec that must land first.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose graph gains an edge.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "to_spec": {
          "description": "The Spec that waits on `from_spec`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "from_spec",
        "mutation",
        "plan_id",
        "to_spec"
      ],
      "title": "PlanEdgeAddRequest",
      "type": "object"
    },
    "name": "plan_edge_add",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Remove a dependency edge from a draft Plan.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.edge.remove` command.",
      "properties": {
        "from_spec": {
          "description": "The Spec that must land first.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose graph loses an edge.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "to_spec": {
          "description": "The Spec that waits on `from_spec`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "from_spec",
        "mutation",
        "plan_id",
        "to_spec"
      ],
      "title": "PlanEdgeRemoveRequest",
      "type": "object"
    },
    "name": "plan_edge_remove",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Freeze a draft Plan's membership, order, and graph into an immutable version.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.activate` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan freezing its shape into a version.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanActivateRequest",
      "type": "object"
    },
    "name": "plan_activate",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Reopen an active Plan's draft and reserve its auditable replacement version.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.replan` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose replacement version is reserved.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanReplanRequest",
      "type": "object"
    },
    "name": "plan_replan",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Complete an active Plan. Complete is terminal and off the active surface.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.complete` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan completing.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanCompleteRequest",
      "type": "object"
    },
    "name": "plan_complete",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Cancel a draft or active Plan. Cancelled is terminal and off the active surface.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.cancel` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan being cancelled.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanCancelRequest",
      "type": "object"
    },
    "name": "plan_cancel",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Archive a Plan from any open state. Archiving is terminal and preserves every recorded fact.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.archive` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan being archived. Archiving is terminal and preserves every recorded fact.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanArchiveRequest",
      "type": "object"
    },
    "name": "plan_archive",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Plan of a Project, terminal states included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `plan.list` query.",
      "properties": {
        "project_id": {
          "description": "The Project whose Plans are listed, terminal states included.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "PlanListQuery",
      "type": "object"
    },
    "name": "plan_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanRecord": {
          "additionalProperties": false,
          "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
          "properties": {
            "edges": {
              "description": "The dependency edges, held separately from the display order.",
              "items": {
                "$ref": "#/definitions/PlanEdge"
              },
              "type": "array"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "number": {
              "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The Project this Plan belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec_numbers": {
              "description": "The display order: the member Spec numbers as a per-Plan sequence.",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "state": {
              "allOf": [
                {
                  "$ref": "#/definitions/PlanState"
                }
              ],
              "description": "The lifecycle state."
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "edges",
            "id",
            "number",
            "project_id",
            "spec_numbers",
            "state",
            "version"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "Response payload for the `plan.list` query.",
      "properties": {
        "plans": {
          "description": "Every Plan of the Project, newest last, all states included.",
          "items": {
            "$ref": "#/definitions/PlanRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "plans"
      ],
      "title": "PlanListResponse",
      "type": "object"
    }
  },
  {
    "description": "Read one Plan with every frozen version beside it.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `plan.get` query.",
      "properties": {
        "plan_id": {
          "description": "The Plan being read.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "plan_id"
      ],
      "title": "PlanGetQuery",
      "type": "object"
    },
    "name": "plan_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanRecord": {
          "additionalProperties": false,
          "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
          "properties": {
            "edges": {
              "description": "The dependency edges, held separately from the display order.",
              "items": {
                "$ref": "#/definitions/PlanEdge"
              },
              "type": "array"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "number": {
              "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The Project this Plan belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec_numbers": {
              "description": "The display order: the member Spec numbers as a per-Plan sequence.",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "state": {
              "allOf": [
                {
                  "$ref": "#/definitions/PlanState"
                }
              ],
              "description": "The lifecycle state."
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "edges",
            "id",
            "number",
            "project_id",
            "spec_numbers",
            "state",
            "version"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        },
        "PlanVersionRecord": {
          "additionalProperties": false,
          "description": "One frozen Plan version as every client sees it: the Spec membership, display order, and dependency graph exactly as they stood at activation. Immutable once minted.",
          "properties": {
            "edges": {
              "description": "The frozen dependency graph.",
              "items": {
                "$ref": "#/definitions/PlanEdge"
              },
              "type": "array"
            },
            "number": {
              "description": "The version's number; the first freeze is one.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec_numbers": {
              "description": "The frozen display order.",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            }
          },
          "required": [
            "edges",
            "number",
            "spec_numbers"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `plan.get` query: the Plan and every frozen version beside it, so prior versions stay queryable.",
      "properties": {
        "plan": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanRecord"
            }
          ],
          "description": "The Plan's current record."
        },
        "versions": {
          "description": "Every frozen version, oldest first.",
          "items": {
            "$ref": "#/definitions/PlanVersionRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "plan",
        "versions"
      ],
      "title": "PlanGetResponse",
      "type": "object"
    }
  },
  {
    "description": "Query the per-Project append-only activity timeline.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        },
        "TimelineEventKind": {
          "description": "The closed vocabulary of timeline event kinds (DR-AE-01).",
          "enum": [
            "transition",
            "run",
            "telemetry",
            "review",
            "finding",
            "evidence",
            "comment",
            "deferral",
            "ruling"
          ],
          "type": "string"
        },
        "TimelineScope": {
          "description": "Where a timeline row belongs. Entities that sit above every Project — Initiatives, for one — are recorded globally; everything inside a Project is recorded against that Project's identity.",
          "oneOf": [
            {
              "description": "Above every Project.",
              "enum": [
                "global"
              ],
              "type": "string"
            },
            {
              "additionalProperties": false,
              "description": "One Project's own timeline, named by its identity.",
              "properties": {
                "project": {
                  "type": "string"
                }
              },
              "required": [
                "project"
              ],
              "type": "object"
            }
          ]
        }
      },
      "description": "Filters for the per-Project timeline query surface.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "kinds": {
          "items": {
            "$ref": "#/definitions/TimelineEventKind"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "scope": {
          "$ref": "#/definitions/TimelineScope"
        },
        "since": {
          "type": [
            "string",
            "null"
          ]
        },
        "until": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "scope"
      ],
      "title": "TimelineQuery",
      "type": "object"
    },
    "name": "timeline_query",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        },
        "TimelineEventKind": {
          "description": "The closed vocabulary of timeline event kinds (DR-AE-01).",
          "enum": [
            "transition",
            "run",
            "telemetry",
            "review",
            "finding",
            "evidence",
            "comment",
            "deferral",
            "ruling"
          ],
          "type": "string"
        },
        "TimelineEventRecord": {
          "additionalProperties": false,
          "description": "One append-only timeline row as returned by queries.",
          "properties": {
            "detail": true,
            "entity": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TimelineEntityRef"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEventKind"
            },
            "recorded_at": {
              "type": "string"
            },
            "scope": {
              "$ref": "#/definitions/TimelineScope"
            }
          },
          "required": [
            "detail",
            "id",
            "kind",
            "recorded_at",
            "scope"
          ],
          "type": "object"
        },
        "TimelineScope": {
          "description": "Where a timeline row belongs. Entities that sit above every Project — Initiatives, for one — are recorded globally; everything inside a Project is recorded against that Project's identity.",
          "oneOf": [
            {
              "description": "Above every Project.",
              "enum": [
                "global"
              ],
              "type": "string"
            },
            {
              "additionalProperties": false,
              "description": "One Project's own timeline, named by its identity.",
              "properties": {
                "project": {
                  "type": "string"
                }
              },
              "required": [
                "project"
              ],
              "type": "object"
            }
          ]
        }
      },
      "description": "The timeline query answer.",
      "properties": {
        "events": {
          "items": {
            "$ref": "#/definitions/TimelineEventRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "events"
      ],
      "title": "TimelineQueryResponse",
      "type": "object"
    }
  },
  {
    "description": "Create a Comment with its first revision on a timeline-visible entity.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `comment.create` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "description": "The owning Project.",
          "type": "string"
        },
        "target": {
          "allOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            }
          ],
          "description": "The entity this Comment attaches to."
        },
        "text": {
          "description": "The initial text; blank text is refused.",
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "project_id",
        "target",
        "text"
      ],
      "title": "CommentCreateRequest",
      "type": "object"
    },
    "name": "comment_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "The Comment record as every client sees it.",
      "properties": {
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The owning Project.",
          "type": "string"
        },
        "target": {
          "allOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            }
          ],
          "description": "The entity this Comment attaches to."
        },
        "text": {
          "description": "The current text, resolved from the latest revision.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "id",
        "project_id",
        "target",
        "text",
        "version"
      ],
      "title": "CommentRecord",
      "type": "object"
    }
  },
  {
    "description": "Edit a Comment by appending a new immutable revision.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `comment.edit` command.",
      "properties": {
        "comment_id": {
          "description": "The Comment being edited.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "text": {
          "description": "The revised text; blank text is refused.",
          "type": "string"
        }
      },
      "required": [
        "comment_id",
        "mutation",
        "text"
      ],
      "title": "CommentEditRequest",
      "type": "object"
    },
    "name": "comment_edit",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "The Comment record as every client sees it.",
      "properties": {
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The owning Project.",
          "type": "string"
        },
        "target": {
          "allOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            }
          ],
          "description": "The entity this Comment attaches to."
        },
        "text": {
          "description": "The current text, resolved from the latest revision.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "id",
        "project_id",
        "target",
        "text",
        "version"
      ],
      "title": "CommentRecord",
      "type": "object"
    }
  },
  {
    "description": "Query the full revision history for one Comment.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `comment.revisions` query.",
      "properties": {
        "comment_id": {
          "description": "The Comment whose history is requested.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "comment_id"
      ],
      "title": "CommentRevisionsQuery",
      "type": "object"
    },
    "name": "comment_revisions",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "CommentRecord": {
          "additionalProperties": false,
          "description": "The Comment record as every client sees it.",
          "properties": {
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The owning Project.",
              "type": "string"
            },
            "target": {
              "allOf": [
                {
                  "$ref": "#/definitions/TimelineEntityRef"
                }
              ],
              "description": "The entity this Comment attaches to."
            },
            "text": {
              "description": "The current text, resolved from the latest revision.",
              "type": "string"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "id",
            "project_id",
            "target",
            "text",
            "version"
          ],
          "type": "object"
        },
        "CommentRevisionRecord": {
          "additionalProperties": false,
          "description": "One immutable revision as returned by queries.",
          "properties": {
            "recorded_at": {
              "description": "When the revision was recorded.",
              "type": "string"
            },
            "revision": {
              "description": "The one-based revision number.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "text": {
              "description": "The revision text.",
              "type": "string"
            }
          },
          "required": [
            "recorded_at",
            "revision",
            "text"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `comment.revisions` query.",
      "properties": {
        "comment": {
          "allOf": [
            {
              "$ref": "#/definitions/CommentRecord"
            }
          ],
          "description": "The Comment with its current text resolved."
        },
        "revisions": {
          "description": "Every revision, oldest first.",
          "items": {
            "$ref": "#/definitions/CommentRevisionRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "comment",
        "revisions"
      ],
      "title": "CommentRevisionsResponse",
      "type": "object"
    }
  },
  {
    "description": "Record an immutable operator ruling on the timeline.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ruling.record` command.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "type": "string"
        },
        "summary": {
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "project_id",
        "summary"
      ],
      "title": "RulingRecordRequest",
      "type": "object"
    },
    "name": "ruling_record",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "One immutable ruling as every client sees it.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "The entity the ruling concerns, if any."
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the ruling belongs to.",
          "type": "string"
        },
        "recorded_at": {
          "description": "When the ruling was recorded.",
          "type": "string"
        },
        "summary": {
          "description": "The operator decision text.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The ruling this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "recorded_at",
        "summary"
      ],
      "title": "RulingRecord",
      "type": "object"
    }
  },
  {
    "description": "Supersede a ruling with a new immutable record.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ruling.supersede` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "type": "string"
        },
        "ruling_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "summary": {
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "project_id",
        "ruling_id",
        "summary"
      ],
      "title": "RulingSupersedeRequest",
      "type": "object"
    },
    "name": "ruling_supersede",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "One immutable ruling as every client sees it.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "The entity the ruling concerns, if any."
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the ruling belongs to.",
          "type": "string"
        },
        "recorded_at": {
          "description": "When the ruling was recorded.",
          "type": "string"
        },
        "summary": {
          "description": "The operator decision text.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The ruling this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "recorded_at",
        "summary"
      ],
      "title": "RulingRecord",
      "type": "object"
    }
  },
  {
    "description": "List every ruling for a project, superseded originals included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ruling.list` query.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "project_id": {
          "type": "string"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "RulingListQuery",
      "type": "object"
    },
    "name": "ruling_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "RulingRecord": {
          "additionalProperties": false,
          "description": "One immutable ruling as every client sees it.",
          "properties": {
            "entity": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TimelineEntityRef"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The entity the ruling concerns, if any."
            },
            "id": {
              "description": "The storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The project the ruling belongs to.",
              "type": "string"
            },
            "recorded_at": {
              "description": "When the ruling was recorded.",
              "type": "string"
            },
            "summary": {
              "description": "The operator decision text.",
              "type": "string"
            },
            "supersedes_id": {
              "description": "The ruling this one supersedes, if any.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "id",
            "project_id",
            "recorded_at",
            "summary"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `ruling.list` query.",
      "properties": {
        "rulings": {
          "description": "Every ruling for the project, superseded originals included.",
          "items": {
            "$ref": "#/definitions/RulingRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "rulings"
      ],
      "title": "RulingListResponse",
      "type": "object"
    }
  },
  {
    "description": "Record an immutable finding deferral on the timeline.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `deferral.record` command.",
      "properties": {
        "finding_id": {
          "type": "string"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "finding_id",
        "mutation",
        "project_id",
        "reason"
      ],
      "title": "DeferralRecordRequest",
      "type": "object"
    },
    "name": "deferral_record",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "One immutable deferral as every client sees it.",
      "properties": {
        "finding_id": {
          "description": "The finding that was deferred.",
          "type": "string"
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the deferral belongs to.",
          "type": "string"
        },
        "reason": {
          "description": "Why the finding was deferred.",
          "type": "string"
        },
        "recorded_at": {
          "description": "When the deferral was recorded.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The deferral this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "finding_id",
        "id",
        "project_id",
        "reason",
        "recorded_at"
      ],
      "title": "DeferralRecord",
      "type": "object"
    }
  },
  {
    "description": "Supersede a deferral with a new immutable record.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `deferral.supersede` command.",
      "properties": {
        "deferral_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "deferral_id",
        "mutation",
        "project_id",
        "reason"
      ],
      "title": "DeferralSupersedeRequest",
      "type": "object"
    },
    "name": "deferral_supersede",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "One immutable deferral as every client sees it.",
      "properties": {
        "finding_id": {
          "description": "The finding that was deferred.",
          "type": "string"
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the deferral belongs to.",
          "type": "string"
        },
        "reason": {
          "description": "Why the finding was deferred.",
          "type": "string"
        },
        "recorded_at": {
          "description": "When the deferral was recorded.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The deferral this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "finding_id",
        "id",
        "project_id",
        "reason",
        "recorded_at"
      ],
      "title": "DeferralRecord",
      "type": "object"
    }
  },
  {
    "description": "List every deferral for a project, superseded originals included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `deferral.list` query.",
      "properties": {
        "finding_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "type": "string"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "DeferralListQuery",
      "type": "object"
    },
    "name": "deferral_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "DeferralRecord": {
          "additionalProperties": false,
          "description": "One immutable deferral as every client sees it.",
          "properties": {
            "finding_id": {
              "description": "The finding that was deferred.",
              "type": "string"
            },
            "id": {
              "description": "The storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The project the deferral belongs to.",
              "type": "string"
            },
            "reason": {
              "description": "Why the finding was deferred.",
              "type": "string"
            },
            "recorded_at": {
              "description": "When the deferral was recorded.",
              "type": "string"
            },
            "supersedes_id": {
              "description": "The deferral this one supersedes, if any.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "finding_id",
            "id",
            "project_id",
            "reason",
            "recorded_at"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `deferral.list` query.",
      "properties": {
        "deferrals": {
          "description": "Every deferral for the project, superseded originals included.",
          "items": {
            "$ref": "#/definitions/DeferralRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "deferrals"
      ],
      "title": "DeferralListResponse",
      "type": "object"
    }
  },
  {
    "description": "Attach managed-file or repository evidence to an entity.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "EvidenceKindDto": {
          "description": "Whether an evidence item is a managed file or repository reference.",
          "enum": [
            "managed_file",
            "repository"
          ],
          "type": "string"
        },
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `evidence.attach` command.",
      "properties": {
        "commit_identity": {
          "type": [
            "string",
            "null"
          ]
        },
        "content_base64": {
          "type": [
            "string",
            "null"
          ]
        },
        "entity_id": {
          "type": "string"
        },
        "entity_kind": {
          "type": "string"
        },
        "evidence_kind": {
          "$ref": "#/definitions/EvidenceKindDto"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "type": "string"
        },
        "relative_path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "entity_id",
        "entity_kind",
        "evidence_kind",
        "mutation",
        "project_id"
      ],
      "title": "EvidenceAttachRequest",
      "type": "object"
    },
    "name": "evidence_attach",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "EvidenceKindDto": {
          "description": "Whether an evidence item is a managed file or repository reference.",
          "enum": [
            "managed_file",
            "repository"
          ],
          "type": "string"
        }
      },
      "description": "One evidence record as every client sees it.",
      "properties": {
        "commit_identity": {
          "type": [
            "string",
            "null"
          ]
        },
        "content_hash": {
          "type": [
            "string",
            "null"
          ]
        },
        "entity_id": {
          "type": "string"
        },
        "entity_kind": {
          "type": "string"
        },
        "evidence_kind": {
          "$ref": "#/definitions/EvidenceKindDto"
        },
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "type": "string"
        },
        "relative_path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "entity_id",
        "entity_kind",
        "evidence_kind",
        "id",
        "project_id"
      ],
      "title": "EvidenceRecord",
      "type": "object"
    }
  },
  {
    "description": "List evidence for a Project and append a timeline event.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `evidence.list` command.",
      "properties": {
        "entity_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "entity_kind": {
          "type": [
            "string",
            "null"
          ]
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "project_id"
      ],
      "title": "EvidenceListRequest",
      "type": "object"
    },
    "name": "evidence_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "EvidenceKindDto": {
          "description": "Whether an evidence item is a managed file or repository reference.",
          "enum": [
            "managed_file",
            "repository"
          ],
          "type": "string"
        },
        "EvidenceRecord": {
          "additionalProperties": false,
          "description": "One evidence record as every client sees it.",
          "properties": {
            "commit_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "content_hash": {
              "type": [
                "string",
                "null"
              ]
            },
            "entity_id": {
              "type": "string"
            },
            "entity_kind": {
              "type": "string"
            },
            "evidence_kind": {
              "$ref": "#/definitions/EvidenceKindDto"
            },
            "id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "type": "string"
            },
            "relative_path": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "entity_id",
            "entity_kind",
            "evidence_kind",
            "id",
            "project_id"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `evidence.list` command.",
      "properties": {
        "evidence": {
          "items": {
            "$ref": "#/definitions/EvidenceRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "evidence"
      ],
      "title": "EvidenceListResponse",
      "type": "object"
    }
  },
  {
    "description": "Return one Project's Herdr settings and connection diagnostics.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `herdr.settings.get` query.",
      "properties": {
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "HerdrSettingsGetQuery",
      "type": "object"
    },
    "name": "herdr_settings_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "HerdrConnectionDiagnostics": {
          "additionalProperties": false,
          "description": "Connection diagnostics for one Project's Herdr session.",
          "properties": {
            "connected": {
              "description": "Whether the per-session socket is connected.",
              "type": "boolean"
            },
            "herdr_workspace": {
              "description": "The Herdr workspace observed through the socket, if connected.",
              "type": [
                "string",
                "null"
              ]
            },
            "last_error": {
              "description": "The last connection or mapping error, if any.",
              "type": [
                "string",
                "null"
              ]
            },
            "last_snapshot_at": {
              "description": "When the last full snapshot was captured.",
              "type": [
                "string",
                "null"
              ]
            },
            "product_workspace": {
              "description": "The product Seed Workspace this session maps to.",
              "type": "string"
            },
            "session_name": {
              "description": "The exclusive named Herdr session.",
              "type": "string"
            }
          },
          "required": [
            "connected",
            "product_workspace",
            "session_name"
          ],
          "type": "object"
        },
        "HerdrProjectSettings": {
          "additionalProperties": false,
          "description": "Per-Project Herdr observation settings.",
          "properties": {
            "missing_result_deadline_secs": {
              "description": "After this many seconds a settled session is result-missing.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "polling_fallback_enabled": {
              "description": "Whether the ten-second whole-session polling fallback is on.",
              "type": "boolean"
            },
            "polling_fallback_interval_secs": {
              "description": "The whole-session polling interval when fallback is enabled.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "reconciliation_interval_secs": {
              "description": "How often reconciliation compares full session state.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "stall_deadline_secs": {
              "description": "After this many seconds a run is considered stale.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "version": {
              "description": "The optimistic version for updates.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "missing_result_deadline_secs",
            "polling_fallback_enabled",
            "polling_fallback_interval_secs",
            "reconciliation_interval_secs",
            "stall_deadline_secs",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `herdr.settings.get` query.",
      "properties": {
        "diagnostics": {
          "$ref": "#/definitions/HerdrConnectionDiagnostics"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "settings": {
          "$ref": "#/definitions/HerdrProjectSettings"
        }
      },
      "required": [
        "diagnostics",
        "project_id",
        "settings"
      ],
      "title": "HerdrSettingsGetResponse",
      "type": "object"
    }
  },
  {
    "description": "Update one Project's Herdr reconciliation, fallback polling, and deadlines.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `herdr.settings.update` command.",
      "properties": {
        "missing_result_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "polling_fallback_enabled": {
          "type": "boolean"
        },
        "polling_fallback_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reconciliation_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "mutation",
        "polling_fallback_enabled",
        "polling_fallback_interval_secs",
        "project_id",
        "reconciliation_interval_secs",
        "stall_deadline_secs"
      ],
      "title": "HerdrSettingsUpdateRequest",
      "type": "object"
    },
    "name": "herdr_settings_update",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Per-Project Herdr observation settings.",
      "properties": {
        "missing_result_deadline_secs": {
          "description": "After this many seconds a settled session is result-missing.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "polling_fallback_enabled": {
          "description": "Whether the ten-second whole-session polling fallback is on.",
          "type": "boolean"
        },
        "polling_fallback_interval_secs": {
          "description": "The whole-session polling interval when fallback is enabled.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reconciliation_interval_secs": {
          "description": "How often reconciliation compares full session state.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "description": "After this many seconds a run is considered stale.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The optimistic version for updates.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "polling_fallback_enabled",
        "polling_fallback_interval_secs",
        "reconciliation_interval_secs",
        "stall_deadline_secs",
        "version"
      ],
      "title": "HerdrProjectSettings",
      "type": "object"
    }
  },
  {
    "description": "Return global Herdr observation defaults.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `herdr.defaults.get` query.",
      "title": "HerdrDefaultsGetQuery",
      "type": "object"
    },
    "name": "herdr_defaults_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "HerdrGlobalDefaults": {
          "additionalProperties": false,
          "description": "Global defaults applied to new Projects and unset fields.",
          "properties": {
            "missing_result_deadline_secs": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "reconciliation_interval_secs": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "stall_deadline_secs": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "missing_result_deadline_secs",
            "reconciliation_interval_secs",
            "stall_deadline_secs",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `herdr.defaults.get` query.",
      "properties": {
        "defaults": {
          "$ref": "#/definitions/HerdrGlobalDefaults"
        }
      },
      "required": [
        "defaults"
      ],
      "title": "HerdrDefaultsGetResponse",
      "type": "object"
    }
  },
  {
    "description": "Update global Herdr reconciliation and deadline defaults.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `herdr.defaults.update` command.",
      "properties": {
        "missing_result_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "reconciliation_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "mutation",
        "reconciliation_interval_secs",
        "stall_deadline_secs"
      ],
      "title": "HerdrDefaultsUpdateRequest",
      "type": "object"
    },
    "name": "herdr_defaults_update",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Global defaults applied to new Projects and unset fields.",
      "properties": {
        "missing_result_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reconciliation_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "reconciliation_interval_secs",
        "stall_deadline_secs",
        "version"
      ],
      "title": "HerdrGlobalDefaults",
      "type": "object"
    }
  },
  {
    "description": "Register a Workspace path for a Project.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `workspace.register` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "path": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "path",
        "project_id"
      ],
      "title": "WorkspaceRegisterRequest",
      "type": "object"
    },
    "name": "workspace_register",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "WorkspaceHealthDto": {
          "description": "The closed health vocabulary as every client sees it.",
          "enum": [
            "available",
            "assigned",
            "dirty",
            "missing",
            "retired"
          ],
          "type": "string"
        },
        "WorkspaceObservationDto": {
          "additionalProperties": false,
          "description": "The git facts the core last observed without mutating the clone.",
          "properties": {
            "branch": {
              "type": [
                "string",
                "null"
              ]
            },
            "head": {
              "type": [
                "string",
                "null"
              ]
            },
            "lane_assignment": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "working_tree_clean": {
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "description": "The Workspace record as every client sees it.",
      "properties": {
        "health": {
          "$ref": "#/definitions/WorkspaceHealthDto"
        },
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "is_seed": {
          "type": "boolean"
        },
        "observation": {
          "$ref": "#/definitions/WorkspaceObservationDto"
        },
        "path": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "health",
        "id",
        "is_seed",
        "observation",
        "path",
        "project_id",
        "version"
      ],
      "title": "WorkspaceRecord",
      "type": "object"
    }
  },
  {
    "description": "Observe git state for a Workspace without mutating the clone.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `workspace.observe` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "workspace_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "workspace_id"
      ],
      "title": "WorkspaceObserveRequest",
      "type": "object"
    },
    "name": "workspace_observe",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "WorkspaceHealthDto": {
          "description": "The closed health vocabulary as every client sees it.",
          "enum": [
            "available",
            "assigned",
            "dirty",
            "missing",
            "retired"
          ],
          "type": "string"
        },
        "WorkspaceObservationDto": {
          "additionalProperties": false,
          "description": "The git facts the core last observed without mutating the clone.",
          "properties": {
            "branch": {
              "type": [
                "string",
                "null"
              ]
            },
            "head": {
              "type": [
                "string",
                "null"
              ]
            },
            "lane_assignment": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "working_tree_clean": {
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "description": "The Workspace record as every client sees it.",
      "properties": {
        "health": {
          "$ref": "#/definitions/WorkspaceHealthDto"
        },
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "is_seed": {
          "type": "boolean"
        },
        "observation": {
          "$ref": "#/definitions/WorkspaceObservationDto"
        },
        "path": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "health",
        "id",
        "is_seed",
        "observation",
        "path",
        "project_id",
        "version"
      ],
      "title": "WorkspaceRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Workspace for one Project with health and observation.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `workspace.list` query.",
      "properties": {
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "WorkspaceListQuery",
      "type": "object"
    },
    "name": "workspace_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "WorkspaceHealthDto": {
          "description": "The closed health vocabulary as every client sees it.",
          "enum": [
            "available",
            "assigned",
            "dirty",
            "missing",
            "retired"
          ],
          "type": "string"
        },
        "WorkspaceObservationDto": {
          "additionalProperties": false,
          "description": "The git facts the core last observed without mutating the clone.",
          "properties": {
            "branch": {
              "type": [
                "string",
                "null"
              ]
            },
            "head": {
              "type": [
                "string",
                "null"
              ]
            },
            "lane_assignment": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "working_tree_clean": {
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "WorkspaceRecord": {
          "additionalProperties": false,
          "description": "The Workspace record as every client sees it.",
          "properties": {
            "health": {
              "$ref": "#/definitions/WorkspaceHealthDto"
            },
            "id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "is_seed": {
              "type": "boolean"
            },
            "observation": {
              "$ref": "#/definitions/WorkspaceObservationDto"
            },
            "path": {
              "type": "string"
            },
            "project_id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "health",
            "id",
            "is_seed",
            "observation",
            "path",
            "project_id",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `workspace.list` query.",
      "properties": {
        "workspaces": {
          "items": {
            "$ref": "#/definitions/WorkspaceRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "workspaces"
      ],
      "title": "WorkspaceListResponse",
      "type": "object"
    }
  }
]

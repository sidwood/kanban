[
  {
    "description": "Return current core health for boot and diagnostics.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `health.get` query.",
      "title": "HealthQuery",
      "type": "object"
    },
    "name": "health_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Response payload for the `health.get` query.",
      "properties": {
        "connected": {
          "type": "boolean"
        },
        "service_version": {
          "type": "string"
        }
      },
      "required": [
        "connected",
        "service_version"
      ],
      "title": "HealthResponse",
      "type": "object"
    }
  },
  {
    "description": "Export one redacted diagnostic bundle of logs, health, and configuration under managed application data.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `diagnostics.export` query.",
      "title": "DiagnosticsExportQuery",
      "type": "object"
    },
    "name": "diagnostics_export",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Response payload for the `diagnostics.export` query.",
      "properties": {
        "bundle_dir": {
          "description": "The exported bundle directory under managed application data.",
          "type": "string"
        }
      },
      "required": [
        "bundle_dir"
      ],
      "title": "DiagnosticsExportResponse",
      "type": "object"
    }
  },
  {
    "description": "Create an Initiative folder with a validated name.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `initiative.create` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The intended name; blank names are refused.",
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "name"
      ],
      "title": "InitiativeCreateRequest",
      "type": "object"
    },
    "name": "initiative_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Initiative record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The trimmed name.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "id",
        "name",
        "version"
      ],
      "title": "InitiativeRecord",
      "type": "object"
    }
  },
  {
    "description": "Rename an active Initiative.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `initiative.rename` command.",
      "properties": {
        "initiative_id": {
          "description": "The Initiative being renamed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The intended name; blank names are refused.",
          "type": "string"
        }
      },
      "required": [
        "initiative_id",
        "mutation",
        "name"
      ],
      "title": "InitiativeRenameRequest",
      "type": "object"
    },
    "name": "initiative_rename",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Initiative record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The trimmed name.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "id",
        "name",
        "version"
      ],
      "title": "InitiativeRecord",
      "type": "object"
    }
  },
  {
    "description": "Archive an Initiative. Archiving is terminal and preserves every recorded fact.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `initiative.archive` command.",
      "properties": {
        "initiative_id": {
          "description": "The Initiative being archived.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        }
      },
      "required": [
        "initiative_id",
        "mutation"
      ],
      "title": "InitiativeArchiveRequest",
      "type": "object"
    },
    "name": "initiative_archive",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Initiative record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The trimmed name.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "id",
        "name",
        "version"
      ],
      "title": "InitiativeRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Initiative, archived ones included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `initiative.list` query.",
      "title": "InitiativeListQuery",
      "type": "object"
    },
    "name": "initiative_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "InitiativeRecord": {
          "additionalProperties": false,
          "description": "The Initiative record as every client sees it.",
          "properties": {
            "archived": {
              "description": "Whether the Initiative is archived. Archived Initiatives stay listed; no fact is ever removed.",
              "type": "boolean"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "name": {
              "description": "The trimmed name.",
              "type": "string"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "archived",
            "id",
            "name",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `initiative.list` query.",
      "properties": {
        "initiatives": {
          "description": "Every Initiative, archived included, newest last.",
          "items": {
            "$ref": "#/definitions/InitiativeRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "initiatives"
      ],
      "title": "InitiativeListResponse",
      "type": "object"
    }
  },
  {
    "description": "Register a Project with one Git repository, Seed Workspace, default branch, and exclusive Herdr session.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `project.register` command.",
      "properties": {
        "code": {
          "description": "The intended code; it must match `[A-Z][A-Z0-9]{1,7}` in full and `KAN` is reserved.",
          "type": "string"
        },
        "default_branch": {
          "description": "The default branch.",
          "type": "string"
        },
        "herdr_session": {
          "description": "The Herdr session name, if one is selected; absence selects Herdr's default session.",
          "type": [
            "string",
            "null"
          ]
        },
        "herdr_workspace": {
          "description": "The required target Herdr workspace.",
          "type": "string"
        },
        "initiative_id": {
          "description": "The Initiative the Project sits under, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The intended name; blank names are refused.",
          "type": "string"
        },
        "repository": {
          "description": "The target Git repository; non-Git targets are refused.",
          "type": "string"
        },
        "seed_workspace": {
          "description": "The Seed Workspace.",
          "type": "string"
        }
      },
      "required": [
        "code",
        "default_branch",
        "herdr_workspace",
        "mutation",
        "name",
        "repository",
        "seed_workspace"
      ],
      "title": "ProjectRegisterRequest",
      "type": "object"
    },
    "name": "project_register",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ProjectCounters": {
          "additionalProperties": false,
          "description": "The Plan, Spec, and Ticket counters of one Project: the last number minted per kind. Zero means nothing has been minted yet, and the counters survive archiving.",
          "properties": {
            "plan": {
              "description": "The last Plan number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec": {
              "description": "The last Spec number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket": {
              "description": "The last Ticket number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "plan",
            "spec",
            "ticket"
          ],
          "type": "object"
        }
      },
      "description": "The Project record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Project is archived. Archived Projects stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "code": {
          "description": "The immutable, globally unique Project code.",
          "type": "string"
        },
        "counters": {
          "allOf": [
            {
              "$ref": "#/definitions/ProjectCounters"
            }
          ],
          "description": "The Project's counters, preserved through every state."
        },
        "default_branch": {
          "description": "The one default branch.",
          "type": "string"
        },
        "herdr_session": {
          "description": "The named Herdr session, if the Project selected one; absence selects Herdr's default session.",
          "type": [
            "string",
            "null"
          ]
        },
        "herdr_workspace": {
          "description": "The one required target Herdr workspace.",
          "type": "string"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "initiative_id": {
          "description": "The Initiative the Project sits under, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "name": {
          "description": "The display name.",
          "type": "string"
        },
        "repository": {
          "description": "The one target Git repository.",
          "type": "string"
        },
        "seed_workspace": {
          "description": "The one Seed Workspace.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "code",
        "counters",
        "default_branch",
        "herdr_workspace",
        "id",
        "name",
        "repository",
        "seed_workspace",
        "version"
      ],
      "title": "ProjectRecord",
      "type": "object"
    }
  },
  {
    "description": "Archive a Project. Archiving is terminal and preserves every recorded fact.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `project.archive` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "description": "The Project being archived.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "project_id"
      ],
      "title": "ProjectArchiveRequest",
      "type": "object"
    },
    "name": "project_archive",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ProjectCounters": {
          "additionalProperties": false,
          "description": "The Plan, Spec, and Ticket counters of one Project: the last number minted per kind. Zero means nothing has been minted yet, and the counters survive archiving.",
          "properties": {
            "plan": {
              "description": "The last Plan number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec": {
              "description": "The last Spec number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket": {
              "description": "The last Ticket number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "plan",
            "spec",
            "ticket"
          ],
          "type": "object"
        }
      },
      "description": "The Project record as every client sees it.",
      "properties": {
        "archived": {
          "description": "Whether the Project is archived. Archived Projects stay listed; no fact is ever removed.",
          "type": "boolean"
        },
        "code": {
          "description": "The immutable, globally unique Project code.",
          "type": "string"
        },
        "counters": {
          "allOf": [
            {
              "$ref": "#/definitions/ProjectCounters"
            }
          ],
          "description": "The Project's counters, preserved through every state."
        },
        "default_branch": {
          "description": "The one default branch.",
          "type": "string"
        },
        "herdr_session": {
          "description": "The named Herdr session, if the Project selected one; absence selects Herdr's default session.",
          "type": [
            "string",
            "null"
          ]
        },
        "herdr_workspace": {
          "description": "The one required target Herdr workspace.",
          "type": "string"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "initiative_id": {
          "description": "The Initiative the Project sits under, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "name": {
          "description": "The display name.",
          "type": "string"
        },
        "repository": {
          "description": "The one target Git repository.",
          "type": "string"
        },
        "seed_workspace": {
          "description": "The one Seed Workspace.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "archived",
        "code",
        "counters",
        "default_branch",
        "herdr_workspace",
        "id",
        "name",
        "repository",
        "seed_workspace",
        "version"
      ],
      "title": "ProjectRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Project, archived ones included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `project.list` query.",
      "title": "ProjectListQuery",
      "type": "object"
    },
    "name": "project_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ProjectCounters": {
          "additionalProperties": false,
          "description": "The Plan, Spec, and Ticket counters of one Project: the last number minted per kind. Zero means nothing has been minted yet, and the counters survive archiving.",
          "properties": {
            "plan": {
              "description": "The last Plan number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec": {
              "description": "The last Spec number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket": {
              "description": "The last Ticket number minted.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "plan",
            "spec",
            "ticket"
          ],
          "type": "object"
        },
        "ProjectRecord": {
          "additionalProperties": false,
          "description": "The Project record as every client sees it.",
          "properties": {
            "archived": {
              "description": "Whether the Project is archived. Archived Projects stay listed; no fact is ever removed.",
              "type": "boolean"
            },
            "code": {
              "description": "The immutable, globally unique Project code.",
              "type": "string"
            },
            "counters": {
              "allOf": [
                {
                  "$ref": "#/definitions/ProjectCounters"
                }
              ],
              "description": "The Project's counters, preserved through every state."
            },
            "default_branch": {
              "description": "The one default branch.",
              "type": "string"
            },
            "herdr_session": {
              "description": "The named Herdr session, if the Project selected one; absence selects Herdr's default session.",
              "type": [
                "string",
                "null"
              ]
            },
            "herdr_workspace": {
              "description": "The one required target Herdr workspace.",
              "type": "string"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "initiative_id": {
              "description": "The Initiative the Project sits under, if any.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "name": {
              "description": "The display name.",
              "type": "string"
            },
            "repository": {
              "description": "The one target Git repository.",
              "type": "string"
            },
            "seed_workspace": {
              "description": "The one Seed Workspace.",
              "type": "string"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "archived",
            "code",
            "counters",
            "default_branch",
            "herdr_workspace",
            "id",
            "name",
            "repository",
            "seed_workspace",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `project.list` query.",
      "properties": {
        "projects": {
          "description": "Every Project, archived included, newest last.",
          "items": {
            "$ref": "#/definitions/ProjectRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "projects"
      ],
      "title": "ProjectListResponse",
      "type": "object"
    }
  },
  {
    "description": "Create a draft Plan, minting its number from the Project counter.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.create` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "description": "The Project the new Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "project_id"
      ],
      "title": "PlanCreateRequest",
      "type": "object"
    },
    "name": "plan_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Add a Spec to a draft Plan's membership and display order.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.spec.add` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan gaining a member.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_number": {
          "description": "The Spec number joining the Plan.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id",
        "spec_number"
      ],
      "title": "PlanSpecAddRequest",
      "type": "object"
    },
    "name": "plan_spec_add",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Remove a Spec from a draft Plan; a Spec carrying edges is refused.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.spec.remove` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan losing a member.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_number": {
          "description": "The Spec number leaving the Plan.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id",
        "spec_number"
      ],
      "title": "PlanSpecRemoveRequest",
      "type": "object"
    },
    "name": "plan_spec_remove",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Move a Spec within a draft Plan's display order.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.spec.move` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose display order changes.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "position": {
          "description": "The position in the display order the Spec moves to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_number": {
          "description": "The Spec number moving.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id",
        "position",
        "spec_number"
      ],
      "title": "PlanSpecMoveRequest",
      "type": "object"
    },
    "name": "plan_spec_move",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Add a dependency edge inside one draft Plan; an edge leaving the Plan is refused.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.edge.add` command.",
      "properties": {
        "from_spec": {
          "description": "The Spec that must land first.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose graph gains an edge.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "to_spec": {
          "description": "The Spec that waits on `from_spec`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "from_spec",
        "mutation",
        "plan_id",
        "to_spec"
      ],
      "title": "PlanEdgeAddRequest",
      "type": "object"
    },
    "name": "plan_edge_add",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Remove a dependency edge from a draft Plan.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.edge.remove` command.",
      "properties": {
        "from_spec": {
          "description": "The Spec that must land first.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose graph loses an edge.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "to_spec": {
          "description": "The Spec that waits on `from_spec`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "from_spec",
        "mutation",
        "plan_id",
        "to_spec"
      ],
      "title": "PlanEdgeRemoveRequest",
      "type": "object"
    },
    "name": "plan_edge_remove",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Freeze a draft Plan's membership, order, and graph into an immutable version.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.activate` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan freezing its shape into a version.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanActivateRequest",
      "type": "object"
    },
    "name": "plan_activate",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Reopen an active Plan's draft and reserve its auditable replacement version.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.replan` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan whose replacement version is reserved.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanReplanRequest",
      "type": "object"
    },
    "name": "plan_replan",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Complete an active Plan. Complete is terminal and off the active surface.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.complete` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan completing.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanCompleteRequest",
      "type": "object"
    },
    "name": "plan_complete",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Cancel a draft or active Plan. Cancelled is terminal and off the active surface.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.cancel` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan being cancelled.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanCancelRequest",
      "type": "object"
    },
    "name": "plan_cancel",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "Archive a Plan from any open state. Archiving is terminal and preserves every recorded fact.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `plan.archive` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan being archived. Archiving is terminal and preserves every recorded fact.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id"
      ],
      "title": "PlanArchiveRequest",
      "type": "object"
    },
    "name": "plan_archive",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
      "properties": {
        "edges": {
          "description": "The dependency edges, held separately from the display order.",
          "items": {
            "$ref": "#/definitions/PlanEdge"
          },
          "type": "array"
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "number": {
          "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The Project this Plan belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_numbers": {
          "description": "The display order: the member Spec numbers as a per-Plan sequence.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanState"
            }
          ],
          "description": "The lifecycle state."
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "id",
        "number",
        "project_id",
        "spec_numbers",
        "state",
        "version"
      ],
      "title": "PlanRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Plan of a Project, terminal states included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `plan.list` query.",
      "properties": {
        "project_id": {
          "description": "The Project whose Plans are listed, terminal states included.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "PlanListQuery",
      "type": "object"
    },
    "name": "plan_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanRecord": {
          "additionalProperties": false,
          "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
          "properties": {
            "edges": {
              "description": "The dependency edges, held separately from the display order.",
              "items": {
                "$ref": "#/definitions/PlanEdge"
              },
              "type": "array"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "number": {
              "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The Project this Plan belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec_numbers": {
              "description": "The display order: the member Spec numbers as a per-Plan sequence.",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "state": {
              "allOf": [
                {
                  "$ref": "#/definitions/PlanState"
                }
              ],
              "description": "The lifecycle state."
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "edges",
            "id",
            "number",
            "project_id",
            "spec_numbers",
            "state",
            "version"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        }
      },
      "description": "Response payload for the `plan.list` query.",
      "properties": {
        "plans": {
          "description": "Every Plan of the Project, newest last, all states included.",
          "items": {
            "$ref": "#/definitions/PlanRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "plans"
      ],
      "title": "PlanListResponse",
      "type": "object"
    }
  },
  {
    "description": "Read one Plan with every frozen version beside it.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `plan.get` query.",
      "properties": {
        "plan_id": {
          "description": "The Plan being read.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "plan_id"
      ],
      "title": "PlanGetQuery",
      "type": "object"
    },
    "name": "plan_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanEdge": {
          "additionalProperties": false,
          "description": "One dependency edge of a Plan's graph: `from_spec` must land before `to_spec` may begin.",
          "properties": {
            "from_spec": {
              "description": "The Spec that must land first.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "to_spec": {
              "description": "The Spec that waits on `from_spec`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_spec",
            "to_spec"
          ],
          "type": "object"
        },
        "PlanRecord": {
          "additionalProperties": false,
          "description": "The Plan record as every client sees it: the lifecycle state, the working display order, and the working dependency graph.",
          "properties": {
            "edges": {
              "description": "The dependency edges, held separately from the display order.",
              "items": {
                "$ref": "#/definitions/PlanEdge"
              },
              "type": "array"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "number": {
              "description": "The number this Project minted for this Plan; rendered with the Project's code, for example `CORE-P1`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The Project this Plan belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec_numbers": {
              "description": "The display order: the member Spec numbers as a per-Plan sequence.",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "state": {
              "allOf": [
                {
                  "$ref": "#/definitions/PlanState"
                }
              ],
              "description": "The lifecycle state."
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "edges",
            "id",
            "number",
            "project_id",
            "spec_numbers",
            "state",
            "version"
          ],
          "type": "object"
        },
        "PlanState": {
          "description": "The closed Plan lifecycle vocabulary on the wire.",
          "enum": [
            "draft",
            "active",
            "complete",
            "cancelled",
            "archived"
          ],
          "type": "string"
        },
        "PlanVersionRecord": {
          "additionalProperties": false,
          "description": "One frozen Plan version as every client sees it: the Spec membership, display order, and dependency graph exactly as they stood at activation. Immutable once minted.",
          "properties": {
            "edges": {
              "description": "The frozen dependency graph.",
              "items": {
                "$ref": "#/definitions/PlanEdge"
              },
              "type": "array"
            },
            "number": {
              "description": "The version's number; the first freeze is one.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "spec_numbers": {
              "description": "The frozen display order.",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            }
          },
          "required": [
            "edges",
            "number",
            "spec_numbers"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `plan.get` query: the Plan and every frozen version beside it, so prior versions stay queryable.",
      "properties": {
        "plan": {
          "allOf": [
            {
              "$ref": "#/definitions/PlanRecord"
            }
          ],
          "description": "The Plan's current record."
        },
        "versions": {
          "description": "Every frozen version, oldest first.",
          "items": {
            "$ref": "#/definitions/PlanVersionRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "plan",
        "versions"
      ],
      "title": "PlanGetResponse",
      "type": "object"
    }
  },
  {
    "description": "Report one Plan graph's blocking diagnostics: dependency cycles, story coverage gaps, and invalid profile references.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `plan.diagnostics` query.",
      "properties": {
        "plan_id": {
          "description": "The Plan whose graph is diagnosed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The frozen version whose graph is diagnosed; when `null` the working shape on display is read instead.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "plan_id"
      ],
      "title": "PlanDiagnosticsQuery",
      "type": "object"
    },
    "name": "plan_diagnostics",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "PlanCoverageGap": {
          "additionalProperties": false,
          "description": "One member Spec's coverage gap against the Plan: the User Stories its scope claims that no criterion claims yet (DR-PS-14).",
          "properties": {
            "claims_no_stories": {
              "description": "The version's story section names no User Story at all: an empty scope admits no coverage, so no Ticket graph could ever cover this Spec.",
              "type": "boolean"
            },
            "spec_number": {
              "description": "The member Spec whose stories stay uncovered.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "uncovered": {
              "description": "Every uncovered User Story, full identities in scope order.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "claims_no_stories",
            "spec_number",
            "uncovered"
          ],
          "type": "object"
        },
        "PlanCycle": {
          "additionalProperties": false,
          "description": "One dependency cycle the planning diagnostics report: a group of member Specs mutually reachable through the Plan's dependency edges, so no execution order exists among them (DR-PS-18).",
          "properties": {
            "spec_numbers": {
              "description": "The ring's member Spec numbers, ascending.",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            }
          },
          "required": [
            "spec_numbers"
          ],
          "type": "object"
        },
        "PlanInvalidProfile": {
          "additionalProperties": false,
          "description": "One profile reference that resolves to no catalogue entry. This is the invalid-profile diagnostics interface of DR-PS-18: the execution profile catalogue feeds the entries (KAN-S7, T38), so the list rides empty until the catalogue exists.",
          "properties": {
            "reference": {
              "description": "The referenced profile identity, as written where it was referenced.",
              "type": "string"
            }
          },
          "required": [
            "reference"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `plan.diagnostics` query: the blocking diagnostics of one graph (DR-PS-18).",
      "properties": {
        "blocking": {
          "description": "Whether any diagnostic blocks the Plan's Ticket graph from becoming executable: a cycle, a coverage gap, or an invalid profile reference.",
          "type": "boolean"
        },
        "coverage_gaps": {
          "description": "One entry per member Spec whose stories stay uncovered; the story-to-criterion-to-Ticket coverage matrix completes the picture with graph approval (T23).",
          "items": {
            "$ref": "#/definitions/PlanCoverageGap"
          },
          "type": "array"
        },
        "cycles": {
          "description": "Every dependency cycle the diagnosed graph holds.",
          "items": {
            "$ref": "#/definitions/PlanCycle"
          },
          "type": "array"
        },
        "invalid_profiles": {
          "description": "Profile references resolving to no catalogue entry; the catalogue feeds this list (KAN-S7, T38).",
          "items": {
            "$ref": "#/definitions/PlanInvalidProfile"
          },
          "type": "array"
        }
      },
      "required": [
        "blocking",
        "coverage_gaps",
        "cycles",
        "invalid_profiles"
      ],
      "title": "PlanDiagnosticsResponse",
      "type": "object"
    }
  },
  {
    "description": "Author a Spec with its opening PRD content, minting its number from the Project counter.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "SpecContent": {
          "additionalProperties": false,
          "description": "The nine PRD sections one Spec carries (DR-PS-07): the lightweight product requirements document of CONTEXT.md. Every section is free text; the name alone is required.",
          "properties": {
            "further_notes": {
              "description": "Anything else worth keeping beside the PRD.",
              "type": "string"
            },
            "implementation_decisions": {
              "description": "The settled implementation decisions.",
              "type": "string"
            },
            "name": {
              "description": "The Spec's name.",
              "type": "string"
            },
            "out_of_scope": {
              "description": "What this Spec deliberately does not deliver.",
              "type": "string"
            },
            "problem_statement": {
              "description": "The problem being solved.",
              "type": "string"
            },
            "short_description": {
              "description": "The one-line description.",
              "type": "string"
            },
            "solution": {
              "description": "The chosen solution.",
              "type": "string"
            },
            "testing_decisions": {
              "description": "The settled testing decisions.",
              "type": "string"
            },
            "user_stories": {
              "description": "The behaviour claims, one `US` bullet per line.",
              "type": "string"
            }
          },
          "required": [
            "further_notes",
            "implementation_decisions",
            "name",
            "out_of_scope",
            "problem_statement",
            "short_description",
            "solution",
            "testing_decisions",
            "user_stories"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `spec.create` command.",
      "properties": {
        "content": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecContent"
            }
          ],
          "description": "The opening PRD content, minted as draft version one."
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "description": "The Project the new Spec belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "content",
        "mutation",
        "project_id"
      ],
      "title": "SpecCreateRequest",
      "type": "object"
    },
    "name": "spec_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        }
      },
      "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
      "properties": {
        "execution": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecExecutionState"
            }
          ],
          "description": "The execution state (DR-PS-12)."
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The Spec's name, from the current content.",
          "type": "string"
        },
        "number": {
          "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "plan_id": {
          "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Spec belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "execution",
        "id",
        "name",
        "number",
        "project_id",
        "version"
      ],
      "title": "SpecRecord",
      "type": "object"
    }
  },
  {
    "description": "Update a Spec's working content; a material change past approval mints a new draft version.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "SpecContent": {
          "additionalProperties": false,
          "description": "The nine PRD sections one Spec carries (DR-PS-07): the lightweight product requirements document of CONTEXT.md. Every section is free text; the name alone is required.",
          "properties": {
            "further_notes": {
              "description": "Anything else worth keeping beside the PRD.",
              "type": "string"
            },
            "implementation_decisions": {
              "description": "The settled implementation decisions.",
              "type": "string"
            },
            "name": {
              "description": "The Spec's name.",
              "type": "string"
            },
            "out_of_scope": {
              "description": "What this Spec deliberately does not deliver.",
              "type": "string"
            },
            "problem_statement": {
              "description": "The problem being solved.",
              "type": "string"
            },
            "short_description": {
              "description": "The one-line description.",
              "type": "string"
            },
            "solution": {
              "description": "The chosen solution.",
              "type": "string"
            },
            "testing_decisions": {
              "description": "The settled testing decisions.",
              "type": "string"
            },
            "user_stories": {
              "description": "The behaviour claims, one `US` bullet per line.",
              "type": "string"
            }
          },
          "required": [
            "further_notes",
            "implementation_decisions",
            "name",
            "out_of_scope",
            "problem_statement",
            "short_description",
            "solution",
            "testing_decisions",
            "user_stories"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `spec.content.update` command.",
      "properties": {
        "content": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecContent"
            }
          ],
          "description": "The new content. A draft is edited in place; content that has moved on mints a new draft version (DR-PS-10)."
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "spec_id": {
          "description": "The Spec whose working content changes.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "content",
        "mutation",
        "spec_id"
      ],
      "title": "SpecContentUpdateRequest",
      "type": "object"
    },
    "name": "spec_content_update",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        }
      },
      "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
      "properties": {
        "execution": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecExecutionState"
            }
          ],
          "description": "The execution state (DR-PS-12)."
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The Spec's name, from the current content.",
          "type": "string"
        },
        "number": {
          "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "plan_id": {
          "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Spec belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "execution",
        "id",
        "name",
        "number",
        "project_id",
        "version"
      ],
      "title": "SpecRecord",
      "type": "object"
    }
  },
  {
    "description": "Approve the working draft into immutable operative content.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `spec.version.approve` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "spec_id": {
          "description": "The Spec whose draft version becomes approved.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "spec_id"
      ],
      "title": "SpecVersionApproveRequest",
      "type": "object"
    },
    "name": "spec_version_approve",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        }
      },
      "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
      "properties": {
        "execution": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecExecutionState"
            }
          ],
          "description": "The execution state (DR-PS-12)."
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The Spec's name, from the current content.",
          "type": "string"
        },
        "number": {
          "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "plan_id": {
          "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Spec belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "execution",
        "id",
        "name",
        "number",
        "project_id",
        "version"
      ],
      "title": "SpecRecord",
      "type": "object"
    }
  },
  {
    "description": "Supersede one content version explicitly. Superseded versions stay queryable for pinned Tickets.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `spec.version.supersede` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "spec_id": {
          "description": "The Spec owning the version being superseded.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The version being superseded, named explicitly (DR-PS-11).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "spec_id",
        "version"
      ],
      "title": "SpecVersionSupersedeRequest",
      "type": "object"
    },
    "name": "spec_version_supersede",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        }
      },
      "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
      "properties": {
        "execution": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecExecutionState"
            }
          ],
          "description": "The execution state (DR-PS-12)."
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The Spec's name, from the current content.",
          "type": "string"
        },
        "number": {
          "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "plan_id": {
          "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Spec belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "execution",
        "id",
        "name",
        "number",
        "project_id",
        "version"
      ],
      "title": "SpecRecord",
      "type": "object"
    }
  },
  {
    "description": "Join an unplanned Spec to the Plan holding its number, planning its execution.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `spec.plan.join` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "plan_id": {
          "description": "The Plan the Spec joins. Must hold the Spec's number and belong to the same Project.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_id": {
          "description": "The Spec joining a Plan.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "plan_id",
        "spec_id"
      ],
      "title": "SpecPlanJoinRequest",
      "type": "object"
    },
    "name": "spec_plan_join",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        }
      },
      "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
      "properties": {
        "execution": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecExecutionState"
            }
          ],
          "description": "The execution state (DR-PS-12)."
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The Spec's name, from the current content.",
          "type": "string"
        },
        "number": {
          "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "plan_id": {
          "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Spec belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "execution",
        "id",
        "name",
        "number",
        "project_id",
        "version"
      ],
      "title": "SpecRecord",
      "type": "object"
    }
  },
  {
    "description": "Move Spec execution along its closed state set, independently of content.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        }
      },
      "description": "Request payload for the `spec.execution.move` command.",
      "properties": {
        "execution": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecExecutionState"
            }
          ],
          "description": "The target state, which must be a legal transition away (DR-PS-12). `planned` is reached by joining a Plan, never by this command."
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "spec_id": {
          "description": "The Spec whose execution moves.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "execution",
        "mutation",
        "spec_id"
      ],
      "title": "SpecExecutionMoveRequest",
      "type": "object"
    },
    "name": "spec_execution_move",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        }
      },
      "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
      "properties": {
        "execution": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecExecutionState"
            }
          ],
          "description": "The execution state (DR-PS-12)."
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "The Spec's name, from the current content.",
          "type": "string"
        },
        "number": {
          "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "plan_id": {
          "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Spec belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "execution",
        "id",
        "name",
        "number",
        "project_id",
        "version"
      ],
      "title": "SpecRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Spec of a Project, terminal execution states included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `spec.list` query.",
      "properties": {
        "project_id": {
          "description": "The Project whose Specs are listed, terminal execution states included.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "SpecListQuery",
      "type": "object"
    },
    "name": "spec_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        },
        "SpecRecord": {
          "additionalProperties": false,
          "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
          "properties": {
            "execution": {
              "allOf": [
                {
                  "$ref": "#/definitions/SpecExecutionState"
                }
              ],
              "description": "The execution state (DR-PS-12)."
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "name": {
              "description": "The Spec's name, from the current content.",
              "type": "string"
            },
            "number": {
              "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "plan_id": {
              "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "project_id": {
              "description": "The Project this Spec belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "execution",
            "id",
            "name",
            "number",
            "project_id",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `spec.list` query.",
      "properties": {
        "specs": {
          "description": "Every Spec of the Project, oldest first, all execution states included.",
          "items": {
            "$ref": "#/definitions/SpecRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "specs"
      ],
      "title": "SpecListResponse",
      "type": "object"
    }
  },
  {
    "description": "Read one Spec with every content version beside it.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `spec.get` query.",
      "properties": {
        "spec_id": {
          "description": "The Spec being read.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "spec_id"
      ],
      "title": "SpecGetQuery",
      "type": "object"
    },
    "name": "spec_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecContent": {
          "additionalProperties": false,
          "description": "The nine PRD sections one Spec carries (DR-PS-07): the lightweight product requirements document of CONTEXT.md. Every section is free text; the name alone is required.",
          "properties": {
            "further_notes": {
              "description": "Anything else worth keeping beside the PRD.",
              "type": "string"
            },
            "implementation_decisions": {
              "description": "The settled implementation decisions.",
              "type": "string"
            },
            "name": {
              "description": "The Spec's name.",
              "type": "string"
            },
            "out_of_scope": {
              "description": "What this Spec deliberately does not deliver.",
              "type": "string"
            },
            "problem_statement": {
              "description": "The problem being solved.",
              "type": "string"
            },
            "short_description": {
              "description": "The one-line description.",
              "type": "string"
            },
            "solution": {
              "description": "The chosen solution.",
              "type": "string"
            },
            "testing_decisions": {
              "description": "The settled testing decisions.",
              "type": "string"
            },
            "user_stories": {
              "description": "The behaviour claims, one `US` bullet per line.",
              "type": "string"
            }
          },
          "required": [
            "further_notes",
            "implementation_decisions",
            "name",
            "out_of_scope",
            "problem_statement",
            "short_description",
            "solution",
            "testing_decisions",
            "user_stories"
          ],
          "type": "object"
        },
        "SpecContentState": {
          "description": "The closed content-version vocabulary on the wire (DR-PS-08).",
          "enum": [
            "draft",
            "approved",
            "superseded"
          ],
          "type": "string"
        },
        "SpecExecutionState": {
          "description": "The closed Spec execution vocabulary on the wire (DR-PS-12), tracked separately from every content version.",
          "enum": [
            "unplanned",
            "planned",
            "blocked",
            "ready",
            "active",
            "integration_review",
            "complete",
            "cancelled"
          ],
          "type": "string"
        },
        "SpecRecord": {
          "additionalProperties": false,
          "description": "The Spec record as every client sees it: the Project it belongs to, the number that Project minted, the execution tracked separately from content, and the Plan it belongs to once planned.",
          "properties": {
            "execution": {
              "allOf": [
                {
                  "$ref": "#/definitions/SpecExecutionState"
                }
              ],
              "description": "The execution state (DR-PS-12)."
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "name": {
              "description": "The Spec's name, from the current content.",
              "type": "string"
            },
            "number": {
              "description": "The number this Project minted for this Spec; rendered with the Project's code, for example `CORE-S1`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "plan_id": {
              "description": "The Plan this Spec belongs to once planned; `None` while unplanned (DR-PS-06).",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "project_id": {
              "description": "The Project this Spec belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "execution",
            "id",
            "name",
            "number",
            "project_id",
            "version"
          ],
          "type": "object"
        },
        "SpecVersionRecord": {
          "additionalProperties": false,
          "description": "One Spec content version as every client sees it: the PRD exactly as it stood when the version was minted or last drafted. Approved and superseded versions never change again, and superseded ones stay readable so Ticket pins resolve (DR-PS-11).",
          "properties": {
            "content": {
              "allOf": [
                {
                  "$ref": "#/definitions/SpecContent"
                }
              ],
              "description": "The PRD this version carries."
            },
            "number": {
              "description": "The version's number; the first content is one and every material change mints the next.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "state": {
              "allOf": [
                {
                  "$ref": "#/definitions/SpecContentState"
                }
              ],
              "description": "The version's lifecycle state."
            }
          },
          "required": [
            "content",
            "number",
            "state"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `spec.get` query: the Spec and every content version beside it, so prior versions stay queryable for diffing and for Ticket pins.",
      "properties": {
        "spec": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecRecord"
            }
          ],
          "description": "The Spec's current record."
        },
        "versions": {
          "description": "Every content version, oldest first.",
          "items": {
            "$ref": "#/definitions/SpecVersionRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "spec",
        "versions"
      ],
      "title": "SpecGetResponse",
      "type": "object"
    }
  },
  {
    "description": "Read one content version, superseded versions included, as Ticket pins resolve.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `spec.version.get` query.",
      "properties": {
        "number": {
          "description": "The version being read — the pin a Ticket resolves through, superseded versions included.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "spec_id": {
          "description": "The Spec owning the version.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "number",
        "spec_id"
      ],
      "title": "SpecVersionGetQuery",
      "type": "object"
    },
    "name": "spec_version_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "SpecContent": {
          "additionalProperties": false,
          "description": "The nine PRD sections one Spec carries (DR-PS-07): the lightweight product requirements document of CONTEXT.md. Every section is free text; the name alone is required.",
          "properties": {
            "further_notes": {
              "description": "Anything else worth keeping beside the PRD.",
              "type": "string"
            },
            "implementation_decisions": {
              "description": "The settled implementation decisions.",
              "type": "string"
            },
            "name": {
              "description": "The Spec's name.",
              "type": "string"
            },
            "out_of_scope": {
              "description": "What this Spec deliberately does not deliver.",
              "type": "string"
            },
            "problem_statement": {
              "description": "The problem being solved.",
              "type": "string"
            },
            "short_description": {
              "description": "The one-line description.",
              "type": "string"
            },
            "solution": {
              "description": "The chosen solution.",
              "type": "string"
            },
            "testing_decisions": {
              "description": "The settled testing decisions.",
              "type": "string"
            },
            "user_stories": {
              "description": "The behaviour claims, one `US` bullet per line.",
              "type": "string"
            }
          },
          "required": [
            "further_notes",
            "implementation_decisions",
            "name",
            "out_of_scope",
            "problem_statement",
            "short_description",
            "solution",
            "testing_decisions",
            "user_stories"
          ],
          "type": "object"
        },
        "SpecContentState": {
          "description": "The closed content-version vocabulary on the wire (DR-PS-08).",
          "enum": [
            "draft",
            "approved",
            "superseded"
          ],
          "type": "string"
        }
      },
      "description": "One Spec content version as every client sees it: the PRD exactly as it stood when the version was minted or last drafted. Approved and superseded versions never change again, and superseded ones stay readable so Ticket pins resolve (DR-PS-11).",
      "properties": {
        "content": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecContent"
            }
          ],
          "description": "The PRD this version carries."
        },
        "number": {
          "description": "The version's number; the first content is one and every material change mints the next.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/SpecContentState"
            }
          ],
          "description": "The version's lifecycle state."
        }
      },
      "required": [
        "content",
        "number",
        "state"
      ],
      "title": "SpecVersionRecord",
      "type": "object"
    }
  },
  {
    "description": "Check one Spec version's story coverage against proposed criteria; the executable gate refuses uncovered stories.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "CoverageCriterionProposal": {
          "additionalProperties": false,
          "description": "One proposed Acceptance Criterion: an observable outcome and the User Stories it claims, named like `CORE-S3-US6`.",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims, one full identity each.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `spec.coverage.check` query.",
      "properties": {
        "criteria": {
          "description": "The proposed criteria of the Ticket graph, every ticket's criteria together.",
          "items": {
            "$ref": "#/definitions/CoverageCriterionProposal"
          },
          "type": "array"
        },
        "spec_id": {
          "description": "The Spec whose pinned version defines the story scope.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The content version the Ticket graph would deliver.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "criteria",
        "spec_id",
        "version"
      ],
      "title": "SpecCoverageCheckQuery",
      "type": "object"
    },
    "name": "spec_coverage_check",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "CriterionRefusal": {
          "description": "Why one proposed Acceptance Criterion was refused.",
          "oneOf": [
            {
              "description": "The outcome states nothing.",
              "enum": [
                "no_outcome"
              ],
              "type": "string"
            },
            {
              "description": "No User Story is linked (DR-PS-13).",
              "enum": [
                "unlinked"
              ],
              "type": "string"
            },
            {
              "description": "The outcome is a technical command (DR-PS-15).",
              "enum": [
                "technical_command"
              ],
              "type": "string"
            },
            {
              "description": "A story link names no User Story of this form.",
              "enum": [
                "malformed_story"
              ],
              "type": "string"
            },
            {
              "description": "A story link names another Project's story.",
              "enum": [
                "foreign_story"
              ],
              "type": "string"
            }
          ]
        },
        "RefusedCriterion": {
          "additionalProperties": false,
          "description": "One refused proposal with the rule it broke.",
          "properties": {
            "outcome": {
              "description": "The refused proposal's outcome text.",
              "type": "string"
            },
            "reason": {
              "allOf": [
                {
                  "$ref": "#/definitions/CriterionRefusal"
                }
              ],
              "description": "Why the domain rules refused it."
            }
          },
          "required": [
            "outcome",
            "reason"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `spec.coverage.check` query.",
      "properties": {
        "executable": {
          "description": "Whether the executable gate passes: no uncovered story and no refused proposal.",
          "type": "boolean"
        },
        "refused": {
          "description": "The proposals the domain rules refused, in proposal order.",
          "items": {
            "$ref": "#/definitions/RefusedCriterion"
          },
          "type": "array"
        },
        "scope": {
          "description": "Every User Story the pinned version claims, full identities in scope order.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "uncovered": {
          "description": "The stories no criterion claims, in scope order (DR-PS-14).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "executable",
        "refused",
        "scope",
        "uncovered"
      ],
      "title": "SpecCoverageCheckResponse",
      "type": "object"
    }
  },
  {
    "description": "Create a Ticket under its kind's schema, minting its number from the Project counter.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "TaskMode": {
          "description": "The closed Task mode vocabulary on the wire (KAN-S4-US4): whether a human or an agent executes the bounded work.",
          "oneOf": [
            {
              "description": "Sid executes the work.",
              "enum": [
                "human"
              ],
              "type": "string"
            },
            {
              "description": "An agent executes the work under dispatch.",
              "enum": [
                "agent"
              ],
              "type": "string"
            }
          ]
        },
        "TaskSubtype": {
          "description": "The closed Task subtype vocabulary on the wire (DR-TK-06): the seven bounded flavours of non-story work.",
          "oneOf": [
            {
              "description": "Keeping the product and its operations running.",
              "enum": [
                "operational"
              ],
              "type": "string"
            },
            {
              "description": "Finding out what is true before acting on it.",
              "enum": [
                "investigative"
              ],
              "type": "string"
            },
            {
              "description": "Bookkeeping and record-keeping work.",
              "enum": [
                "administrative"
              ],
              "type": "string"
            },
            {
              "description": "Learning without a deliverable committed up front.",
              "enum": [
                "research"
              ],
              "type": "string"
            },
            {
              "description": "Proving an approach before committing to it.",
              "enum": [
                "prototype"
              ],
              "type": "string"
            },
            {
              "description": "Moving work or data from one place to another.",
              "enum": [
                "migration"
              ],
              "type": "string"
            },
            {
              "description": "Work done by hand, on purpose.",
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          ]
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketKind": {
          "description": "The closed Ticket kind vocabulary on the wire (DR-TK-01).",
          "oneOf": [
            {
              "description": "A small vertical product slice of exactly one Spec.",
              "enum": [
                "implementation"
              ],
              "type": "string"
            },
            {
              "description": "A record of incorrect behaviour, with qualification evidence.",
              "enum": [
                "bug"
              ],
              "type": "string"
            },
            {
              "description": "Bounded operational, investigative, or administrative work.",
              "enum": [
                "task"
              ],
              "type": "string"
            }
          ]
        },
        "TicketPriority": {
          "description": "The closed priority vocabulary on the wire (DR-LC-12): urgent, high, normal, low.",
          "oneOf": [
            {
              "description": "Ahead of everything else.",
              "enum": [
                "urgent"
              ],
              "type": "string"
            },
            {
              "description": "Ahead of normal work.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "The everyday order.",
              "enum": [
                "normal"
              ],
              "type": "string"
            },
            {
              "description": "Behind normal work.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Request payload for the `ticket.create` command. Each kind sends exactly its own fields: an Implementation names its Spec, slice, and criteria; a Bug names its quick-capture facts; a Task names its title, subtype, mode, completion criteria, and optional timing. Fields the kind does not carry are simply absent.",
      "properties": {
        "actual_behaviour": {
          "description": "The Bug's actual behaviour (DR-TK-08); required for a Bug.",
          "type": [
            "string",
            "null"
          ]
        },
        "completion": {
          "description": "The Task's completion criteria: observable outcomes that bound the work, with no story links.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "criteria": {
          "description": "The Implementation's story-linked criteria. A Task never carries these (DR-TK-07); it sends `completion` instead.",
          "items": {
            "$ref": "#/definitions/TicketCriterion"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "due": {
          "description": "The Task's due date, RFC 3339.",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketKind"
            }
          ],
          "description": "The kind whose schema the Ticket carries."
        },
        "mode": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's human-or-agent mode."
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "priority": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketPriority"
            }
          ],
          "description": "The Ticket's priority (DR-LC-12)."
        },
        "project_id": {
          "description": "The Project the new Ticket belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reporter_evidence": {
          "description": "The Bug's reporter evidence (DR-TK-08); required for a Bug.",
          "type": [
            "string",
            "null"
          ]
        },
        "scheduled_for": {
          "description": "The Task's one-time activation instant, RFC 3339; stored for KAN-S11, whose activation behaviour is KAN-T53 and KAN-T54.",
          "type": [
            "string",
            "null"
          ]
        },
        "slice": {
          "description": "The Implementation slice description, naming the behaviour delivered end to end.",
          "type": [
            "string",
            "null"
          ]
        },
        "spec_id": {
          "description": "The Spec this Ticket attaches to. An Implementation must name exactly one (DR-TK-02); a Bug or Task may name one or none (DR-TK-03).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "subtype": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskSubtype"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's subtype; a Task names exactly one (DR-TK-06)."
        },
        "title": {
          "description": "The Bug or Task title.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind",
        "mutation",
        "priority",
        "project_id"
      ],
      "title": "TicketCreateRequest",
      "type": "object"
    },
    "name": "ticket_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TaskMode": {
          "description": "The closed Task mode vocabulary on the wire (KAN-S4-US4): whether a human or an agent executes the bounded work.",
          "oneOf": [
            {
              "description": "Sid executes the work.",
              "enum": [
                "human"
              ],
              "type": "string"
            },
            {
              "description": "An agent executes the work under dispatch.",
              "enum": [
                "agent"
              ],
              "type": "string"
            }
          ]
        },
        "TaskSubtype": {
          "description": "The closed Task subtype vocabulary on the wire (DR-TK-06): the seven bounded flavours of non-story work.",
          "oneOf": [
            {
              "description": "Keeping the product and its operations running.",
              "enum": [
                "operational"
              ],
              "type": "string"
            },
            {
              "description": "Finding out what is true before acting on it.",
              "enum": [
                "investigative"
              ],
              "type": "string"
            },
            {
              "description": "Bookkeeping and record-keeping work.",
              "enum": [
                "administrative"
              ],
              "type": "string"
            },
            {
              "description": "Learning without a deliverable committed up front.",
              "enum": [
                "research"
              ],
              "type": "string"
            },
            {
              "description": "Proving an approach before committing to it.",
              "enum": [
                "prototype"
              ],
              "type": "string"
            },
            {
              "description": "Moving work or data from one place to another.",
              "enum": [
                "migration"
              ],
              "type": "string"
            },
            {
              "description": "Work done by hand, on purpose.",
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          ]
        },
        "TicketBugQualification": {
          "additionalProperties": false,
          "description": "One complete Bug qualification (DR-TK-09): the ten facts a Bug needs before it may leave draft, with the severity qualification assigned (DR-LC-13). Sent whole; qualification is one act.",
          "properties": {
            "affected_scope": {
              "description": "What the defect affects.",
              "type": "string"
            },
            "criteria": {
              "description": "The story-linked Acceptance Criteria, at least one.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "environment": {
              "description": "Where the defect occurs.",
              "type": "string"
            },
            "expected_behaviour": {
              "description": "The behaviour that should have happened.",
              "type": "string"
            },
            "frequency": {
              "description": "How often the defect occurs.",
              "type": "string"
            },
            "reproduction": {
              "description": "The reproduction steps or the failing test demonstrating the defect.",
              "type": "string"
            },
            "risk": {
              "description": "What the defect puts at risk.",
              "type": "string"
            },
            "severity": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketSeverity"
                }
              ],
              "description": "The severity qualification assigned (DR-LC-13)."
            },
            "verification_steps": {
              "description": "The Verification Steps, at least one.",
              "items": {
                "$ref": "#/definitions/TicketVerificationStep"
              },
              "type": "array"
            }
          },
          "required": [
            "affected_scope",
            "criteria",
            "environment",
            "expected_behaviour",
            "frequency",
            "reproduction",
            "risk",
            "severity",
            "verification_steps"
          ],
          "type": "object"
        },
        "TicketBugRecord": {
          "additionalProperties": false,
          "description": "The Bug-specific body of a Ticket record: the quick-capture facts, the qualification once one exists, and the vendor-neutral collections the Bug carries (DR-TK-08 to DR-TK-10).",
          "properties": {
            "actual_behaviour": {
              "description": "What the Bug records happening, as the reporter stated it.",
              "type": "string"
            },
            "evidence_ids": {
              "description": "The identities of the Evidence Items this Bug carries (DR-TK-10).",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "external_references": {
              "description": "The External References, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketExternalReference"
              },
              "type": "array"
            },
            "occurrence_snapshots": {
              "description": "The Occurrence Snapshots, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketOccurrenceSnapshot"
              },
              "type": "array"
            },
            "qualification": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TicketBugQualification"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The complete qualification, once one exists (DR-TK-09)."
            },
            "reporter_evidence": {
              "description": "The evidence the reporter holds, as the reporter stated it.",
              "type": "string"
            }
          },
          "required": [
            "actual_behaviour",
            "evidence_ids",
            "external_references",
            "occurrence_snapshots",
            "reporter_evidence"
          ],
          "type": "object"
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketExternalReference": {
          "additionalProperties": false,
          "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
          "properties": {
            "label": {
              "description": "The reference's label, when it carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "The referenced URI.",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "TicketKind": {
          "description": "The closed Ticket kind vocabulary on the wire (DR-TK-01).",
          "oneOf": [
            {
              "description": "A small vertical product slice of exactly one Spec.",
              "enum": [
                "implementation"
              ],
              "type": "string"
            },
            {
              "description": "A record of incorrect behaviour, with qualification evidence.",
              "enum": [
                "bug"
              ],
              "type": "string"
            },
            {
              "description": "Bounded operational, investigative, or administrative work.",
              "enum": [
                "task"
              ],
              "type": "string"
            }
          ]
        },
        "TicketOccurrenceSnapshot": {
          "additionalProperties": false,
          "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
          "properties": {
            "observation": {
              "description": "What was observed at that moment.",
              "type": "string"
            },
            "observed_at": {
              "description": "The observed moment, RFC 3339.",
              "type": "string"
            }
          },
          "required": [
            "observation",
            "observed_at"
          ],
          "type": "object"
        },
        "TicketPriority": {
          "description": "The closed priority vocabulary on the wire (DR-LC-12): urgent, high, normal, low.",
          "oneOf": [
            {
              "description": "Ahead of everything else.",
              "enum": [
                "urgent"
              ],
              "type": "string"
            },
            {
              "description": "Ahead of normal work.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "The everyday order.",
              "enum": [
                "normal"
              ],
              "type": "string"
            },
            {
              "description": "Behind normal work.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketSeverity": {
          "description": "The closed Bug severity vocabulary on the wire (DR-LC-13): critical, high, medium, low. Severity arrives only inside a qualification (DR-TK-09).",
          "oneOf": [
            {
              "description": "Data loss, secret exposure, or unauthorised execution.",
              "enum": [
                "critical"
              ],
              "type": "string"
            },
            {
              "description": "A failed approved criterion or workflow invariant.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "Usable behaviour, wrongly shaped.",
              "enum": [
                "medium"
              ],
              "type": "string"
            },
            {
              "description": "Cosmetic or local clarity.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        },
        "TicketVerificationStep": {
          "additionalProperties": false,
          "description": "One Verification Step a Ticket claims: the command or scripted procedure that demonstrates a criterion (DR-PS-15). Commands live here and never as criteria.",
          "properties": {
            "command": {
              "description": "The command as it runs.",
              "type": "string"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "description": "The Ticket record as every client sees it: the Project it belongs to, the number that Project minted, the kind whose schema it carries, the kind-specific fields — a title for Bugs and Tasks, a slice and criteria for Implementations, the Bug body for a Bug, and for Tasks the subtype, mode, completion criteria, and optional timing — and the Execution Profile the assignment names, if it carries one.",
      "properties": {
        "bug": {
          "anyOf": [
            {
              "$ref": "#/definitions/TicketBugRecord"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Bug body, if this Ticket carries one."
        },
        "completion": {
          "description": "The Task's completion criteria; empty for every other kind.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "criteria": {
          "description": "The Implementation's story-linked criteria; empty for every other kind.",
          "items": {
            "$ref": "#/definitions/TicketCriterion"
          },
          "type": "array"
        },
        "due": {
          "description": "The Task's due date, if it carries one; RFC 3339 in UTC.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketKind"
            }
          ],
          "description": "The kind whose schema this Ticket carries."
        },
        "mode": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's human-or-agent mode, if this Ticket carries one."
        },
        "number": {
          "description": "The number this Project minted for this Ticket; rendered with the Project's code, for example `CORE-T17`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "priority": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketPriority"
            }
          ],
          "description": "The Ticket's priority (DR-LC-12)."
        },
        "profile": {
          "description": "The Execution Profile this Ticket's assignment names, by its catalogue name. The reference keeps its name through every later catalogue change (DR-EP-05).",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Ticket belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "scheduled_for": {
          "description": "The Task's one-time activation instant, if it carries one; RFC 3339 in UTC, stored for KAN-S11.",
          "type": [
            "string",
            "null"
          ]
        },
        "slice": {
          "description": "The Implementation slice description, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "spec_id": {
          "description": "The Spec this Ticket attaches to, if it attaches to one.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketState"
            }
          ],
          "description": "The lifecycle state (DR-LC-01)."
        },
        "subtype": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskSubtype"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's subtype, if this Ticket carries one."
        },
        "title": {
          "description": "The Bug or Task title, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "completion",
        "criteria",
        "id",
        "kind",
        "number",
        "priority",
        "project_id",
        "state",
        "version"
      ],
      "title": "TicketRecord",
      "type": "object"
    }
  },
  {
    "description": "Qualify one Bug with its complete qualification, severity included; the Bug stays draft and readiness stays computed.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "TicketBugQualification": {
          "additionalProperties": false,
          "description": "One complete Bug qualification (DR-TK-09): the ten facts a Bug needs before it may leave draft, with the severity qualification assigned (DR-LC-13). Sent whole; qualification is one act.",
          "properties": {
            "affected_scope": {
              "description": "What the defect affects.",
              "type": "string"
            },
            "criteria": {
              "description": "The story-linked Acceptance Criteria, at least one.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "environment": {
              "description": "Where the defect occurs.",
              "type": "string"
            },
            "expected_behaviour": {
              "description": "The behaviour that should have happened.",
              "type": "string"
            },
            "frequency": {
              "description": "How often the defect occurs.",
              "type": "string"
            },
            "reproduction": {
              "description": "The reproduction steps or the failing test demonstrating the defect.",
              "type": "string"
            },
            "risk": {
              "description": "What the defect puts at risk.",
              "type": "string"
            },
            "severity": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketSeverity"
                }
              ],
              "description": "The severity qualification assigned (DR-LC-13)."
            },
            "verification_steps": {
              "description": "The Verification Steps, at least one.",
              "items": {
                "$ref": "#/definitions/TicketVerificationStep"
              },
              "type": "array"
            }
          },
          "required": [
            "affected_scope",
            "criteria",
            "environment",
            "expected_behaviour",
            "frequency",
            "reproduction",
            "risk",
            "severity",
            "verification_steps"
          ],
          "type": "object"
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketSeverity": {
          "description": "The closed Bug severity vocabulary on the wire (DR-LC-13): critical, high, medium, low. Severity arrives only inside a qualification (DR-TK-09).",
          "oneOf": [
            {
              "description": "Data loss, secret exposure, or unauthorised execution.",
              "enum": [
                "critical"
              ],
              "type": "string"
            },
            {
              "description": "A failed approved criterion or workflow invariant.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "Usable behaviour, wrongly shaped.",
              "enum": [
                "medium"
              ],
              "type": "string"
            },
            {
              "description": "Cosmetic or local clarity.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketVerificationStep": {
          "additionalProperties": false,
          "description": "One Verification Step a Ticket claims: the command or scripted procedure that demonstrates a criterion (DR-PS-15). Commands live here and never as criteria.",
          "properties": {
            "command": {
              "description": "The command as it runs.",
              "type": "string"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ticket.bug.qualify` command: one complete qualification for one Bug (DR-TK-09), replacing any earlier one.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "qualification": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketBugQualification"
            }
          ],
          "description": "The complete qualification, severity included (DR-LC-13)."
        },
        "ticket_id": {
          "description": "The Bug being qualified.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "qualification",
        "ticket_id"
      ],
      "title": "TicketBugQualifyRequest",
      "type": "object"
    },
    "name": "ticket_bug_qualify",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TaskMode": {
          "description": "The closed Task mode vocabulary on the wire (KAN-S4-US4): whether a human or an agent executes the bounded work.",
          "oneOf": [
            {
              "description": "Sid executes the work.",
              "enum": [
                "human"
              ],
              "type": "string"
            },
            {
              "description": "An agent executes the work under dispatch.",
              "enum": [
                "agent"
              ],
              "type": "string"
            }
          ]
        },
        "TaskSubtype": {
          "description": "The closed Task subtype vocabulary on the wire (DR-TK-06): the seven bounded flavours of non-story work.",
          "oneOf": [
            {
              "description": "Keeping the product and its operations running.",
              "enum": [
                "operational"
              ],
              "type": "string"
            },
            {
              "description": "Finding out what is true before acting on it.",
              "enum": [
                "investigative"
              ],
              "type": "string"
            },
            {
              "description": "Bookkeeping and record-keeping work.",
              "enum": [
                "administrative"
              ],
              "type": "string"
            },
            {
              "description": "Learning without a deliverable committed up front.",
              "enum": [
                "research"
              ],
              "type": "string"
            },
            {
              "description": "Proving an approach before committing to it.",
              "enum": [
                "prototype"
              ],
              "type": "string"
            },
            {
              "description": "Moving work or data from one place to another.",
              "enum": [
                "migration"
              ],
              "type": "string"
            },
            {
              "description": "Work done by hand, on purpose.",
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          ]
        },
        "TicketBugQualification": {
          "additionalProperties": false,
          "description": "One complete Bug qualification (DR-TK-09): the ten facts a Bug needs before it may leave draft, with the severity qualification assigned (DR-LC-13). Sent whole; qualification is one act.",
          "properties": {
            "affected_scope": {
              "description": "What the defect affects.",
              "type": "string"
            },
            "criteria": {
              "description": "The story-linked Acceptance Criteria, at least one.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "environment": {
              "description": "Where the defect occurs.",
              "type": "string"
            },
            "expected_behaviour": {
              "description": "The behaviour that should have happened.",
              "type": "string"
            },
            "frequency": {
              "description": "How often the defect occurs.",
              "type": "string"
            },
            "reproduction": {
              "description": "The reproduction steps or the failing test demonstrating the defect.",
              "type": "string"
            },
            "risk": {
              "description": "What the defect puts at risk.",
              "type": "string"
            },
            "severity": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketSeverity"
                }
              ],
              "description": "The severity qualification assigned (DR-LC-13)."
            },
            "verification_steps": {
              "description": "The Verification Steps, at least one.",
              "items": {
                "$ref": "#/definitions/TicketVerificationStep"
              },
              "type": "array"
            }
          },
          "required": [
            "affected_scope",
            "criteria",
            "environment",
            "expected_behaviour",
            "frequency",
            "reproduction",
            "risk",
            "severity",
            "verification_steps"
          ],
          "type": "object"
        },
        "TicketBugRecord": {
          "additionalProperties": false,
          "description": "The Bug-specific body of a Ticket record: the quick-capture facts, the qualification once one exists, and the vendor-neutral collections the Bug carries (DR-TK-08 to DR-TK-10).",
          "properties": {
            "actual_behaviour": {
              "description": "What the Bug records happening, as the reporter stated it.",
              "type": "string"
            },
            "evidence_ids": {
              "description": "The identities of the Evidence Items this Bug carries (DR-TK-10).",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "external_references": {
              "description": "The External References, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketExternalReference"
              },
              "type": "array"
            },
            "occurrence_snapshots": {
              "description": "The Occurrence Snapshots, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketOccurrenceSnapshot"
              },
              "type": "array"
            },
            "qualification": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TicketBugQualification"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The complete qualification, once one exists (DR-TK-09)."
            },
            "reporter_evidence": {
              "description": "The evidence the reporter holds, as the reporter stated it.",
              "type": "string"
            }
          },
          "required": [
            "actual_behaviour",
            "evidence_ids",
            "external_references",
            "occurrence_snapshots",
            "reporter_evidence"
          ],
          "type": "object"
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketExternalReference": {
          "additionalProperties": false,
          "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
          "properties": {
            "label": {
              "description": "The reference's label, when it carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "The referenced URI.",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "TicketKind": {
          "description": "The closed Ticket kind vocabulary on the wire (DR-TK-01).",
          "oneOf": [
            {
              "description": "A small vertical product slice of exactly one Spec.",
              "enum": [
                "implementation"
              ],
              "type": "string"
            },
            {
              "description": "A record of incorrect behaviour, with qualification evidence.",
              "enum": [
                "bug"
              ],
              "type": "string"
            },
            {
              "description": "Bounded operational, investigative, or administrative work.",
              "enum": [
                "task"
              ],
              "type": "string"
            }
          ]
        },
        "TicketOccurrenceSnapshot": {
          "additionalProperties": false,
          "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
          "properties": {
            "observation": {
              "description": "What was observed at that moment.",
              "type": "string"
            },
            "observed_at": {
              "description": "The observed moment, RFC 3339.",
              "type": "string"
            }
          },
          "required": [
            "observation",
            "observed_at"
          ],
          "type": "object"
        },
        "TicketPriority": {
          "description": "The closed priority vocabulary on the wire (DR-LC-12): urgent, high, normal, low.",
          "oneOf": [
            {
              "description": "Ahead of everything else.",
              "enum": [
                "urgent"
              ],
              "type": "string"
            },
            {
              "description": "Ahead of normal work.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "The everyday order.",
              "enum": [
                "normal"
              ],
              "type": "string"
            },
            {
              "description": "Behind normal work.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketSeverity": {
          "description": "The closed Bug severity vocabulary on the wire (DR-LC-13): critical, high, medium, low. Severity arrives only inside a qualification (DR-TK-09).",
          "oneOf": [
            {
              "description": "Data loss, secret exposure, or unauthorised execution.",
              "enum": [
                "critical"
              ],
              "type": "string"
            },
            {
              "description": "A failed approved criterion or workflow invariant.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "Usable behaviour, wrongly shaped.",
              "enum": [
                "medium"
              ],
              "type": "string"
            },
            {
              "description": "Cosmetic or local clarity.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        },
        "TicketVerificationStep": {
          "additionalProperties": false,
          "description": "One Verification Step a Ticket claims: the command or scripted procedure that demonstrates a criterion (DR-PS-15). Commands live here and never as criteria.",
          "properties": {
            "command": {
              "description": "The command as it runs.",
              "type": "string"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "description": "The Ticket record as every client sees it: the Project it belongs to, the number that Project minted, the kind whose schema it carries, the kind-specific fields — a title for Bugs and Tasks, a slice and criteria for Implementations, the Bug body for a Bug, and for Tasks the subtype, mode, completion criteria, and optional timing — and the Execution Profile the assignment names, if it carries one.",
      "properties": {
        "bug": {
          "anyOf": [
            {
              "$ref": "#/definitions/TicketBugRecord"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Bug body, if this Ticket carries one."
        },
        "completion": {
          "description": "The Task's completion criteria; empty for every other kind.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "criteria": {
          "description": "The Implementation's story-linked criteria; empty for every other kind.",
          "items": {
            "$ref": "#/definitions/TicketCriterion"
          },
          "type": "array"
        },
        "due": {
          "description": "The Task's due date, if it carries one; RFC 3339 in UTC.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketKind"
            }
          ],
          "description": "The kind whose schema this Ticket carries."
        },
        "mode": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's human-or-agent mode, if this Ticket carries one."
        },
        "number": {
          "description": "The number this Project minted for this Ticket; rendered with the Project's code, for example `CORE-T17`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "priority": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketPriority"
            }
          ],
          "description": "The Ticket's priority (DR-LC-12)."
        },
        "profile": {
          "description": "The Execution Profile this Ticket's assignment names, by its catalogue name. The reference keeps its name through every later catalogue change (DR-EP-05).",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Ticket belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "scheduled_for": {
          "description": "The Task's one-time activation instant, if it carries one; RFC 3339 in UTC, stored for KAN-S11.",
          "type": [
            "string",
            "null"
          ]
        },
        "slice": {
          "description": "The Implementation slice description, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "spec_id": {
          "description": "The Spec this Ticket attaches to, if it attaches to one.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketState"
            }
          ],
          "description": "The lifecycle state (DR-LC-01)."
        },
        "subtype": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskSubtype"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's subtype, if this Ticket carries one."
        },
        "title": {
          "description": "The Bug or Task title, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "completion",
        "criteria",
        "id",
        "kind",
        "number",
        "priority",
        "project_id",
        "state",
        "version"
      ],
      "title": "TicketRecord",
      "type": "object"
    }
  },
  {
    "description": "Record the vendor-neutral External References, Occurrence Snapshots, and Evidence Items one Bug carries.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "TicketExternalReference": {
          "additionalProperties": false,
          "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
          "properties": {
            "label": {
              "description": "The reference's label, when it carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "The referenced URI.",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "TicketOccurrenceSnapshot": {
          "additionalProperties": false,
          "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
          "properties": {
            "observation": {
              "description": "What was observed at that moment.",
              "type": "string"
            },
            "observed_at": {
              "description": "The observed moment, RFC 3339.",
              "type": "string"
            }
          },
          "required": [
            "observation",
            "observed_at"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ticket.bug.facts` command: the vendor-neutral collections one Bug carries (DR-TK-10), replaced whole.",
      "properties": {
        "evidence_ids": {
          "description": "The identities of the Evidence Items attached to this Bug.",
          "items": {
            "format": "uint64",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "external_references": {
          "description": "The External References, in recording order.",
          "items": {
            "$ref": "#/definitions/TicketExternalReference"
          },
          "type": "array"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "occurrence_snapshots": {
          "description": "The Occurrence Snapshots, in recording order.",
          "items": {
            "$ref": "#/definitions/TicketOccurrenceSnapshot"
          },
          "type": "array"
        },
        "ticket_id": {
          "description": "The Bug carrying the collections.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "evidence_ids",
        "external_references",
        "mutation",
        "occurrence_snapshots",
        "ticket_id"
      ],
      "title": "TicketBugFactsRequest",
      "type": "object"
    },
    "name": "ticket_bug_facts",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TaskMode": {
          "description": "The closed Task mode vocabulary on the wire (KAN-S4-US4): whether a human or an agent executes the bounded work.",
          "oneOf": [
            {
              "description": "Sid executes the work.",
              "enum": [
                "human"
              ],
              "type": "string"
            },
            {
              "description": "An agent executes the work under dispatch.",
              "enum": [
                "agent"
              ],
              "type": "string"
            }
          ]
        },
        "TaskSubtype": {
          "description": "The closed Task subtype vocabulary on the wire (DR-TK-06): the seven bounded flavours of non-story work.",
          "oneOf": [
            {
              "description": "Keeping the product and its operations running.",
              "enum": [
                "operational"
              ],
              "type": "string"
            },
            {
              "description": "Finding out what is true before acting on it.",
              "enum": [
                "investigative"
              ],
              "type": "string"
            },
            {
              "description": "Bookkeeping and record-keeping work.",
              "enum": [
                "administrative"
              ],
              "type": "string"
            },
            {
              "description": "Learning without a deliverable committed up front.",
              "enum": [
                "research"
              ],
              "type": "string"
            },
            {
              "description": "Proving an approach before committing to it.",
              "enum": [
                "prototype"
              ],
              "type": "string"
            },
            {
              "description": "Moving work or data from one place to another.",
              "enum": [
                "migration"
              ],
              "type": "string"
            },
            {
              "description": "Work done by hand, on purpose.",
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          ]
        },
        "TicketBugQualification": {
          "additionalProperties": false,
          "description": "One complete Bug qualification (DR-TK-09): the ten facts a Bug needs before it may leave draft, with the severity qualification assigned (DR-LC-13). Sent whole; qualification is one act.",
          "properties": {
            "affected_scope": {
              "description": "What the defect affects.",
              "type": "string"
            },
            "criteria": {
              "description": "The story-linked Acceptance Criteria, at least one.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "environment": {
              "description": "Where the defect occurs.",
              "type": "string"
            },
            "expected_behaviour": {
              "description": "The behaviour that should have happened.",
              "type": "string"
            },
            "frequency": {
              "description": "How often the defect occurs.",
              "type": "string"
            },
            "reproduction": {
              "description": "The reproduction steps or the failing test demonstrating the defect.",
              "type": "string"
            },
            "risk": {
              "description": "What the defect puts at risk.",
              "type": "string"
            },
            "severity": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketSeverity"
                }
              ],
              "description": "The severity qualification assigned (DR-LC-13)."
            },
            "verification_steps": {
              "description": "The Verification Steps, at least one.",
              "items": {
                "$ref": "#/definitions/TicketVerificationStep"
              },
              "type": "array"
            }
          },
          "required": [
            "affected_scope",
            "criteria",
            "environment",
            "expected_behaviour",
            "frequency",
            "reproduction",
            "risk",
            "severity",
            "verification_steps"
          ],
          "type": "object"
        },
        "TicketBugRecord": {
          "additionalProperties": false,
          "description": "The Bug-specific body of a Ticket record: the quick-capture facts, the qualification once one exists, and the vendor-neutral collections the Bug carries (DR-TK-08 to DR-TK-10).",
          "properties": {
            "actual_behaviour": {
              "description": "What the Bug records happening, as the reporter stated it.",
              "type": "string"
            },
            "evidence_ids": {
              "description": "The identities of the Evidence Items this Bug carries (DR-TK-10).",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "external_references": {
              "description": "The External References, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketExternalReference"
              },
              "type": "array"
            },
            "occurrence_snapshots": {
              "description": "The Occurrence Snapshots, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketOccurrenceSnapshot"
              },
              "type": "array"
            },
            "qualification": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TicketBugQualification"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The complete qualification, once one exists (DR-TK-09)."
            },
            "reporter_evidence": {
              "description": "The evidence the reporter holds, as the reporter stated it.",
              "type": "string"
            }
          },
          "required": [
            "actual_behaviour",
            "evidence_ids",
            "external_references",
            "occurrence_snapshots",
            "reporter_evidence"
          ],
          "type": "object"
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketExternalReference": {
          "additionalProperties": false,
          "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
          "properties": {
            "label": {
              "description": "The reference's label, when it carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "The referenced URI.",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "TicketKind": {
          "description": "The closed Ticket kind vocabulary on the wire (DR-TK-01).",
          "oneOf": [
            {
              "description": "A small vertical product slice of exactly one Spec.",
              "enum": [
                "implementation"
              ],
              "type": "string"
            },
            {
              "description": "A record of incorrect behaviour, with qualification evidence.",
              "enum": [
                "bug"
              ],
              "type": "string"
            },
            {
              "description": "Bounded operational, investigative, or administrative work.",
              "enum": [
                "task"
              ],
              "type": "string"
            }
          ]
        },
        "TicketOccurrenceSnapshot": {
          "additionalProperties": false,
          "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
          "properties": {
            "observation": {
              "description": "What was observed at that moment.",
              "type": "string"
            },
            "observed_at": {
              "description": "The observed moment, RFC 3339.",
              "type": "string"
            }
          },
          "required": [
            "observation",
            "observed_at"
          ],
          "type": "object"
        },
        "TicketPriority": {
          "description": "The closed priority vocabulary on the wire (DR-LC-12): urgent, high, normal, low.",
          "oneOf": [
            {
              "description": "Ahead of everything else.",
              "enum": [
                "urgent"
              ],
              "type": "string"
            },
            {
              "description": "Ahead of normal work.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "The everyday order.",
              "enum": [
                "normal"
              ],
              "type": "string"
            },
            {
              "description": "Behind normal work.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketSeverity": {
          "description": "The closed Bug severity vocabulary on the wire (DR-LC-13): critical, high, medium, low. Severity arrives only inside a qualification (DR-TK-09).",
          "oneOf": [
            {
              "description": "Data loss, secret exposure, or unauthorised execution.",
              "enum": [
                "critical"
              ],
              "type": "string"
            },
            {
              "description": "A failed approved criterion or workflow invariant.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "Usable behaviour, wrongly shaped.",
              "enum": [
                "medium"
              ],
              "type": "string"
            },
            {
              "description": "Cosmetic or local clarity.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        },
        "TicketVerificationStep": {
          "additionalProperties": false,
          "description": "One Verification Step a Ticket claims: the command or scripted procedure that demonstrates a criterion (DR-PS-15). Commands live here and never as criteria.",
          "properties": {
            "command": {
              "description": "The command as it runs.",
              "type": "string"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "description": "The Ticket record as every client sees it: the Project it belongs to, the number that Project minted, the kind whose schema it carries, the kind-specific fields — a title for Bugs and Tasks, a slice and criteria for Implementations, the Bug body for a Bug, and for Tasks the subtype, mode, completion criteria, and optional timing — and the Execution Profile the assignment names, if it carries one.",
      "properties": {
        "bug": {
          "anyOf": [
            {
              "$ref": "#/definitions/TicketBugRecord"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Bug body, if this Ticket carries one."
        },
        "completion": {
          "description": "The Task's completion criteria; empty for every other kind.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "criteria": {
          "description": "The Implementation's story-linked criteria; empty for every other kind.",
          "items": {
            "$ref": "#/definitions/TicketCriterion"
          },
          "type": "array"
        },
        "due": {
          "description": "The Task's due date, if it carries one; RFC 3339 in UTC.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketKind"
            }
          ],
          "description": "The kind whose schema this Ticket carries."
        },
        "mode": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's human-or-agent mode, if this Ticket carries one."
        },
        "number": {
          "description": "The number this Project minted for this Ticket; rendered with the Project's code, for example `CORE-T17`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "priority": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketPriority"
            }
          ],
          "description": "The Ticket's priority (DR-LC-12)."
        },
        "profile": {
          "description": "The Execution Profile this Ticket's assignment names, by its catalogue name. The reference keeps its name through every later catalogue change (DR-EP-05).",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Ticket belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "scheduled_for": {
          "description": "The Task's one-time activation instant, if it carries one; RFC 3339 in UTC, stored for KAN-S11.",
          "type": [
            "string",
            "null"
          ]
        },
        "slice": {
          "description": "The Implementation slice description, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "spec_id": {
          "description": "The Spec this Ticket attaches to, if it attaches to one.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketState"
            }
          ],
          "description": "The lifecycle state (DR-LC-01)."
        },
        "subtype": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskSubtype"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's subtype, if this Ticket carries one."
        },
        "title": {
          "description": "The Bug or Task title, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "completion",
        "criteria",
        "id",
        "kind",
        "number",
        "priority",
        "project_id",
        "state",
        "version"
      ],
      "title": "TicketRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Ticket of a Project, terminal lifecycle states included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `ticket.list` query.",
      "properties": {
        "project_id": {
          "description": "The Project whose Tickets are listed, terminal states included.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "TicketListQuery",
      "type": "object"
    },
    "name": "ticket_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TaskMode": {
          "description": "The closed Task mode vocabulary on the wire (KAN-S4-US4): whether a human or an agent executes the bounded work.",
          "oneOf": [
            {
              "description": "Sid executes the work.",
              "enum": [
                "human"
              ],
              "type": "string"
            },
            {
              "description": "An agent executes the work under dispatch.",
              "enum": [
                "agent"
              ],
              "type": "string"
            }
          ]
        },
        "TaskSubtype": {
          "description": "The closed Task subtype vocabulary on the wire (DR-TK-06): the seven bounded flavours of non-story work.",
          "oneOf": [
            {
              "description": "Keeping the product and its operations running.",
              "enum": [
                "operational"
              ],
              "type": "string"
            },
            {
              "description": "Finding out what is true before acting on it.",
              "enum": [
                "investigative"
              ],
              "type": "string"
            },
            {
              "description": "Bookkeeping and record-keeping work.",
              "enum": [
                "administrative"
              ],
              "type": "string"
            },
            {
              "description": "Learning without a deliverable committed up front.",
              "enum": [
                "research"
              ],
              "type": "string"
            },
            {
              "description": "Proving an approach before committing to it.",
              "enum": [
                "prototype"
              ],
              "type": "string"
            },
            {
              "description": "Moving work or data from one place to another.",
              "enum": [
                "migration"
              ],
              "type": "string"
            },
            {
              "description": "Work done by hand, on purpose.",
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          ]
        },
        "TicketBugQualification": {
          "additionalProperties": false,
          "description": "One complete Bug qualification (DR-TK-09): the ten facts a Bug needs before it may leave draft, with the severity qualification assigned (DR-LC-13). Sent whole; qualification is one act.",
          "properties": {
            "affected_scope": {
              "description": "What the defect affects.",
              "type": "string"
            },
            "criteria": {
              "description": "The story-linked Acceptance Criteria, at least one.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "environment": {
              "description": "Where the defect occurs.",
              "type": "string"
            },
            "expected_behaviour": {
              "description": "The behaviour that should have happened.",
              "type": "string"
            },
            "frequency": {
              "description": "How often the defect occurs.",
              "type": "string"
            },
            "reproduction": {
              "description": "The reproduction steps or the failing test demonstrating the defect.",
              "type": "string"
            },
            "risk": {
              "description": "What the defect puts at risk.",
              "type": "string"
            },
            "severity": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketSeverity"
                }
              ],
              "description": "The severity qualification assigned (DR-LC-13)."
            },
            "verification_steps": {
              "description": "The Verification Steps, at least one.",
              "items": {
                "$ref": "#/definitions/TicketVerificationStep"
              },
              "type": "array"
            }
          },
          "required": [
            "affected_scope",
            "criteria",
            "environment",
            "expected_behaviour",
            "frequency",
            "reproduction",
            "risk",
            "severity",
            "verification_steps"
          ],
          "type": "object"
        },
        "TicketBugRecord": {
          "additionalProperties": false,
          "description": "The Bug-specific body of a Ticket record: the quick-capture facts, the qualification once one exists, and the vendor-neutral collections the Bug carries (DR-TK-08 to DR-TK-10).",
          "properties": {
            "actual_behaviour": {
              "description": "What the Bug records happening, as the reporter stated it.",
              "type": "string"
            },
            "evidence_ids": {
              "description": "The identities of the Evidence Items this Bug carries (DR-TK-10).",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "external_references": {
              "description": "The External References, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketExternalReference"
              },
              "type": "array"
            },
            "occurrence_snapshots": {
              "description": "The Occurrence Snapshots, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketOccurrenceSnapshot"
              },
              "type": "array"
            },
            "qualification": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TicketBugQualification"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The complete qualification, once one exists (DR-TK-09)."
            },
            "reporter_evidence": {
              "description": "The evidence the reporter holds, as the reporter stated it.",
              "type": "string"
            }
          },
          "required": [
            "actual_behaviour",
            "evidence_ids",
            "external_references",
            "occurrence_snapshots",
            "reporter_evidence"
          ],
          "type": "object"
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketExternalReference": {
          "additionalProperties": false,
          "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
          "properties": {
            "label": {
              "description": "The reference's label, when it carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "The referenced URI.",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "TicketKind": {
          "description": "The closed Ticket kind vocabulary on the wire (DR-TK-01).",
          "oneOf": [
            {
              "description": "A small vertical product slice of exactly one Spec.",
              "enum": [
                "implementation"
              ],
              "type": "string"
            },
            {
              "description": "A record of incorrect behaviour, with qualification evidence.",
              "enum": [
                "bug"
              ],
              "type": "string"
            },
            {
              "description": "Bounded operational, investigative, or administrative work.",
              "enum": [
                "task"
              ],
              "type": "string"
            }
          ]
        },
        "TicketOccurrenceSnapshot": {
          "additionalProperties": false,
          "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
          "properties": {
            "observation": {
              "description": "What was observed at that moment.",
              "type": "string"
            },
            "observed_at": {
              "description": "The observed moment, RFC 3339.",
              "type": "string"
            }
          },
          "required": [
            "observation",
            "observed_at"
          ],
          "type": "object"
        },
        "TicketPriority": {
          "description": "The closed priority vocabulary on the wire (DR-LC-12): urgent, high, normal, low.",
          "oneOf": [
            {
              "description": "Ahead of everything else.",
              "enum": [
                "urgent"
              ],
              "type": "string"
            },
            {
              "description": "Ahead of normal work.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "The everyday order.",
              "enum": [
                "normal"
              ],
              "type": "string"
            },
            {
              "description": "Behind normal work.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketRecord": {
          "additionalProperties": false,
          "description": "The Ticket record as every client sees it: the Project it belongs to, the number that Project minted, the kind whose schema it carries, the kind-specific fields — a title for Bugs and Tasks, a slice and criteria for Implementations, the Bug body for a Bug, and for Tasks the subtype, mode, completion criteria, and optional timing — and the Execution Profile the assignment names, if it carries one.",
          "properties": {
            "bug": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TicketBugRecord"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The Bug body, if this Ticket carries one."
            },
            "completion": {
              "description": "The Task's completion criteria; empty for every other kind.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "criteria": {
              "description": "The Implementation's story-linked criteria; empty for every other kind.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "due": {
              "description": "The Task's due date, if it carries one; RFC 3339 in UTC.",
              "type": [
                "string",
                "null"
              ]
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "kind": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketKind"
                }
              ],
              "description": "The kind whose schema this Ticket carries."
            },
            "mode": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TaskMode"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The Task's human-or-agent mode, if this Ticket carries one."
            },
            "number": {
              "description": "The number this Project minted for this Ticket; rendered with the Project's code, for example `CORE-T17`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "priority": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketPriority"
                }
              ],
              "description": "The Ticket's priority (DR-LC-12)."
            },
            "profile": {
              "description": "The Execution Profile this Ticket's assignment names, by its catalogue name. The reference keeps its name through every later catalogue change (DR-EP-05).",
              "type": [
                "string",
                "null"
              ]
            },
            "project_id": {
              "description": "The Project this Ticket belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "scheduled_for": {
              "description": "The Task's one-time activation instant, if it carries one; RFC 3339 in UTC, stored for KAN-S11.",
              "type": [
                "string",
                "null"
              ]
            },
            "slice": {
              "description": "The Implementation slice description, if this Ticket carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "spec_id": {
              "description": "The Spec this Ticket attaches to, if it attaches to one.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "state": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketState"
                }
              ],
              "description": "The lifecycle state (DR-LC-01)."
            },
            "subtype": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TaskSubtype"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The Task's subtype, if this Ticket carries one."
            },
            "title": {
              "description": "The Bug or Task title, if this Ticket carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "completion",
            "criteria",
            "id",
            "kind",
            "number",
            "priority",
            "project_id",
            "state",
            "version"
          ],
          "type": "object"
        },
        "TicketSeverity": {
          "description": "The closed Bug severity vocabulary on the wire (DR-LC-13): critical, high, medium, low. Severity arrives only inside a qualification (DR-TK-09).",
          "oneOf": [
            {
              "description": "Data loss, secret exposure, or unauthorised execution.",
              "enum": [
                "critical"
              ],
              "type": "string"
            },
            {
              "description": "A failed approved criterion or workflow invariant.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "Usable behaviour, wrongly shaped.",
              "enum": [
                "medium"
              ],
              "type": "string"
            },
            {
              "description": "Cosmetic or local clarity.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        },
        "TicketVerificationStep": {
          "additionalProperties": false,
          "description": "One Verification Step a Ticket claims: the command or scripted procedure that demonstrates a criterion (DR-PS-15). Commands live here and never as criteria.",
          "properties": {
            "command": {
              "description": "The command as it runs.",
              "type": "string"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `ticket.list` query.",
      "properties": {
        "tickets": {
          "description": "Every Ticket of the Project, oldest first, all lifecycle states included.",
          "items": {
            "$ref": "#/definitions/TicketRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "tickets"
      ],
      "title": "TicketListResponse",
      "type": "object"
    }
  },
  {
    "description": "Read one Ticket with the record of its kind's schema.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `ticket.get` query.",
      "properties": {
        "ticket_id": {
          "description": "The Ticket being read.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "ticket_id"
      ],
      "title": "TicketGetQuery",
      "type": "object"
    },
    "name": "ticket_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TaskMode": {
          "description": "The closed Task mode vocabulary on the wire (KAN-S4-US4): whether a human or an agent executes the bounded work.",
          "oneOf": [
            {
              "description": "Sid executes the work.",
              "enum": [
                "human"
              ],
              "type": "string"
            },
            {
              "description": "An agent executes the work under dispatch.",
              "enum": [
                "agent"
              ],
              "type": "string"
            }
          ]
        },
        "TaskSubtype": {
          "description": "The closed Task subtype vocabulary on the wire (DR-TK-06): the seven bounded flavours of non-story work.",
          "oneOf": [
            {
              "description": "Keeping the product and its operations running.",
              "enum": [
                "operational"
              ],
              "type": "string"
            },
            {
              "description": "Finding out what is true before acting on it.",
              "enum": [
                "investigative"
              ],
              "type": "string"
            },
            {
              "description": "Bookkeeping and record-keeping work.",
              "enum": [
                "administrative"
              ],
              "type": "string"
            },
            {
              "description": "Learning without a deliverable committed up front.",
              "enum": [
                "research"
              ],
              "type": "string"
            },
            {
              "description": "Proving an approach before committing to it.",
              "enum": [
                "prototype"
              ],
              "type": "string"
            },
            {
              "description": "Moving work or data from one place to another.",
              "enum": [
                "migration"
              ],
              "type": "string"
            },
            {
              "description": "Work done by hand, on purpose.",
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          ]
        },
        "TicketBugQualification": {
          "additionalProperties": false,
          "description": "One complete Bug qualification (DR-TK-09): the ten facts a Bug needs before it may leave draft, with the severity qualification assigned (DR-LC-13). Sent whole; qualification is one act.",
          "properties": {
            "affected_scope": {
              "description": "What the defect affects.",
              "type": "string"
            },
            "criteria": {
              "description": "The story-linked Acceptance Criteria, at least one.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "environment": {
              "description": "Where the defect occurs.",
              "type": "string"
            },
            "expected_behaviour": {
              "description": "The behaviour that should have happened.",
              "type": "string"
            },
            "frequency": {
              "description": "How often the defect occurs.",
              "type": "string"
            },
            "reproduction": {
              "description": "The reproduction steps or the failing test demonstrating the defect.",
              "type": "string"
            },
            "risk": {
              "description": "What the defect puts at risk.",
              "type": "string"
            },
            "severity": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketSeverity"
                }
              ],
              "description": "The severity qualification assigned (DR-LC-13)."
            },
            "verification_steps": {
              "description": "The Verification Steps, at least one.",
              "items": {
                "$ref": "#/definitions/TicketVerificationStep"
              },
              "type": "array"
            }
          },
          "required": [
            "affected_scope",
            "criteria",
            "environment",
            "expected_behaviour",
            "frequency",
            "reproduction",
            "risk",
            "severity",
            "verification_steps"
          ],
          "type": "object"
        },
        "TicketBugRecord": {
          "additionalProperties": false,
          "description": "The Bug-specific body of a Ticket record: the quick-capture facts, the qualification once one exists, and the vendor-neutral collections the Bug carries (DR-TK-08 to DR-TK-10).",
          "properties": {
            "actual_behaviour": {
              "description": "What the Bug records happening, as the reporter stated it.",
              "type": "string"
            },
            "evidence_ids": {
              "description": "The identities of the Evidence Items this Bug carries (DR-TK-10).",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "external_references": {
              "description": "The External References, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketExternalReference"
              },
              "type": "array"
            },
            "occurrence_snapshots": {
              "description": "The Occurrence Snapshots, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketOccurrenceSnapshot"
              },
              "type": "array"
            },
            "qualification": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TicketBugQualification"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The complete qualification, once one exists (DR-TK-09)."
            },
            "reporter_evidence": {
              "description": "The evidence the reporter holds, as the reporter stated it.",
              "type": "string"
            }
          },
          "required": [
            "actual_behaviour",
            "evidence_ids",
            "external_references",
            "occurrence_snapshots",
            "reporter_evidence"
          ],
          "type": "object"
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketExternalReference": {
          "additionalProperties": false,
          "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
          "properties": {
            "label": {
              "description": "The reference's label, when it carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "The referenced URI.",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "TicketKind": {
          "description": "The closed Ticket kind vocabulary on the wire (DR-TK-01).",
          "oneOf": [
            {
              "description": "A small vertical product slice of exactly one Spec.",
              "enum": [
                "implementation"
              ],
              "type": "string"
            },
            {
              "description": "A record of incorrect behaviour, with qualification evidence.",
              "enum": [
                "bug"
              ],
              "type": "string"
            },
            {
              "description": "Bounded operational, investigative, or administrative work.",
              "enum": [
                "task"
              ],
              "type": "string"
            }
          ]
        },
        "TicketOccurrenceSnapshot": {
          "additionalProperties": false,
          "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
          "properties": {
            "observation": {
              "description": "What was observed at that moment.",
              "type": "string"
            },
            "observed_at": {
              "description": "The observed moment, RFC 3339.",
              "type": "string"
            }
          },
          "required": [
            "observation",
            "observed_at"
          ],
          "type": "object"
        },
        "TicketPriority": {
          "description": "The closed priority vocabulary on the wire (DR-LC-12): urgent, high, normal, low.",
          "oneOf": [
            {
              "description": "Ahead of everything else.",
              "enum": [
                "urgent"
              ],
              "type": "string"
            },
            {
              "description": "Ahead of normal work.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "The everyday order.",
              "enum": [
                "normal"
              ],
              "type": "string"
            },
            {
              "description": "Behind normal work.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketSeverity": {
          "description": "The closed Bug severity vocabulary on the wire (DR-LC-13): critical, high, medium, low. Severity arrives only inside a qualification (DR-TK-09).",
          "oneOf": [
            {
              "description": "Data loss, secret exposure, or unauthorised execution.",
              "enum": [
                "critical"
              ],
              "type": "string"
            },
            {
              "description": "A failed approved criterion or workflow invariant.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "Usable behaviour, wrongly shaped.",
              "enum": [
                "medium"
              ],
              "type": "string"
            },
            {
              "description": "Cosmetic or local clarity.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        },
        "TicketVerificationStep": {
          "additionalProperties": false,
          "description": "One Verification Step a Ticket claims: the command or scripted procedure that demonstrates a criterion (DR-PS-15). Commands live here and never as criteria.",
          "properties": {
            "command": {
              "description": "The command as it runs.",
              "type": "string"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "description": "The Ticket record as every client sees it: the Project it belongs to, the number that Project minted, the kind whose schema it carries, the kind-specific fields — a title for Bugs and Tasks, a slice and criteria for Implementations, the Bug body for a Bug, and for Tasks the subtype, mode, completion criteria, and optional timing — and the Execution Profile the assignment names, if it carries one.",
      "properties": {
        "bug": {
          "anyOf": [
            {
              "$ref": "#/definitions/TicketBugRecord"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Bug body, if this Ticket carries one."
        },
        "completion": {
          "description": "The Task's completion criteria; empty for every other kind.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "criteria": {
          "description": "The Implementation's story-linked criteria; empty for every other kind.",
          "items": {
            "$ref": "#/definitions/TicketCriterion"
          },
          "type": "array"
        },
        "due": {
          "description": "The Task's due date, if it carries one; RFC 3339 in UTC.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketKind"
            }
          ],
          "description": "The kind whose schema this Ticket carries."
        },
        "mode": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's human-or-agent mode, if this Ticket carries one."
        },
        "number": {
          "description": "The number this Project minted for this Ticket; rendered with the Project's code, for example `CORE-T17`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "priority": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketPriority"
            }
          ],
          "description": "The Ticket's priority (DR-LC-12)."
        },
        "profile": {
          "description": "The Execution Profile this Ticket's assignment names, by its catalogue name. The reference keeps its name through every later catalogue change (DR-EP-05).",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Ticket belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "scheduled_for": {
          "description": "The Task's one-time activation instant, if it carries one; RFC 3339 in UTC, stored for KAN-S11.",
          "type": [
            "string",
            "null"
          ]
        },
        "slice": {
          "description": "The Implementation slice description, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "spec_id": {
          "description": "The Spec this Ticket attaches to, if it attaches to one.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketState"
            }
          ],
          "description": "The lifecycle state (DR-LC-01)."
        },
        "subtype": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskSubtype"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's subtype, if this Ticket carries one."
        },
        "title": {
          "description": "The Bug or Task title, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "completion",
        "criteria",
        "id",
        "kind",
        "number",
        "priority",
        "project_id",
        "state",
        "version"
      ],
      "title": "TicketRecord",
      "type": "object"
    }
  },
  {
    "description": "Register a Ticket dependency. Dependencies may cross Specs and registered Projects; cycles are refused.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ticket.dependency.add` command: the blocking Ticket must land before the waiting Ticket may begin (DR-DE-02). Both endpoints must name registered Tickets; work no Ticket carries is recorded with `ticket.blocker.add` instead (DR-DE-04).",
      "properties": {
        "from_ticket": {
          "description": "The Ticket that must land first.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "to_ticket": {
          "description": "The Ticket that waits on `from_ticket`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "from_ticket",
        "mutation",
        "to_ticket"
      ],
      "title": "TicketDependencyAddRequest",
      "type": "object"
    },
    "name": "ticket_dependency_add",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TicketBlockerRecord": {
          "additionalProperties": false,
          "description": "One recorded external blocker as every client sees it (DR-DE-04).",
          "properties": {
            "description": {
              "description": "The unregistered work, in prose.",
              "type": "string"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket_id": {
              "description": "The Ticket waiting on the described work.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "description",
            "id",
            "ticket_id"
          ],
          "type": "object"
        },
        "TicketDependencyRecord": {
          "additionalProperties": false,
          "description": "One registered dependency as every client sees it: the blocking Ticket, the Project it belongs to, the number that Project minted for it, and its lifecycle state.",
          "properties": {
            "from_number": {
              "description": "The number the blocking Ticket's Project minted for it; rendered with the Project's code, for example `CORE-T17`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_project_id": {
              "description": "The Project the blocking Ticket belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_state": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketState"
                }
              ],
              "description": "The blocking Ticket's lifecycle state."
            },
            "from_ticket_id": {
              "description": "The blocking Ticket: it must land first (DR-DE-02).",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_number",
            "from_project_id",
            "from_state",
            "from_ticket_id"
          ],
          "type": "object"
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Response payload for the `ticket.dependencies` query and for every dependency and blocker command: the registered dependencies one Ticket waits on, its explicit external blockers, and the Ticket's aggregate version after the change.",
      "properties": {
        "blockers": {
          "description": "The Ticket's explicit external blockers, in recording order.",
          "items": {
            "$ref": "#/definitions/TicketBlockerRecord"
          },
          "type": "array"
        },
        "dependencies": {
          "description": "The registered dependencies the Ticket waits on, in registration order.",
          "items": {
            "$ref": "#/definitions/TicketDependencyRecord"
          },
          "type": "array"
        },
        "ticket_id": {
          "description": "The Ticket the dependencies belong to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The Ticket's aggregate version, for optimistic checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "blockers",
        "dependencies",
        "ticket_id",
        "version"
      ],
      "title": "TicketDependenciesResponse",
      "type": "object"
    }
  },
  {
    "description": "Remove one registered Ticket dependency.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ticket.dependency.remove` command.",
      "properties": {
        "from_ticket": {
          "description": "The Ticket that must land first.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "to_ticket": {
          "description": "The Ticket that waits on `from_ticket`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "from_ticket",
        "mutation",
        "to_ticket"
      ],
      "title": "TicketDependencyRemoveRequest",
      "type": "object"
    },
    "name": "ticket_dependency_remove",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TicketBlockerRecord": {
          "additionalProperties": false,
          "description": "One recorded external blocker as every client sees it (DR-DE-04).",
          "properties": {
            "description": {
              "description": "The unregistered work, in prose.",
              "type": "string"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket_id": {
              "description": "The Ticket waiting on the described work.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "description",
            "id",
            "ticket_id"
          ],
          "type": "object"
        },
        "TicketDependencyRecord": {
          "additionalProperties": false,
          "description": "One registered dependency as every client sees it: the blocking Ticket, the Project it belongs to, the number that Project minted for it, and its lifecycle state.",
          "properties": {
            "from_number": {
              "description": "The number the blocking Ticket's Project minted for it; rendered with the Project's code, for example `CORE-T17`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_project_id": {
              "description": "The Project the blocking Ticket belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_state": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketState"
                }
              ],
              "description": "The blocking Ticket's lifecycle state."
            },
            "from_ticket_id": {
              "description": "The blocking Ticket: it must land first (DR-DE-02).",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_number",
            "from_project_id",
            "from_state",
            "from_ticket_id"
          ],
          "type": "object"
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Response payload for the `ticket.dependencies` query and for every dependency and blocker command: the registered dependencies one Ticket waits on, its explicit external blockers, and the Ticket's aggregate version after the change.",
      "properties": {
        "blockers": {
          "description": "The Ticket's explicit external blockers, in recording order.",
          "items": {
            "$ref": "#/definitions/TicketBlockerRecord"
          },
          "type": "array"
        },
        "dependencies": {
          "description": "The registered dependencies the Ticket waits on, in registration order.",
          "items": {
            "$ref": "#/definitions/TicketDependencyRecord"
          },
          "type": "array"
        },
        "ticket_id": {
          "description": "The Ticket the dependencies belong to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The Ticket's aggregate version, for optimistic checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "blockers",
        "dependencies",
        "ticket_id",
        "version"
      ],
      "title": "TicketDependenciesResponse",
      "type": "object"
    }
  },
  {
    "description": "Record one explicit external blocker naming the unregistered work a Ticket waits on.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ticket.blocker.add` command: one explicit external blocker, naming the unregistered work a Ticket waits on in prose (DR-DE-04).",
      "properties": {
        "description": {
          "description": "The unregistered work, in prose.",
          "type": "string"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "ticket_id": {
          "description": "The Ticket waiting on the described work.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "description",
        "mutation",
        "ticket_id"
      ],
      "title": "TicketBlockerAddRequest",
      "type": "object"
    },
    "name": "ticket_blocker_add",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TicketBlockerRecord": {
          "additionalProperties": false,
          "description": "One recorded external blocker as every client sees it (DR-DE-04).",
          "properties": {
            "description": {
              "description": "The unregistered work, in prose.",
              "type": "string"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket_id": {
              "description": "The Ticket waiting on the described work.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "description",
            "id",
            "ticket_id"
          ],
          "type": "object"
        },
        "TicketDependencyRecord": {
          "additionalProperties": false,
          "description": "One registered dependency as every client sees it: the blocking Ticket, the Project it belongs to, the number that Project minted for it, and its lifecycle state.",
          "properties": {
            "from_number": {
              "description": "The number the blocking Ticket's Project minted for it; rendered with the Project's code, for example `CORE-T17`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_project_id": {
              "description": "The Project the blocking Ticket belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_state": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketState"
                }
              ],
              "description": "The blocking Ticket's lifecycle state."
            },
            "from_ticket_id": {
              "description": "The blocking Ticket: it must land first (DR-DE-02).",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_number",
            "from_project_id",
            "from_state",
            "from_ticket_id"
          ],
          "type": "object"
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Response payload for the `ticket.dependencies` query and for every dependency and blocker command: the registered dependencies one Ticket waits on, its explicit external blockers, and the Ticket's aggregate version after the change.",
      "properties": {
        "blockers": {
          "description": "The Ticket's explicit external blockers, in recording order.",
          "items": {
            "$ref": "#/definitions/TicketBlockerRecord"
          },
          "type": "array"
        },
        "dependencies": {
          "description": "The registered dependencies the Ticket waits on, in registration order.",
          "items": {
            "$ref": "#/definitions/TicketDependencyRecord"
          },
          "type": "array"
        },
        "ticket_id": {
          "description": "The Ticket the dependencies belong to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The Ticket's aggregate version, for optimistic checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "blockers",
        "dependencies",
        "ticket_id",
        "version"
      ],
      "title": "TicketDependenciesResponse",
      "type": "object"
    }
  },
  {
    "description": "Remove one explicit external blocker; removal is the operator action that clears it.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ticket.blocker.remove` command: removing an external blocker is the explicit operator action that clears it (DR-DE-04).",
      "properties": {
        "blocker_id": {
          "description": "The recorded blocker being removed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "ticket_id": {
          "description": "The Ticket the blocker was recorded against.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "blocker_id",
        "mutation",
        "ticket_id"
      ],
      "title": "TicketBlockerRemoveRequest",
      "type": "object"
    },
    "name": "ticket_blocker_remove",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TicketBlockerRecord": {
          "additionalProperties": false,
          "description": "One recorded external blocker as every client sees it (DR-DE-04).",
          "properties": {
            "description": {
              "description": "The unregistered work, in prose.",
              "type": "string"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket_id": {
              "description": "The Ticket waiting on the described work.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "description",
            "id",
            "ticket_id"
          ],
          "type": "object"
        },
        "TicketDependencyRecord": {
          "additionalProperties": false,
          "description": "One registered dependency as every client sees it: the blocking Ticket, the Project it belongs to, the number that Project minted for it, and its lifecycle state.",
          "properties": {
            "from_number": {
              "description": "The number the blocking Ticket's Project minted for it; rendered with the Project's code, for example `CORE-T17`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_project_id": {
              "description": "The Project the blocking Ticket belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_state": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketState"
                }
              ],
              "description": "The blocking Ticket's lifecycle state."
            },
            "from_ticket_id": {
              "description": "The blocking Ticket: it must land first (DR-DE-02).",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_number",
            "from_project_id",
            "from_state",
            "from_ticket_id"
          ],
          "type": "object"
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Response payload for the `ticket.dependencies` query and for every dependency and blocker command: the registered dependencies one Ticket waits on, its explicit external blockers, and the Ticket's aggregate version after the change.",
      "properties": {
        "blockers": {
          "description": "The Ticket's explicit external blockers, in recording order.",
          "items": {
            "$ref": "#/definitions/TicketBlockerRecord"
          },
          "type": "array"
        },
        "dependencies": {
          "description": "The registered dependencies the Ticket waits on, in registration order.",
          "items": {
            "$ref": "#/definitions/TicketDependencyRecord"
          },
          "type": "array"
        },
        "ticket_id": {
          "description": "The Ticket the dependencies belong to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The Ticket's aggregate version, for optimistic checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "blockers",
        "dependencies",
        "ticket_id",
        "version"
      ],
      "title": "TicketDependenciesResponse",
      "type": "object"
    }
  },
  {
    "description": "Read one Ticket's registered dependencies and external blockers.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `ticket.dependencies` query.",
      "properties": {
        "ticket_id": {
          "description": "The Ticket whose dependencies and blockers are read.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "ticket_id"
      ],
      "title": "TicketDependenciesQuery",
      "type": "object"
    },
    "name": "ticket_dependencies",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TicketBlockerRecord": {
          "additionalProperties": false,
          "description": "One recorded external blocker as every client sees it (DR-DE-04).",
          "properties": {
            "description": {
              "description": "The unregistered work, in prose.",
              "type": "string"
            },
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket_id": {
              "description": "The Ticket waiting on the described work.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "description",
            "id",
            "ticket_id"
          ],
          "type": "object"
        },
        "TicketDependencyRecord": {
          "additionalProperties": false,
          "description": "One registered dependency as every client sees it: the blocking Ticket, the Project it belongs to, the number that Project minted for it, and its lifecycle state.",
          "properties": {
            "from_number": {
              "description": "The number the blocking Ticket's Project minted for it; rendered with the Project's code, for example `CORE-T17`.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_project_id": {
              "description": "The Project the blocking Ticket belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "from_state": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketState"
                }
              ],
              "description": "The blocking Ticket's lifecycle state."
            },
            "from_ticket_id": {
              "description": "The blocking Ticket: it must land first (DR-DE-02).",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "from_number",
            "from_project_id",
            "from_state",
            "from_ticket_id"
          ],
          "type": "object"
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Response payload for the `ticket.dependencies` query and for every dependency and blocker command: the registered dependencies one Ticket waits on, its explicit external blockers, and the Ticket's aggregate version after the change.",
      "properties": {
        "blockers": {
          "description": "The Ticket's explicit external blockers, in recording order.",
          "items": {
            "$ref": "#/definitions/TicketBlockerRecord"
          },
          "type": "array"
        },
        "dependencies": {
          "description": "The registered dependencies the Ticket waits on, in registration order.",
          "items": {
            "$ref": "#/definitions/TicketDependencyRecord"
          },
          "type": "array"
        },
        "ticket_id": {
          "description": "The Ticket the dependencies belong to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The Ticket's aggregate version, for optimistic checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "blockers",
        "dependencies",
        "ticket_id",
        "version"
      ],
      "title": "TicketDependenciesResponse",
      "type": "object"
    }
  },
  {
    "description": "Compute one Ticket's readiness from its dependencies and external blockers. The projection never mutates state.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `ticket.readiness` query.",
      "properties": {
        "ticket_id": {
          "description": "The Ticket whose readiness is computed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "ticket_id"
      ],
      "title": "TicketReadinessQuery",
      "type": "object"
    },
    "name": "ticket_readiness",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TicketReadinessBlocker": {
          "description": "What still holds one Ticket back.",
          "oneOf": [
            {
              "additionalProperties": false,
              "description": "A registered dependency whose blocker has not landed.",
              "properties": {
                "Ticket": {
                  "properties": {
                    "from_number": {
                      "description": "The number the blocking Ticket's Project minted for it.",
                      "format": "uint64",
                      "minimum": 0.0,
                      "type": "integer"
                    },
                    "from_project_id": {
                      "description": "The Project the blocking Ticket belongs to.",
                      "format": "uint64",
                      "minimum": 0.0,
                      "type": "integer"
                    },
                    "from_state": {
                      "allOf": [
                        {
                          "$ref": "#/definitions/TicketState"
                        }
                      ],
                      "description": "The blocking Ticket's lifecycle state."
                    },
                    "from_ticket_id": {
                      "description": "The blocking Ticket: it must land first (DR-DE-02).",
                      "format": "uint64",
                      "minimum": 0.0,
                      "type": "integer"
                    }
                  },
                  "required": [
                    "from_number",
                    "from_project_id",
                    "from_state",
                    "from_ticket_id"
                  ],
                  "type": "object"
                }
              },
              "required": [
                "Ticket"
              ],
              "type": "object"
            },
            {
              "additionalProperties": false,
              "description": "An explicit external blocker (DR-DE-04).",
              "properties": {
                "External": {
                  "properties": {
                    "blocker_id": {
                      "description": "The recorded blocker.",
                      "format": "uint64",
                      "minimum": 0.0,
                      "type": "integer"
                    },
                    "description": {
                      "description": "The unregistered work, in prose.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "blocker_id",
                    "description"
                  ],
                  "type": "object"
                }
              },
              "required": [
                "External"
              ],
              "type": "object"
            }
          ]
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Response payload for the `ticket.readiness` query: the computed readiness projection of one Ticket's dependencies and external blockers (DR-DE-03). The projection never mutates state; dispatch consumes it.",
      "properties": {
        "blocked_by": {
          "description": "What holds the Ticket back, dependencies first, then external blockers.",
          "items": {
            "$ref": "#/definitions/TicketReadinessBlocker"
          },
          "type": "array"
        },
        "ready": {
          "description": "Whether nothing holds the Ticket back.",
          "type": "boolean"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketState"
            }
          ],
          "description": "The Ticket's own lifecycle state, for context."
        },
        "ticket_id": {
          "description": "The Ticket the readiness was computed for.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "blocked_by",
        "ready",
        "state",
        "ticket_id"
      ],
      "title": "TicketReadinessResponse",
      "type": "object"
    }
  },
  {
    "description": "Assign a Ticket to an Execution Profile by name; an unknown name is rejected.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ticket.assign` command: the Ticket's assignment names one catalogue entry by reference, and an unknown name is rejected (DR-EP-03).",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "profile": {
          "description": "The Execution Profile the assignment names, by its catalogue name.",
          "type": "string"
        },
        "ticket_id": {
          "description": "The Ticket being assigned.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "profile",
        "ticket_id"
      ],
      "title": "TicketAssignRequest",
      "type": "object"
    },
    "name": "ticket_assign",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TaskMode": {
          "description": "The closed Task mode vocabulary on the wire (KAN-S4-US4): whether a human or an agent executes the bounded work.",
          "oneOf": [
            {
              "description": "Sid executes the work.",
              "enum": [
                "human"
              ],
              "type": "string"
            },
            {
              "description": "An agent executes the work under dispatch.",
              "enum": [
                "agent"
              ],
              "type": "string"
            }
          ]
        },
        "TaskSubtype": {
          "description": "The closed Task subtype vocabulary on the wire (DR-TK-06): the seven bounded flavours of non-story work.",
          "oneOf": [
            {
              "description": "Keeping the product and its operations running.",
              "enum": [
                "operational"
              ],
              "type": "string"
            },
            {
              "description": "Finding out what is true before acting on it.",
              "enum": [
                "investigative"
              ],
              "type": "string"
            },
            {
              "description": "Bookkeeping and record-keeping work.",
              "enum": [
                "administrative"
              ],
              "type": "string"
            },
            {
              "description": "Learning without a deliverable committed up front.",
              "enum": [
                "research"
              ],
              "type": "string"
            },
            {
              "description": "Proving an approach before committing to it.",
              "enum": [
                "prototype"
              ],
              "type": "string"
            },
            {
              "description": "Moving work or data from one place to another.",
              "enum": [
                "migration"
              ],
              "type": "string"
            },
            {
              "description": "Work done by hand, on purpose.",
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          ]
        },
        "TicketBugQualification": {
          "additionalProperties": false,
          "description": "One complete Bug qualification (DR-TK-09): the ten facts a Bug needs before it may leave draft, with the severity qualification assigned (DR-LC-13). Sent whole; qualification is one act.",
          "properties": {
            "affected_scope": {
              "description": "What the defect affects.",
              "type": "string"
            },
            "criteria": {
              "description": "The story-linked Acceptance Criteria, at least one.",
              "items": {
                "$ref": "#/definitions/TicketCriterion"
              },
              "type": "array"
            },
            "environment": {
              "description": "Where the defect occurs.",
              "type": "string"
            },
            "expected_behaviour": {
              "description": "The behaviour that should have happened.",
              "type": "string"
            },
            "frequency": {
              "description": "How often the defect occurs.",
              "type": "string"
            },
            "reproduction": {
              "description": "The reproduction steps or the failing test demonstrating the defect.",
              "type": "string"
            },
            "risk": {
              "description": "What the defect puts at risk.",
              "type": "string"
            },
            "severity": {
              "allOf": [
                {
                  "$ref": "#/definitions/TicketSeverity"
                }
              ],
              "description": "The severity qualification assigned (DR-LC-13)."
            },
            "verification_steps": {
              "description": "The Verification Steps, at least one.",
              "items": {
                "$ref": "#/definitions/TicketVerificationStep"
              },
              "type": "array"
            }
          },
          "required": [
            "affected_scope",
            "criteria",
            "environment",
            "expected_behaviour",
            "frequency",
            "reproduction",
            "risk",
            "severity",
            "verification_steps"
          ],
          "type": "object"
        },
        "TicketBugRecord": {
          "additionalProperties": false,
          "description": "The Bug-specific body of a Ticket record: the quick-capture facts, the qualification once one exists, and the vendor-neutral collections the Bug carries (DR-TK-08 to DR-TK-10).",
          "properties": {
            "actual_behaviour": {
              "description": "What the Bug records happening, as the reporter stated it.",
              "type": "string"
            },
            "evidence_ids": {
              "description": "The identities of the Evidence Items this Bug carries (DR-TK-10).",
              "items": {
                "format": "uint64",
                "minimum": 0.0,
                "type": "integer"
              },
              "type": "array"
            },
            "external_references": {
              "description": "The External References, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketExternalReference"
              },
              "type": "array"
            },
            "occurrence_snapshots": {
              "description": "The Occurrence Snapshots, in recording order (DR-TK-10).",
              "items": {
                "$ref": "#/definitions/TicketOccurrenceSnapshot"
              },
              "type": "array"
            },
            "qualification": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TicketBugQualification"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The complete qualification, once one exists (DR-TK-09)."
            },
            "reporter_evidence": {
              "description": "The evidence the reporter holds, as the reporter stated it.",
              "type": "string"
            }
          },
          "required": [
            "actual_behaviour",
            "evidence_ids",
            "external_references",
            "occurrence_snapshots",
            "reporter_evidence"
          ],
          "type": "object"
        },
        "TicketCriterion": {
          "additionalProperties": false,
          "description": "One story-linked criterion a Ticket claims: an observable outcome and the User Stories it delivers, named like `CORE-S3-US6` or `S3-US6` (DR-TK-04, DR-PS-13).",
          "properties": {
            "outcome": {
              "description": "The observable outcome the criterion states.",
              "type": "string"
            },
            "stories": {
              "description": "The User Stories the criterion claims.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "outcome",
            "stories"
          ],
          "type": "object"
        },
        "TicketExternalReference": {
          "additionalProperties": false,
          "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
          "properties": {
            "label": {
              "description": "The reference's label, when it carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "The referenced URI.",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "TicketKind": {
          "description": "The closed Ticket kind vocabulary on the wire (DR-TK-01).",
          "oneOf": [
            {
              "description": "A small vertical product slice of exactly one Spec.",
              "enum": [
                "implementation"
              ],
              "type": "string"
            },
            {
              "description": "A record of incorrect behaviour, with qualification evidence.",
              "enum": [
                "bug"
              ],
              "type": "string"
            },
            {
              "description": "Bounded operational, investigative, or administrative work.",
              "enum": [
                "task"
              ],
              "type": "string"
            }
          ]
        },
        "TicketOccurrenceSnapshot": {
          "additionalProperties": false,
          "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
          "properties": {
            "observation": {
              "description": "What was observed at that moment.",
              "type": "string"
            },
            "observed_at": {
              "description": "The observed moment, RFC 3339.",
              "type": "string"
            }
          },
          "required": [
            "observation",
            "observed_at"
          ],
          "type": "object"
        },
        "TicketPriority": {
          "description": "The closed priority vocabulary on the wire (DR-LC-12): urgent, high, normal, low.",
          "oneOf": [
            {
              "description": "Ahead of everything else.",
              "enum": [
                "urgent"
              ],
              "type": "string"
            },
            {
              "description": "Ahead of normal work.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "The everyday order.",
              "enum": [
                "normal"
              ],
              "type": "string"
            },
            {
              "description": "Behind normal work.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketSeverity": {
          "description": "The closed Bug severity vocabulary on the wire (DR-LC-13): critical, high, medium, low. Severity arrives only inside a qualification (DR-TK-09).",
          "oneOf": [
            {
              "description": "Data loss, secret exposure, or unauthorised execution.",
              "enum": [
                "critical"
              ],
              "type": "string"
            },
            {
              "description": "A failed approved criterion or workflow invariant.",
              "enum": [
                "high"
              ],
              "type": "string"
            },
            {
              "description": "Usable behaviour, wrongly shaped.",
              "enum": [
                "medium"
              ],
              "type": "string"
            },
            {
              "description": "Cosmetic or local clarity.",
              "enum": [
                "low"
              ],
              "type": "string"
            }
          ]
        },
        "TicketState": {
          "description": "The closed Ticket lifecycle vocabulary on the wire (DR-LC-01): the canonical states in order, with the terminal states after them. Every Ticket is created into draft; the transition rules are the lifecycle slice's.",
          "oneOf": [
            {
              "description": "Newly created; not yet qualified or planned.",
              "enum": [
                "draft"
              ],
              "type": "string"
            },
            {
              "description": "Deliberately set aside.",
              "enum": [
                "parked"
              ],
              "type": "string"
            },
            {
              "description": "Waiting on something outside the Ticket.",
              "enum": [
                "blocked"
              ],
              "type": "string"
            },
            {
              "description": "Qualified but unavailable until activation.",
              "enum": [
                "scheduled"
              ],
              "type": "string"
            },
            {
              "description": "Free to execute when capacity allows.",
              "enum": [
                "ready"
              ],
              "type": "string"
            },
            {
              "description": "Executing.",
              "enum": [
                "active"
              ],
              "type": "string"
            },
            {
              "description": "Under review.",
              "enum": [
                "in_review"
              ],
              "type": "string"
            },
            {
              "description": "Review approved, waiting to land.",
              "enum": [
                "approved"
              ],
              "type": "string"
            },
            {
              "description": "Landing through the Seed Workspace.",
              "enum": [
                "landing"
              ],
              "type": "string"
            },
            {
              "description": "Landed in full.",
              "enum": [
                "done"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: will not execute.",
              "enum": [
                "cancelled"
              ],
              "type": "string"
            },
            {
              "description": "Terminal: replaced by a reassignment Ticket.",
              "enum": [
                "superseded"
              ],
              "type": "string"
            }
          ]
        },
        "TicketVerificationStep": {
          "additionalProperties": false,
          "description": "One Verification Step a Ticket claims: the command or scripted procedure that demonstrates a criterion (DR-PS-15). Commands live here and never as criteria.",
          "properties": {
            "command": {
              "description": "The command as it runs.",
              "type": "string"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "description": "The Ticket record as every client sees it: the Project it belongs to, the number that Project minted, the kind whose schema it carries, the kind-specific fields — a title for Bugs and Tasks, a slice and criteria for Implementations, the Bug body for a Bug, and for Tasks the subtype, mode, completion criteria, and optional timing — and the Execution Profile the assignment names, if it carries one.",
      "properties": {
        "bug": {
          "anyOf": [
            {
              "$ref": "#/definitions/TicketBugRecord"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Bug body, if this Ticket carries one."
        },
        "completion": {
          "description": "The Task's completion criteria; empty for every other kind.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "criteria": {
          "description": "The Implementation's story-linked criteria; empty for every other kind.",
          "items": {
            "$ref": "#/definitions/TicketCriterion"
          },
          "type": "array"
        },
        "due": {
          "description": "The Task's due date, if it carries one; RFC 3339 in UTC.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketKind"
            }
          ],
          "description": "The kind whose schema this Ticket carries."
        },
        "mode": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's human-or-agent mode, if this Ticket carries one."
        },
        "number": {
          "description": "The number this Project minted for this Ticket; rendered with the Project's code, for example `CORE-T17`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "priority": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketPriority"
            }
          ],
          "description": "The Ticket's priority (DR-LC-12)."
        },
        "profile": {
          "description": "The Execution Profile this Ticket's assignment names, by its catalogue name. The reference keeps its name through every later catalogue change (DR-EP-05).",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "The Project this Ticket belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "scheduled_for": {
          "description": "The Task's one-time activation instant, if it carries one; RFC 3339 in UTC, stored for KAN-S11.",
          "type": [
            "string",
            "null"
          ]
        },
        "slice": {
          "description": "The Implementation slice description, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "spec_id": {
          "description": "The Spec this Ticket attaches to, if it attaches to one.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/TicketState"
            }
          ],
          "description": "The lifecycle state (DR-LC-01)."
        },
        "subtype": {
          "anyOf": [
            {
              "$ref": "#/definitions/TaskSubtype"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Task's subtype, if this Ticket carries one."
        },
        "title": {
          "description": "The Bug or Task title, if this Ticket carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "completion",
        "criteria",
        "id",
        "kind",
        "number",
        "priority",
        "project_id",
        "state",
        "version"
      ],
      "title": "TicketRecord",
      "type": "object"
    }
  },
  {
    "description": "Define one named Execution Profile carrying harness, model, effort, usage pool, and fallback policy.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `profile.define` command: one new named entry carrying the closed schema's five decisions.",
      "properties": {
        "effort": {
          "description": "The effort.",
          "type": "string"
        },
        "fallback": {
          "description": "The fallback policy, as the profile another entry names; a profile with no fallback policy omits the field.",
          "type": [
            "string",
            "null"
          ]
        },
        "harness": {
          "description": "The harness family.",
          "type": "string"
        },
        "model": {
          "description": "The model family.",
          "type": "string"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The name the entry is defined under; unique and immutable.",
          "type": "string"
        },
        "usage_pool": {
          "description": "The usage pool.",
          "type": "string"
        }
      },
      "required": [
        "effort",
        "harness",
        "model",
        "mutation",
        "name",
        "usage_pool"
      ],
      "title": "ProfileDefineRequest",
      "type": "object"
    },
    "name": "profile_define",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The profile record as every client sees it: the closed schema's five decisions under the entry's immutable name, with the entry's lifecycle state for the assignable surface.",
      "properties": {
        "effort": {
          "description": "The effort.",
          "type": "string"
        },
        "fallback": {
          "description": "The fallback policy, as the profile another entry names, if the entry carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "harness": {
          "description": "The harness family.",
          "type": "string"
        },
        "model": {
          "description": "The model family.",
          "type": "string"
        },
        "name": {
          "description": "The entry's immutable identity.",
          "type": "string"
        },
        "retired": {
          "description": "Whether the entry is retired: terminal, preserved, and no longer assignable.",
          "type": "boolean"
        },
        "usage_pool": {
          "description": "The usage pool.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "effort",
        "harness",
        "model",
        "name",
        "retired",
        "usage_pool",
        "version"
      ],
      "title": "ProfileRecord",
      "type": "object"
    }
  },
  {
    "description": "Replace one entry's definition under its own name; names are immutable.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `profile.update` command: the definition of one named entry, replaced wholesale under the same name.",
      "properties": {
        "effort": {
          "description": "The effort.",
          "type": "string"
        },
        "fallback": {
          "description": "The fallback policy, as the profile another entry names.",
          "type": [
            "string",
            "null"
          ]
        },
        "harness": {
          "description": "The harness family.",
          "type": "string"
        },
        "model": {
          "description": "The model family.",
          "type": "string"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The entry being updated; names are never renamed.",
          "type": "string"
        },
        "usage_pool": {
          "description": "The usage pool.",
          "type": "string"
        }
      },
      "required": [
        "effort",
        "harness",
        "model",
        "mutation",
        "name",
        "usage_pool"
      ],
      "title": "ProfileUpdateRequest",
      "type": "object"
    },
    "name": "profile_update",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The profile record as every client sees it: the closed schema's five decisions under the entry's immutable name, with the entry's lifecycle state for the assignable surface.",
      "properties": {
        "effort": {
          "description": "The effort.",
          "type": "string"
        },
        "fallback": {
          "description": "The fallback policy, as the profile another entry names, if the entry carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "harness": {
          "description": "The harness family.",
          "type": "string"
        },
        "model": {
          "description": "The model family.",
          "type": "string"
        },
        "name": {
          "description": "The entry's immutable identity.",
          "type": "string"
        },
        "retired": {
          "description": "Whether the entry is retired: terminal, preserved, and no longer assignable.",
          "type": "boolean"
        },
        "usage_pool": {
          "description": "The usage pool.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "effort",
        "harness",
        "model",
        "name",
        "retired",
        "usage_pool",
        "version"
      ],
      "title": "ProfileRecord",
      "type": "object"
    }
  },
  {
    "description": "Retire one entry. Retirement is terminal and preserves every recorded fact.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `profile.retire` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "name": {
          "description": "The entry being retired. Retirement is terminal and preserves every recorded fact.",
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "name"
      ],
      "title": "ProfileRetireRequest",
      "type": "object"
    },
    "name": "profile_retire",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The profile record as every client sees it: the closed schema's five decisions under the entry's immutable name, with the entry's lifecycle state for the assignable surface.",
      "properties": {
        "effort": {
          "description": "The effort.",
          "type": "string"
        },
        "fallback": {
          "description": "The fallback policy, as the profile another entry names, if the entry carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "harness": {
          "description": "The harness family.",
          "type": "string"
        },
        "model": {
          "description": "The model family.",
          "type": "string"
        },
        "name": {
          "description": "The entry's immutable identity.",
          "type": "string"
        },
        "retired": {
          "description": "Whether the entry is retired: terminal, preserved, and no longer assignable.",
          "type": "boolean"
        },
        "usage_pool": {
          "description": "The usage pool.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "effort",
        "harness",
        "model",
        "name",
        "retired",
        "usage_pool",
        "version"
      ],
      "title": "ProfileRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Execution Profile, retired ones included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `profile.list` query.",
      "title": "ProfileListQuery",
      "type": "object"
    },
    "name": "profile_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ProfileRecord": {
          "additionalProperties": false,
          "description": "The profile record as every client sees it: the closed schema's five decisions under the entry's immutable name, with the entry's lifecycle state for the assignable surface.",
          "properties": {
            "effort": {
              "description": "The effort.",
              "type": "string"
            },
            "fallback": {
              "description": "The fallback policy, as the profile another entry names, if the entry carries one.",
              "type": [
                "string",
                "null"
              ]
            },
            "harness": {
              "description": "The harness family.",
              "type": "string"
            },
            "model": {
              "description": "The model family.",
              "type": "string"
            },
            "name": {
              "description": "The entry's immutable identity.",
              "type": "string"
            },
            "retired": {
              "description": "Whether the entry is retired: terminal, preserved, and no longer assignable.",
              "type": "boolean"
            },
            "usage_pool": {
              "description": "The usage pool.",
              "type": "string"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "effort",
            "harness",
            "model",
            "name",
            "retired",
            "usage_pool",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `profile.list` query.",
      "properties": {
        "profiles": {
          "description": "Every catalogue entry, retired ones included.",
          "items": {
            "$ref": "#/definitions/ProfileRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "profiles"
      ],
      "title": "ProfileListResponse",
      "type": "object"
    }
  },
  {
    "description": "Read one Execution Profile by name.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `profile.get` query.",
      "properties": {
        "name": {
          "description": "The entry being read.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "title": "ProfileGetQuery",
      "type": "object"
    },
    "name": "profile_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The profile record as every client sees it: the closed schema's five decisions under the entry's immutable name, with the entry's lifecycle state for the assignable surface.",
      "properties": {
        "effort": {
          "description": "The effort.",
          "type": "string"
        },
        "fallback": {
          "description": "The fallback policy, as the profile another entry names, if the entry carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "harness": {
          "description": "The harness family.",
          "type": "string"
        },
        "model": {
          "description": "The model family.",
          "type": "string"
        },
        "name": {
          "description": "The entry's immutable identity.",
          "type": "string"
        },
        "retired": {
          "description": "Whether the entry is retired: terminal, preserved, and no longer assignable.",
          "type": "boolean"
        },
        "usage_pool": {
          "description": "The usage pool.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "effort",
        "harness",
        "model",
        "name",
        "retired",
        "usage_pool",
        "version"
      ],
      "title": "ProfileRecord",
      "type": "object"
    }
  },
  {
    "description": "Query the per-Project append-only activity timeline.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        },
        "TimelineEventKind": {
          "description": "The closed vocabulary of timeline event kinds (DR-AE-01).",
          "enum": [
            "transition",
            "run",
            "telemetry",
            "review",
            "finding",
            "evidence",
            "comment",
            "deferral",
            "ruling"
          ],
          "type": "string"
        },
        "TimelineScope": {
          "description": "Where a timeline row belongs. Entities that sit above every Project — Initiatives, for one — are recorded globally; everything inside a Project is recorded against that Project's numeric identity, resolved through the Project store before any write.",
          "oneOf": [
            {
              "description": "Above every Project.",
              "enum": [
                "global"
              ],
              "type": "string"
            },
            {
              "additionalProperties": false,
              "description": "One Project's own timeline, named by its numeric identity.",
              "properties": {
                "project": {
                  "format": "uint64",
                  "minimum": 0.0,
                  "type": "integer"
                }
              },
              "required": [
                "project"
              ],
              "type": "object"
            }
          ]
        }
      },
      "description": "Filters for the per-Project timeline query surface.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "kinds": {
          "items": {
            "$ref": "#/definitions/TimelineEventKind"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "scope": {
          "$ref": "#/definitions/TimelineScope"
        },
        "since": {
          "type": [
            "string",
            "null"
          ]
        },
        "until": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "scope"
      ],
      "title": "TimelineQuery",
      "type": "object"
    },
    "name": "timeline_query",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        },
        "TimelineEventKind": {
          "description": "The closed vocabulary of timeline event kinds (DR-AE-01).",
          "enum": [
            "transition",
            "run",
            "telemetry",
            "review",
            "finding",
            "evidence",
            "comment",
            "deferral",
            "ruling"
          ],
          "type": "string"
        },
        "TimelineEventRecord": {
          "additionalProperties": false,
          "description": "One append-only timeline row as returned by queries.",
          "properties": {
            "detail": true,
            "entity": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TimelineEntityRef"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEventKind"
            },
            "recorded_at": {
              "type": "string"
            },
            "scope": {
              "$ref": "#/definitions/TimelineScope"
            }
          },
          "required": [
            "detail",
            "id",
            "kind",
            "recorded_at",
            "scope"
          ],
          "type": "object"
        },
        "TimelineScope": {
          "description": "Where a timeline row belongs. Entities that sit above every Project — Initiatives, for one — are recorded globally; everything inside a Project is recorded against that Project's numeric identity, resolved through the Project store before any write.",
          "oneOf": [
            {
              "description": "Above every Project.",
              "enum": [
                "global"
              ],
              "type": "string"
            },
            {
              "additionalProperties": false,
              "description": "One Project's own timeline, named by its numeric identity.",
              "properties": {
                "project": {
                  "format": "uint64",
                  "minimum": 0.0,
                  "type": "integer"
                }
              },
              "required": [
                "project"
              ],
              "type": "object"
            }
          ]
        }
      },
      "description": "The timeline query answer.",
      "properties": {
        "events": {
          "items": {
            "$ref": "#/definitions/TimelineEventRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "events"
      ],
      "title": "TimelineQueryResponse",
      "type": "object"
    }
  },
  {
    "description": "Create a Comment with its first revision on a timeline-visible entity.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `comment.create` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "description": "The owning Project, resolved before any write.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "target": {
          "allOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            }
          ],
          "description": "The entity this Comment attaches to."
        },
        "text": {
          "description": "The initial text; blank text is refused.",
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "project_id",
        "target",
        "text"
      ],
      "title": "CommentCreateRequest",
      "type": "object"
    },
    "name": "comment_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "The Comment record as every client sees it.",
      "properties": {
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The owning Project.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "target": {
          "allOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            }
          ],
          "description": "The entity this Comment attaches to."
        },
        "text": {
          "description": "The current text, resolved from the latest revision.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "id",
        "project_id",
        "target",
        "text",
        "version"
      ],
      "title": "CommentRecord",
      "type": "object"
    }
  },
  {
    "description": "Edit a Comment by appending a new immutable revision.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `comment.edit` command.",
      "properties": {
        "comment_id": {
          "description": "The Comment being edited.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "text": {
          "description": "The revised text; blank text is refused.",
          "type": "string"
        }
      },
      "required": [
        "comment_id",
        "mutation",
        "text"
      ],
      "title": "CommentEditRequest",
      "type": "object"
    },
    "name": "comment_edit",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "The Comment record as every client sees it.",
      "properties": {
        "id": {
          "description": "The immutable, storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The owning Project.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "target": {
          "allOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            }
          ],
          "description": "The entity this Comment attaches to."
        },
        "text": {
          "description": "The current text, resolved from the latest revision.",
          "type": "string"
        },
        "version": {
          "description": "The aggregate version, for optimistic mutation checks.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "id",
        "project_id",
        "target",
        "text",
        "version"
      ],
      "title": "CommentRecord",
      "type": "object"
    }
  },
  {
    "description": "Query the full revision history for one Comment.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `comment.revisions` query.",
      "properties": {
        "comment_id": {
          "description": "The Comment whose history is requested.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "comment_id"
      ],
      "title": "CommentRevisionsQuery",
      "type": "object"
    },
    "name": "comment_revisions",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "CommentRecord": {
          "additionalProperties": false,
          "description": "The Comment record as every client sees it.",
          "properties": {
            "id": {
              "description": "The immutable, storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The owning Project.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "target": {
              "allOf": [
                {
                  "$ref": "#/definitions/TimelineEntityRef"
                }
              ],
              "description": "The entity this Comment attaches to."
            },
            "text": {
              "description": "The current text, resolved from the latest revision.",
              "type": "string"
            },
            "version": {
              "description": "The aggregate version, for optimistic mutation checks.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "id",
            "project_id",
            "target",
            "text",
            "version"
          ],
          "type": "object"
        },
        "CommentRevisionRecord": {
          "additionalProperties": false,
          "description": "One immutable revision as returned by queries.",
          "properties": {
            "recorded_at": {
              "description": "When the revision was recorded.",
              "type": "string"
            },
            "revision": {
              "description": "The one-based revision number.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "text": {
              "description": "The revision text.",
              "type": "string"
            }
          },
          "required": [
            "recorded_at",
            "revision",
            "text"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `comment.revisions` query.",
      "properties": {
        "comment": {
          "allOf": [
            {
              "$ref": "#/definitions/CommentRecord"
            }
          ],
          "description": "The Comment with its current text resolved."
        },
        "revisions": {
          "description": "Every revision, oldest first.",
          "items": {
            "$ref": "#/definitions/CommentRevisionRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "comment",
        "revisions"
      ],
      "title": "CommentRevisionsResponse",
      "type": "object"
    }
  },
  {
    "description": "Record an immutable operator ruling on the timeline.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ruling.record` command.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "summary": {
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "project_id",
        "summary"
      ],
      "title": "RulingRecordRequest",
      "type": "object"
    },
    "name": "ruling_record",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "One immutable ruling as every client sees it.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "The entity the ruling concerns, if any."
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the ruling belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "recorded_at": {
          "description": "When the ruling was recorded.",
          "type": "string"
        },
        "summary": {
          "description": "The operator decision text.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The ruling this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "recorded_at",
        "summary"
      ],
      "title": "RulingRecord",
      "type": "object"
    }
  },
  {
    "description": "Supersede a ruling with a new immutable record.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ruling.supersede` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "ruling_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "summary": {
          "type": "string"
        }
      },
      "required": [
        "mutation",
        "project_id",
        "ruling_id",
        "summary"
      ],
      "title": "RulingSupersedeRequest",
      "type": "object"
    },
    "name": "ruling_supersede",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "One immutable ruling as every client sees it.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "The entity the ruling concerns, if any."
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the ruling belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "recorded_at": {
          "description": "When the ruling was recorded.",
          "type": "string"
        },
        "summary": {
          "description": "The operator decision text.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The ruling this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "recorded_at",
        "summary"
      ],
      "title": "RulingRecord",
      "type": "object"
    }
  },
  {
    "description": "List every ruling for a project, superseded originals included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `ruling.list` query.",
      "properties": {
        "entity": {
          "anyOf": [
            {
              "$ref": "#/definitions/TimelineEntityRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "RulingListQuery",
      "type": "object"
    },
    "name": "ruling_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "RulingRecord": {
          "additionalProperties": false,
          "description": "One immutable ruling as every client sees it.",
          "properties": {
            "entity": {
              "anyOf": [
                {
                  "$ref": "#/definitions/TimelineEntityRef"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The entity the ruling concerns, if any."
            },
            "id": {
              "description": "The storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The project the ruling belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "recorded_at": {
              "description": "When the ruling was recorded.",
              "type": "string"
            },
            "summary": {
              "description": "The operator decision text.",
              "type": "string"
            },
            "supersedes_id": {
              "description": "The ruling this one supersedes, if any.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "id",
            "project_id",
            "recorded_at",
            "summary"
          ],
          "type": "object"
        },
        "TimelineEntityKind": {
          "description": "Entity kinds that may appear on the activity timeline.",
          "enum": [
            "initiative",
            "project",
            "plan",
            "spec",
            "ticket",
            "run",
            "review",
            "finding",
            "evidence",
            "comment",
            "workspace",
            "lane",
            "profile"
          ],
          "type": "string"
        },
        "TimelineEntityRef": {
          "additionalProperties": false,
          "description": "A timeline-visible entity reference.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/TimelineEntityKind"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `ruling.list` query.",
      "properties": {
        "rulings": {
          "description": "Every ruling for the project, superseded originals included.",
          "items": {
            "$ref": "#/definitions/RulingRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "rulings"
      ],
      "title": "RulingListResponse",
      "type": "object"
    }
  },
  {
    "description": "Record an immutable finding deferral on the timeline.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `deferral.record` command.",
      "properties": {
        "finding_id": {
          "type": "string"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "finding_id",
        "mutation",
        "project_id",
        "reason"
      ],
      "title": "DeferralRecordRequest",
      "type": "object"
    },
    "name": "deferral_record",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "One immutable deferral as every client sees it.",
      "properties": {
        "finding_id": {
          "description": "The finding that was deferred.",
          "type": "string"
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the deferral belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reason": {
          "description": "Why the finding was deferred.",
          "type": "string"
        },
        "recorded_at": {
          "description": "When the deferral was recorded.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The deferral this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "finding_id",
        "id",
        "project_id",
        "reason",
        "recorded_at"
      ],
      "title": "DeferralRecord",
      "type": "object"
    }
  },
  {
    "description": "Supersede a deferral with a new immutable record.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `deferral.supersede` command.",
      "properties": {
        "deferral_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "deferral_id",
        "mutation",
        "project_id",
        "reason"
      ],
      "title": "DeferralSupersedeRequest",
      "type": "object"
    },
    "name": "deferral_supersede",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "One immutable deferral as every client sees it.",
      "properties": {
        "finding_id": {
          "description": "The finding that was deferred.",
          "type": "string"
        },
        "id": {
          "description": "The storage-assigned identity.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "description": "The project the deferral belongs to.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reason": {
          "description": "Why the finding was deferred.",
          "type": "string"
        },
        "recorded_at": {
          "description": "When the deferral was recorded.",
          "type": "string"
        },
        "supersedes_id": {
          "description": "The deferral this one supersedes, if any.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "finding_id",
        "id",
        "project_id",
        "reason",
        "recorded_at"
      ],
      "title": "DeferralRecord",
      "type": "object"
    }
  },
  {
    "description": "List every deferral for a project, superseded originals included.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `deferral.list` query.",
      "properties": {
        "finding_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "DeferralListQuery",
      "type": "object"
    },
    "name": "deferral_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "DeferralRecord": {
          "additionalProperties": false,
          "description": "One immutable deferral as every client sees it.",
          "properties": {
            "finding_id": {
              "description": "The finding that was deferred.",
              "type": "string"
            },
            "id": {
              "description": "The storage-assigned identity.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "description": "The project the deferral belongs to.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "reason": {
              "description": "Why the finding was deferred.",
              "type": "string"
            },
            "recorded_at": {
              "description": "When the deferral was recorded.",
              "type": "string"
            },
            "supersedes_id": {
              "description": "The deferral this one supersedes, if any.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "finding_id",
            "id",
            "project_id",
            "reason",
            "recorded_at"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `deferral.list` query.",
      "properties": {
        "deferrals": {
          "description": "Every deferral for the project, superseded originals included.",
          "items": {
            "$ref": "#/definitions/DeferralRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "deferrals"
      ],
      "title": "DeferralListResponse",
      "type": "object"
    }
  },
  {
    "description": "Attach managed-file or repository evidence to an entity.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "EvidenceKindDto": {
          "description": "Whether an evidence item is a managed file or repository reference.",
          "enum": [
            "managed_file",
            "repository"
          ],
          "type": "string"
        },
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `evidence.attach` command.",
      "properties": {
        "commit_identity": {
          "type": [
            "string",
            "null"
          ]
        },
        "content_base64": {
          "type": [
            "string",
            "null"
          ]
        },
        "entity_id": {
          "type": "string"
        },
        "entity_kind": {
          "type": "string"
        },
        "evidence_kind": {
          "$ref": "#/definitions/EvidenceKindDto"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "relative_path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "entity_id",
        "entity_kind",
        "evidence_kind",
        "mutation",
        "project_id"
      ],
      "title": "EvidenceAttachRequest",
      "type": "object"
    },
    "name": "evidence_attach",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "EvidenceKindDto": {
          "description": "Whether an evidence item is a managed file or repository reference.",
          "enum": [
            "managed_file",
            "repository"
          ],
          "type": "string"
        }
      },
      "description": "One evidence record as every client sees it.",
      "properties": {
        "commit_identity": {
          "type": [
            "string",
            "null"
          ]
        },
        "content_hash": {
          "type": [
            "string",
            "null"
          ]
        },
        "entity_id": {
          "type": "string"
        },
        "entity_kind": {
          "type": "string"
        },
        "evidence_kind": {
          "$ref": "#/definitions/EvidenceKindDto"
        },
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "relative_path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "entity_id",
        "entity_kind",
        "evidence_kind",
        "id",
        "project_id"
      ],
      "title": "EvidenceRecord",
      "type": "object"
    }
  },
  {
    "description": "List evidence for a Project without mutating the timeline.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `evidence.list` query.",
      "properties": {
        "entity_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "entity_kind": {
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "EvidenceListQuery",
      "type": "object"
    },
    "name": "evidence_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "EvidenceKindDto": {
          "description": "Whether an evidence item is a managed file or repository reference.",
          "enum": [
            "managed_file",
            "repository"
          ],
          "type": "string"
        },
        "EvidenceRecord": {
          "additionalProperties": false,
          "description": "One evidence record as every client sees it.",
          "properties": {
            "commit_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "content_hash": {
              "type": [
                "string",
                "null"
              ]
            },
            "entity_id": {
              "type": "string"
            },
            "entity_kind": {
              "type": "string"
            },
            "evidence_kind": {
              "$ref": "#/definitions/EvidenceKindDto"
            },
            "id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "relative_path": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "entity_id",
            "entity_kind",
            "evidence_kind",
            "id",
            "project_id"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `evidence.list` query.",
      "properties": {
        "evidence": {
          "items": {
            "$ref": "#/definitions/EvidenceRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "evidence"
      ],
      "title": "EvidenceListResponse",
      "type": "object"
    }
  },
  {
    "description": "Return one Project's Herdr settings and connection diagnostics.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `herdr.settings.get` query.",
      "properties": {
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "HerdrSettingsGetQuery",
      "type": "object"
    },
    "name": "herdr_settings_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "HerdrConnectionDiagnostics": {
          "additionalProperties": false,
          "description": "Connection diagnostics for one Project's Herdr session.",
          "properties": {
            "connected": {
              "description": "Whether the per-session socket is connected.",
              "type": "boolean"
            },
            "herdr_workspace": {
              "description": "The required target Herdr workspace, resolved inside the effective session.",
              "type": "string"
            },
            "last_error": {
              "description": "The last connection or mapping error, if any.",
              "type": [
                "string",
                "null"
              ]
            },
            "last_snapshot_at": {
              "description": "When the last full snapshot was captured.",
              "type": [
                "string",
                "null"
              ]
            },
            "product_workspace": {
              "description": "The product Seed Workspace this session maps to.",
              "type": "string"
            },
            "session_name": {
              "description": "The named Herdr session, if the Project selected one; absence selects Herdr's default session.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "connected",
            "herdr_workspace",
            "product_workspace"
          ],
          "type": "object"
        },
        "HerdrProjectSettings": {
          "additionalProperties": false,
          "description": "Per-Project Herdr observation settings.",
          "properties": {
            "missing_result_deadline_secs": {
              "description": "After this many seconds a settled session is result-missing.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "polling_fallback_enabled": {
              "description": "Whether the ten-second whole-session polling fallback is on.",
              "type": "boolean"
            },
            "polling_fallback_interval_secs": {
              "description": "The whole-session polling interval when fallback is enabled.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "reconciliation_interval_secs": {
              "description": "How often reconciliation compares full session state.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "stall_deadline_secs": {
              "description": "After this many seconds a run is considered stale.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "version": {
              "description": "The optimistic version for updates.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "missing_result_deadline_secs",
            "polling_fallback_enabled",
            "polling_fallback_interval_secs",
            "reconciliation_interval_secs",
            "stall_deadline_secs",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `herdr.settings.get` query.",
      "properties": {
        "diagnostics": {
          "$ref": "#/definitions/HerdrConnectionDiagnostics"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "settings": {
          "$ref": "#/definitions/HerdrProjectSettings"
        }
      },
      "required": [
        "diagnostics",
        "project_id",
        "settings"
      ],
      "title": "HerdrSettingsGetResponse",
      "type": "object"
    }
  },
  {
    "description": "Update one Project's Herdr reconciliation, fallback polling, and deadlines.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `herdr.settings.update` command.",
      "properties": {
        "missing_result_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "polling_fallback_enabled": {
          "type": "boolean"
        },
        "polling_fallback_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reconciliation_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "mutation",
        "polling_fallback_enabled",
        "polling_fallback_interval_secs",
        "project_id",
        "reconciliation_interval_secs",
        "stall_deadline_secs"
      ],
      "title": "HerdrSettingsUpdateRequest",
      "type": "object"
    },
    "name": "herdr_settings_update",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Per-Project Herdr observation settings.",
      "properties": {
        "missing_result_deadline_secs": {
          "description": "After this many seconds a settled session is result-missing.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "polling_fallback_enabled": {
          "description": "Whether the ten-second whole-session polling fallback is on.",
          "type": "boolean"
        },
        "polling_fallback_interval_secs": {
          "description": "The whole-session polling interval when fallback is enabled.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reconciliation_interval_secs": {
          "description": "How often reconciliation compares full session state.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "description": "After this many seconds a run is considered stale.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "description": "The optimistic version for updates.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "polling_fallback_enabled",
        "polling_fallback_interval_secs",
        "reconciliation_interval_secs",
        "stall_deadline_secs",
        "version"
      ],
      "title": "HerdrProjectSettings",
      "type": "object"
    }
  },
  {
    "description": "Return global Herdr observation defaults.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `herdr.defaults.get` query.",
      "title": "HerdrDefaultsGetQuery",
      "type": "object"
    },
    "name": "herdr_defaults_get",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "HerdrGlobalDefaults": {
          "additionalProperties": false,
          "description": "Global defaults applied to new Projects and unset fields.",
          "properties": {
            "missing_result_deadline_secs": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "reconciliation_interval_secs": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "stall_deadline_secs": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "missing_result_deadline_secs",
            "reconciliation_interval_secs",
            "stall_deadline_secs",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `herdr.defaults.get` query.",
      "properties": {
        "defaults": {
          "$ref": "#/definitions/HerdrGlobalDefaults"
        }
      },
      "required": [
        "defaults"
      ],
      "title": "HerdrDefaultsGetResponse",
      "type": "object"
    }
  },
  {
    "description": "Update global Herdr reconciliation and deadline defaults.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `herdr.defaults.update` command.",
      "properties": {
        "missing_result_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "reconciliation_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "mutation",
        "reconciliation_interval_secs",
        "stall_deadline_secs"
      ],
      "title": "HerdrDefaultsUpdateRequest",
      "type": "object"
    },
    "name": "herdr_defaults_update",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Global defaults applied to new Projects and unset fields.",
      "properties": {
        "missing_result_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reconciliation_interval_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stall_deadline_secs": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "missing_result_deadline_secs",
        "reconciliation_interval_secs",
        "stall_deadline_secs",
        "version"
      ],
      "title": "HerdrGlobalDefaults",
      "type": "object"
    }
  },
  {
    "description": "Register a Workspace path for a Project.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `workspace.register` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "path": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "path",
        "project_id"
      ],
      "title": "WorkspaceRegisterRequest",
      "type": "object"
    },
    "name": "workspace_register",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "WorkspaceCheckoutDto": {
          "description": "The closed checkout vocabulary as every client sees it: HEAD is on a branch, or detached. Detached is a state of its own, never a branch name.",
          "enum": [
            "branch",
            "detached"
          ],
          "type": "string"
        },
        "WorkspaceHealthDto": {
          "description": "The closed health vocabulary as every client sees it. The `unobserved` state means the last git status read could not complete: the tree claims neither clean nor dirty (KAN-T99).",
          "enum": [
            "available",
            "assigned",
            "dirty",
            "missing",
            "retired",
            "unobserved"
          ],
          "type": "string"
        },
        "WorkspaceObservationDto": {
          "additionalProperties": false,
          "description": "The git facts the core last observed without mutating the clone.",
          "properties": {
            "branch": {
              "type": [
                "string",
                "null"
              ]
            },
            "checkout": {
              "anyOf": [
                {
                  "$ref": "#/definitions/WorkspaceCheckoutDto"
                },
                {
                  "type": "null"
                }
              ]
            },
            "head": {
              "type": [
                "string",
                "null"
              ]
            },
            "lane_assignment": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "unique_unlanded_commits": {
              "description": "Whether the Workspace holds unique unlanded commits; absent when the observer could not decide.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "working_tree_clean": {
              "description": "Whether the working tree is clean. Absent when no status read completed — a failed observation, never a clean verdict.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "WorkspaceReuseDto": {
          "additionalProperties": false,
          "description": "The reuse verdict with every condition evaluated (DR-LW-06).",
          "properties": {
            "clean": {
              "description": "The working tree is clean on a present path.",
              "type": "boolean"
            },
            "free_of_unlanded_commits": {
              "description": "No unique unlanded commits sit on the Workspace.",
              "type": "boolean"
            },
            "reusable": {
              "description": "Reusable only when every condition holds and the record is present and not retired.",
              "type": "boolean"
            },
            "unassigned": {
              "description": "No Lane assignment holds the Workspace.",
              "type": "boolean"
            }
          },
          "required": [
            "clean",
            "free_of_unlanded_commits",
            "reusable",
            "unassigned"
          ],
          "type": "object"
        }
      },
      "description": "The Workspace record as every client sees it.",
      "properties": {
        "health": {
          "$ref": "#/definitions/WorkspaceHealthDto"
        },
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "is_seed": {
          "type": "boolean"
        },
        "observation": {
          "$ref": "#/definitions/WorkspaceObservationDto"
        },
        "path": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reuse": {
          "$ref": "#/definitions/WorkspaceReuseDto"
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "health",
        "id",
        "is_seed",
        "observation",
        "path",
        "project_id",
        "reuse",
        "version"
      ],
      "title": "WorkspaceRecord",
      "type": "object"
    }
  },
  {
    "description": "Observe git state for a Workspace without mutating the clone.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `workspace.observe` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "workspace_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "workspace_id"
      ],
      "title": "WorkspaceObserveRequest",
      "type": "object"
    },
    "name": "workspace_observe",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "WorkspaceCheckoutDto": {
          "description": "The closed checkout vocabulary as every client sees it: HEAD is on a branch, or detached. Detached is a state of its own, never a branch name.",
          "enum": [
            "branch",
            "detached"
          ],
          "type": "string"
        },
        "WorkspaceHealthDto": {
          "description": "The closed health vocabulary as every client sees it. The `unobserved` state means the last git status read could not complete: the tree claims neither clean nor dirty (KAN-T99).",
          "enum": [
            "available",
            "assigned",
            "dirty",
            "missing",
            "retired",
            "unobserved"
          ],
          "type": "string"
        },
        "WorkspaceObservationDto": {
          "additionalProperties": false,
          "description": "The git facts the core last observed without mutating the clone.",
          "properties": {
            "branch": {
              "type": [
                "string",
                "null"
              ]
            },
            "checkout": {
              "anyOf": [
                {
                  "$ref": "#/definitions/WorkspaceCheckoutDto"
                },
                {
                  "type": "null"
                }
              ]
            },
            "head": {
              "type": [
                "string",
                "null"
              ]
            },
            "lane_assignment": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "unique_unlanded_commits": {
              "description": "Whether the Workspace holds unique unlanded commits; absent when the observer could not decide.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "working_tree_clean": {
              "description": "Whether the working tree is clean. Absent when no status read completed — a failed observation, never a clean verdict.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "WorkspaceReuseDto": {
          "additionalProperties": false,
          "description": "The reuse verdict with every condition evaluated (DR-LW-06).",
          "properties": {
            "clean": {
              "description": "The working tree is clean on a present path.",
              "type": "boolean"
            },
            "free_of_unlanded_commits": {
              "description": "No unique unlanded commits sit on the Workspace.",
              "type": "boolean"
            },
            "reusable": {
              "description": "Reusable only when every condition holds and the record is present and not retired.",
              "type": "boolean"
            },
            "unassigned": {
              "description": "No Lane assignment holds the Workspace.",
              "type": "boolean"
            }
          },
          "required": [
            "clean",
            "free_of_unlanded_commits",
            "reusable",
            "unassigned"
          ],
          "type": "object"
        }
      },
      "description": "The Workspace record as every client sees it.",
      "properties": {
        "health": {
          "$ref": "#/definitions/WorkspaceHealthDto"
        },
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "is_seed": {
          "type": "boolean"
        },
        "observation": {
          "$ref": "#/definitions/WorkspaceObservationDto"
        },
        "path": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reuse": {
          "$ref": "#/definitions/WorkspaceReuseDto"
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "health",
        "id",
        "is_seed",
        "observation",
        "path",
        "project_id",
        "reuse",
        "version"
      ],
      "title": "WorkspaceRecord",
      "type": "object"
    }
  },
  {
    "description": "Retire a Workspace. Retirement is the explicit operator action; the record is preserved, never deleted.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `workspace.retire` command. Retirement is the explicit operator action that ends reuse; the record is preserved, never deleted.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "workspace_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "workspace_id"
      ],
      "title": "WorkspaceRetireRequest",
      "type": "object"
    },
    "name": "workspace_retire",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "WorkspaceCheckoutDto": {
          "description": "The closed checkout vocabulary as every client sees it: HEAD is on a branch, or detached. Detached is a state of its own, never a branch name.",
          "enum": [
            "branch",
            "detached"
          ],
          "type": "string"
        },
        "WorkspaceHealthDto": {
          "description": "The closed health vocabulary as every client sees it. The `unobserved` state means the last git status read could not complete: the tree claims neither clean nor dirty (KAN-T99).",
          "enum": [
            "available",
            "assigned",
            "dirty",
            "missing",
            "retired",
            "unobserved"
          ],
          "type": "string"
        },
        "WorkspaceObservationDto": {
          "additionalProperties": false,
          "description": "The git facts the core last observed without mutating the clone.",
          "properties": {
            "branch": {
              "type": [
                "string",
                "null"
              ]
            },
            "checkout": {
              "anyOf": [
                {
                  "$ref": "#/definitions/WorkspaceCheckoutDto"
                },
                {
                  "type": "null"
                }
              ]
            },
            "head": {
              "type": [
                "string",
                "null"
              ]
            },
            "lane_assignment": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "unique_unlanded_commits": {
              "description": "Whether the Workspace holds unique unlanded commits; absent when the observer could not decide.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "working_tree_clean": {
              "description": "Whether the working tree is clean. Absent when no status read completed — a failed observation, never a clean verdict.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "WorkspaceReuseDto": {
          "additionalProperties": false,
          "description": "The reuse verdict with every condition evaluated (DR-LW-06).",
          "properties": {
            "clean": {
              "description": "The working tree is clean on a present path.",
              "type": "boolean"
            },
            "free_of_unlanded_commits": {
              "description": "No unique unlanded commits sit on the Workspace.",
              "type": "boolean"
            },
            "reusable": {
              "description": "Reusable only when every condition holds and the record is present and not retired.",
              "type": "boolean"
            },
            "unassigned": {
              "description": "No Lane assignment holds the Workspace.",
              "type": "boolean"
            }
          },
          "required": [
            "clean",
            "free_of_unlanded_commits",
            "reusable",
            "unassigned"
          ],
          "type": "object"
        }
      },
      "description": "The Workspace record as every client sees it.",
      "properties": {
        "health": {
          "$ref": "#/definitions/WorkspaceHealthDto"
        },
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "is_seed": {
          "type": "boolean"
        },
        "observation": {
          "$ref": "#/definitions/WorkspaceObservationDto"
        },
        "path": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "reuse": {
          "$ref": "#/definitions/WorkspaceReuseDto"
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "health",
        "id",
        "is_seed",
        "observation",
        "path",
        "project_id",
        "reuse",
        "version"
      ],
      "title": "WorkspaceRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Workspace for one Project with health and observation.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `workspace.list` query.",
      "properties": {
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "WorkspaceListQuery",
      "type": "object"
    },
    "name": "workspace_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "WorkspaceCheckoutDto": {
          "description": "The closed checkout vocabulary as every client sees it: HEAD is on a branch, or detached. Detached is a state of its own, never a branch name.",
          "enum": [
            "branch",
            "detached"
          ],
          "type": "string"
        },
        "WorkspaceHealthDto": {
          "description": "The closed health vocabulary as every client sees it. The `unobserved` state means the last git status read could not complete: the tree claims neither clean nor dirty (KAN-T99).",
          "enum": [
            "available",
            "assigned",
            "dirty",
            "missing",
            "retired",
            "unobserved"
          ],
          "type": "string"
        },
        "WorkspaceObservationDto": {
          "additionalProperties": false,
          "description": "The git facts the core last observed without mutating the clone.",
          "properties": {
            "branch": {
              "type": [
                "string",
                "null"
              ]
            },
            "checkout": {
              "anyOf": [
                {
                  "$ref": "#/definitions/WorkspaceCheckoutDto"
                },
                {
                  "type": "null"
                }
              ]
            },
            "head": {
              "type": [
                "string",
                "null"
              ]
            },
            "lane_assignment": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository_identity": {
              "type": [
                "string",
                "null"
              ]
            },
            "unique_unlanded_commits": {
              "description": "Whether the Workspace holds unique unlanded commits; absent when the observer could not decide.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "working_tree_clean": {
              "description": "Whether the working tree is clean. Absent when no status read completed — a failed observation, never a clean verdict.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "WorkspaceRecord": {
          "additionalProperties": false,
          "description": "The Workspace record as every client sees it.",
          "properties": {
            "health": {
              "$ref": "#/definitions/WorkspaceHealthDto"
            },
            "id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "is_seed": {
              "type": "boolean"
            },
            "observation": {
              "$ref": "#/definitions/WorkspaceObservationDto"
            },
            "path": {
              "type": "string"
            },
            "project_id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "reuse": {
              "$ref": "#/definitions/WorkspaceReuseDto"
            },
            "version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "health",
            "id",
            "is_seed",
            "observation",
            "path",
            "project_id",
            "reuse",
            "version"
          ],
          "type": "object"
        },
        "WorkspaceReuseDto": {
          "additionalProperties": false,
          "description": "The reuse verdict with every condition evaluated (DR-LW-06).",
          "properties": {
            "clean": {
              "description": "The working tree is clean on a present path.",
              "type": "boolean"
            },
            "free_of_unlanded_commits": {
              "description": "No unique unlanded commits sit on the Workspace.",
              "type": "boolean"
            },
            "reusable": {
              "description": "Reusable only when every condition holds and the record is present and not retired.",
              "type": "boolean"
            },
            "unassigned": {
              "description": "No Lane assignment holds the Workspace.",
              "type": "boolean"
            }
          },
          "required": [
            "clean",
            "free_of_unlanded_commits",
            "reusable",
            "unassigned"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `workspace.list` query.",
      "properties": {
        "workspaces": {
          "items": {
            "$ref": "#/definitions/WorkspaceRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "workspaces"
      ],
      "title": "WorkspaceListResponse",
      "type": "object"
    }
  },
  {
    "description": "Create a Lane: a durable execution slot holding at most one active Ticket.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `lane.create` command.",
      "properties": {
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "mutation",
        "project_id"
      ],
      "title": "LaneCreateRequest",
      "type": "object"
    },
    "name": "lane_create",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Lane record as every client sees it: a durable execution slot of one Project, holding at most one Workspace claim and at most one active Ticket (DR-LW-01 to DR-LW-03).",
      "properties": {
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "ticket_id": {
          "description": "The Ticket this Lane holds, when one is active.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "workspace_id": {
          "description": "The Workspace this Lane runs in, when claimed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "version"
      ],
      "title": "LaneRecord",
      "type": "object"
    }
  },
  {
    "description": "Claim a Workspace for a Lane. The Seed Workspace is refused and the refusal recorded.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `lane.workspace.assign` command. The Seed Workspace is refused and the refusal recorded (DR-LW-07).",
      "properties": {
        "lane_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "workspace_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "lane_id",
        "mutation",
        "workspace_id"
      ],
      "title": "LaneWorkspaceAssignRequest",
      "type": "object"
    },
    "name": "lane_workspace_assign",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Lane record as every client sees it: a durable execution slot of one Project, holding at most one Workspace claim and at most one active Ticket (DR-LW-01 to DR-LW-03).",
      "properties": {
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "ticket_id": {
          "description": "The Ticket this Lane holds, when one is active.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "workspace_id": {
          "description": "The Workspace this Lane runs in, when claimed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "version"
      ],
      "title": "LaneRecord",
      "type": "object"
    }
  },
  {
    "description": "Release the Workspace a Lane claims. The Workspace record and its health follow.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `lane.workspace.release` command.",
      "properties": {
        "lane_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        }
      },
      "required": [
        "lane_id",
        "mutation"
      ],
      "title": "LaneWorkspaceReleaseRequest",
      "type": "object"
    },
    "name": "lane_workspace_release",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Lane record as every client sees it: a durable execution slot of one Project, holding at most one Workspace claim and at most one active Ticket (DR-LW-01 to DR-LW-03).",
      "properties": {
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "ticket_id": {
          "description": "The Ticket this Lane holds, when one is active.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "workspace_id": {
          "description": "The Workspace this Lane runs in, when claimed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "version"
      ],
      "title": "LaneRecord",
      "type": "object"
    }
  },
  {
    "description": "Hold a Ticket as a Lane's active occupant; a second Ticket is refused.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `lane.ticket.assign` command.",
      "properties": {
        "lane_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "ticket_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "lane_id",
        "mutation",
        "ticket_id"
      ],
      "title": "LaneTicketAssignRequest",
      "type": "object"
    },
    "name": "lane_ticket_assign",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Lane record as every client sees it: a durable execution slot of one Project, holding at most one Workspace claim and at most one active Ticket (DR-LW-01 to DR-LW-03).",
      "properties": {
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "ticket_id": {
          "description": "The Ticket this Lane holds, when one is active.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "workspace_id": {
          "description": "The Workspace this Lane runs in, when claimed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "version"
      ],
      "title": "LaneRecord",
      "type": "object"
    }
  },
  {
    "description": "Free a Lane's Ticket slot.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `lane.ticket.release` command.",
      "properties": {
        "lane_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        }
      },
      "required": [
        "lane_id",
        "mutation"
      ],
      "title": "LaneTicketReleaseRequest",
      "type": "object"
    },
    "name": "lane_ticket_release",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "The Lane record as every client sees it: a durable execution slot of one Project, holding at most one Workspace claim and at most one active Ticket (DR-LW-01 to DR-LW-03).",
      "properties": {
        "id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "ticket_id": {
          "description": "The Ticket this Lane holds, when one is active.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "workspace_id": {
          "description": "The Workspace this Lane runs in, when claimed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "project_id",
        "version"
      ],
      "title": "LaneRecord",
      "type": "object"
    }
  },
  {
    "description": "List every Lane of one Project with its Workspace claim and held Ticket.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `lane.list` query.",
      "properties": {
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "project_id"
      ],
      "title": "LaneListQuery",
      "type": "object"
    },
    "name": "lane_list",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "LaneRecord": {
          "additionalProperties": false,
          "description": "The Lane record as every client sees it: a durable execution slot of one Project, holding at most one Workspace claim and at most one active Ticket (DR-LW-01 to DR-LW-03).",
          "properties": {
            "id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "project_id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "ticket_id": {
              "description": "The Ticket this Lane holds, when one is active.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "workspace_id": {
              "description": "The Workspace this Lane runs in, when claimed.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "id",
            "project_id",
            "version"
          ],
          "type": "object"
        }
      },
      "description": "Response payload for the `lane.list` query.",
      "properties": {
        "lanes": {
          "items": {
            "$ref": "#/definitions/LaneRecord"
          },
          "type": "array"
        }
      },
      "required": [
        "lanes"
      ],
      "title": "LaneListResponse",
      "type": "object"
    }
  },
  {
    "description": "Render Plans, Specs, and Tickets as deterministic Markdown into a directory within the Seed. Files replace atomically; nothing is committed, pushed, or staged.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "MutationContext": {
          "additionalProperties": false,
          "description": "Metadata every mutation must carry so retries and stale edits are safe.",
          "properties": {
            "idempotency_key": {
              "type": "string"
            },
            "optimistic_version": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "idempotency_key",
            "optimistic_version"
          ],
          "type": "object"
        }
      },
      "description": "Request payload for the `export.render` command. `directory` names the directory within the Project's Seed Workspace the Markdown lands in; a directory that would leave the Seed is refused.",
      "properties": {
        "directory": {
          "type": "string"
        },
        "mutation": {
          "$ref": "#/definitions/MutationContext"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "directory",
        "mutation",
        "project_id"
      ],
      "title": "ExportRenderRequest",
      "type": "object"
    },
    "name": "export_render",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Response payload for the `export.render` command: the files written, each relative to the configured directory.",
      "properties": {
        "directory": {
          "type": "string"
        },
        "files": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "directory",
        "files",
        "project_id"
      ],
      "title": "ExportRenderResponse",
      "type": "object"
    }
  },
  {
    "description": "Report drift between the exported Markdown on disk and the current planning state.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "description": "Request payload for the `export.drift` query.",
      "properties": {
        "directory": {
          "type": "string"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "directory",
        "project_id"
      ],
      "title": "ExportDriftQuery",
      "type": "object"
    },
    "name": "export_drift",
    "outputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "definitions": {
        "ExportDriftEntry": {
          "additionalProperties": false,
          "description": "One drifted file: its path relative to the configured directory and how it stands against the current planning state.",
          "properties": {
            "path": {
              "type": "string"
            },
            "status": {
              "$ref": "#/definitions/ExportDriftStatus"
            }
          },
          "required": [
            "path",
            "status"
          ],
          "type": "object"
        },
        "ExportDriftStatus": {
          "description": "The closed drift vocabulary: how one file on disk stands against the current planning state.",
          "oneOf": [
            {
              "description": "The current state renders a file the directory does not hold.",
              "enum": [
                "missing"
              ],
              "type": "string"
            },
            {
              "description": "The file on disk is not the bytes the current state renders.",
              "enum": [
                "differs"
              ],
              "type": "string"
            },
            {
              "description": "The directory holds a file the current state does not render.",
              "enum": [
                "unmatched"
              ],
              "type": "string"
            }
          ]
        }
      },
      "description": "Response payload for the `export.drift` query: every drifted file, in path order, with the verdict for the whole directory.",
      "properties": {
        "directory": {
          "type": "string"
        },
        "entries": {
          "items": {
            "$ref": "#/definitions/ExportDriftEntry"
          },
          "type": "array"
        },
        "in_drift": {
          "description": "Whether any entry drifted; a clean export renders this false with no entries.",
          "type": "boolean"
        },
        "project_id": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "directory",
        "entries",
        "in_drift",
        "project_id"
      ],
      "title": "ExportDriftResponse",
      "type": "object"
    }
  }
]

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "ErrorCode": {
      "description": "Stable error codes shared by commands, queries, and events.",
      "enum": [
        "unknown_field",
        "stale_version",
        "duplicate_idempotency_key",
        "not_found",
        "invalid_request",
        "internal"
      ],
      "type": "string"
    }
  },
  "description": "A transport-visible failure returned to every client.",
  "properties": {
    "code": {
      "$ref": "#/definitions/ErrorCode"
    },
    "current_version": {
      "description": "The aggregate's current version, carried by `stale_version` rejections so the client can correct and retry.",
      "format": "uint64",
      "minimum": 0.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "message": {
      "type": "string"
    }
  },
  "required": [
    "code",
    "message"
  ],
  "title": "ApiError",
  "type": "object"
}

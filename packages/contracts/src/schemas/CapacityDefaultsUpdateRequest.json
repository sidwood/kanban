{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "MutationContext": {
      "additionalProperties": false,
      "description": "Metadata every mutation must carry so retries and stale edits are safe.",
      "properties": {
        "idempotency_key": {
          "type": "string"
        },
        "optimistic_version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "idempotency_key",
        "optimistic_version"
      ],
      "type": "object"
    }
  },
  "description": "Request payload for the `capacity.defaults.update` command: the three global limits, replaced wholesale.",
  "properties": {
    "max_active_per_harness": {
      "description": "The most active runs one harness family may carry.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "max_active_per_model": {
      "description": "The most active runs one model family may carry.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "max_active_per_usage_pool": {
      "description": "The most active runs one usage pool may carry.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "mutation": {
      "$ref": "#/definitions/MutationContext"
    }
  },
  "required": [
    "max_active_per_harness",
    "max_active_per_model",
    "max_active_per_usage_pool",
    "mutation"
  ],
  "title": "CapacityDefaultsUpdateRequest",
  "type": "object"
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "The global capacity defaults as every client sees them: the maximum active runs one harness, model family, or usage pool may carry across every Project (DR-EP-06).",
  "properties": {
    "max_active_per_harness": {
      "description": "The most active runs one harness family may carry.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "max_active_per_model": {
      "description": "The most active runs one model family may carry.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "max_active_per_usage_pool": {
      "description": "The most active runs one usage pool may carry.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "version": {
      "description": "The aggregate version, for optimistic mutation checks.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "max_active_per_harness",
    "max_active_per_model",
    "max_active_per_usage_pool",
    "version"
  ],
  "title": "CapacityGlobalDefaults",
  "type": "object"
}

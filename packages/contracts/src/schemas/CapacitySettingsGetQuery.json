{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Request payload for the `capacity.settings.get` query.",
  "properties": {
    "project_id": {
      "description": "The Project whose caps are being read.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "project_id"
  ],
  "title": "CapacitySettingsGetQuery",
  "type": "object"
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "MutationContext": {
      "additionalProperties": false,
      "description": "Metadata every mutation must carry so retries and stale edits are safe.",
      "properties": {
        "idempotency_key": {
          "type": "string"
        },
        "optimistic_version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "idempotency_key",
        "optimistic_version"
      ],
      "type": "object"
    }
  },
  "description": "Request payload for the `clone.create` command. The source is not client-supplied: Kanban resolves the Project's registered repository and refuses conflicting paths, branches, and Lane assignments before invoking anything (DR-LW-09, DR-LW-10).",
  "properties": {
    "branch": {
      "description": "The branch the clone checks out.",
      "type": "string"
    },
    "mutation": {
      "$ref": "#/definitions/MutationContext"
    },
    "path": {
      "description": "The directory the branch clone lands in.",
      "type": "string"
    },
    "project_id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "branch",
    "mutation",
    "path",
    "project_id"
  ],
  "title": "CloneCreateRequest",
  "type": "object"
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "MutationContext": {
      "additionalProperties": false,
      "description": "Metadata every mutation must carry so retries and stale edits are safe.",
      "properties": {
        "idempotency_key": {
          "type": "string"
        },
        "optimistic_version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "idempotency_key",
        "optimistic_version"
      ],
      "type": "object"
    }
  },
  "description": "Request payload for the `clone.remove` command. The Workspace record is preserved, never deleted or retired (DR-LW-11); only the clone on disk goes, and only through the guarded fleet skill. The removal itself marks the Workspace missing.",
  "properties": {
    "mutation": {
      "$ref": "#/definitions/MutationContext"
    },
    "workspace_id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "mutation",
    "workspace_id"
  ],
  "title": "CloneRemoveRequest",
  "type": "object"
}

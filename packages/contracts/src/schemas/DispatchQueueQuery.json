{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Request payload for the `dispatch.queue` query.",
  "properties": {
    "project_id": {
      "description": "The Project whose queued requests are listed.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "project_id"
  ],
  "title": "DispatchQueueQuery",
  "type": "object"
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Where Done work sits under a view.",
  "oneOf": [
    {
      "description": "Done stands with the other groups.",
      "enum": [
        "column"
      ],
      "type": "string"
    },
    {
      "description": "Done sits below the board as a table.",
      "enum": [
        "table"
      ],
      "type": "string"
    }
  ],
  "title": "DonePlacement"
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "NotificationPermissionState": {
      "enum": [
        "not_determined",
        "denied",
        "granted",
        "unavailable"
      ],
      "type": "string"
    }
  },
  "properties": {
    "reason": {
      "type": [
        "string",
        "null"
      ]
    },
    "request_pending": {
      "type": "boolean"
    },
    "state": {
      "$ref": "#/definitions/NotificationPermissionState"
    }
  },
  "required": [
    "request_pending",
    "state"
  ],
  "title": "NotificationPermissionRecord",
  "type": "object"
}

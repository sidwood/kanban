{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Request payload for the `run.list` query.",
  "properties": {
    "project_id": {
      "description": "The Project whose runs are listed.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "project_id"
  ],
  "title": "RunListQuery",
  "type": "object"
}

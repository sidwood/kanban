{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "RunRecoveryAction": {
      "enum": [
        "operator_ruling",
        "retry",
        "resume"
      ],
      "type": "string"
    }
  },
  "properties": {
    "action": {
      "$ref": "#/definitions/RunRecoveryAction"
    },
    "created_at": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "project_id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "replacement_dispatch_request_id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "ruling_id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "run_id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "summary": {
      "type": "string"
    },
    "version": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "action",
    "created_at",
    "id",
    "project_id",
    "ruling_id",
    "run_id",
    "summary",
    "version"
  ],
  "title": "RunRecoveryRecord",
  "type": "object"
}

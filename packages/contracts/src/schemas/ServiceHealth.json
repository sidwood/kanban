{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "The service component: the core process answering the query.",
  "properties": {
    "source_epoch": {
      "default": 0,
      "description": "Reproducible source timestamp, not the time the build ran.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "source_revision": {
      "default": "",
      "description": "Full source revision embedded by the packaging hook.",
      "type": "string"
    },
    "started_at": {
      "description": "When this core process started serving; its state last changed by coming into being.",
      "type": "string"
    }
  },
  "required": [
    "started_at"
  ],
  "title": "ServiceHealth",
  "type": "object"
}

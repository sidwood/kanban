{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "MutationContext": {
      "additionalProperties": false,
      "description": "Metadata every mutation must carry so retries and stale edits are safe.",
      "properties": {
        "idempotency_key": {
          "type": "string"
        },
        "optimistic_version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "idempotency_key",
        "optimistic_version"
      ],
      "type": "object"
    }
  },
  "properties": {
    "branch": {
      "type": "string"
    },
    "mutation": {
      "$ref": "#/definitions/MutationContext"
    },
    "spec_id": {
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    },
    "workspace_path": {
      "type": "string"
    }
  },
  "required": [
    "branch",
    "mutation",
    "spec_id",
    "workspace_path"
  ],
  "title": "SpecIntegrationClaimRequest",
  "type": "object"
}

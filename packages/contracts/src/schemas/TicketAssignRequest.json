{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "MutationContext": {
      "additionalProperties": false,
      "description": "Metadata every mutation must carry so retries and stale edits are safe.",
      "properties": {
        "idempotency_key": {
          "type": "string"
        },
        "optimistic_version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "idempotency_key",
        "optimistic_version"
      ],
      "type": "object"
    }
  },
  "description": "Request payload for the `ticket.assign` command: the Ticket's assignment names one catalogue entry by reference, and an unknown name is rejected (DR-EP-03).",
  "properties": {
    "mutation": {
      "$ref": "#/definitions/MutationContext"
    },
    "profile": {
      "description": "The Execution Profile the assignment names, by its catalogue name.",
      "type": "string"
    },
    "ticket_id": {
      "description": "The Ticket being assigned.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "mutation",
    "profile",
    "ticket_id"
  ],
  "title": "TicketAssignRequest",
  "type": "object"
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "MutationContext": {
      "additionalProperties": false,
      "description": "Metadata every mutation must carry so retries and stale edits are safe.",
      "properties": {
        "idempotency_key": {
          "type": "string"
        },
        "optimistic_version": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "idempotency_key",
        "optimistic_version"
      ],
      "type": "object"
    },
    "TicketExternalReference": {
      "additionalProperties": false,
      "description": "One vendor-neutral External Reference a Bug carries (DR-TK-10): a URI naming an outside link, with an optional label.",
      "properties": {
        "label": {
          "description": "The reference's label, when it carries one.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "The referenced URI.",
          "type": "string"
        }
      },
      "required": [
        "uri"
      ],
      "type": "object"
    },
    "TicketOccurrenceSnapshot": {
      "additionalProperties": false,
      "description": "One Occurrence Snapshot a Bug carries (DR-TK-10): the RFC 3339 moment one occurrence was observed and what was seen then.",
      "properties": {
        "observation": {
          "description": "What was observed at that moment.",
          "type": "string"
        },
        "observed_at": {
          "description": "The observed moment, RFC 3339.",
          "type": "string"
        }
      },
      "required": [
        "observation",
        "observed_at"
      ],
      "type": "object"
    }
  },
  "description": "Request payload for the `ticket.bug.facts` command: the vendor-neutral collections one Bug carries (DR-TK-10), replaced whole.",
  "properties": {
    "evidence_ids": {
      "description": "The identities of the Evidence Items attached to this Bug.",
      "items": {
        "format": "uint64",
        "minimum": 0.0,
        "type": "integer"
      },
      "type": "array"
    },
    "external_references": {
      "description": "The External References, in recording order.",
      "items": {
        "$ref": "#/definitions/TicketExternalReference"
      },
      "type": "array"
    },
    "mutation": {
      "$ref": "#/definitions/MutationContext"
    },
    "occurrence_snapshots": {
      "description": "The Occurrence Snapshots, in recording order.",
      "items": {
        "$ref": "#/definitions/TicketOccurrenceSnapshot"
      },
      "type": "array"
    },
    "ticket_id": {
      "description": "The Bug carrying the collections.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "evidence_ids",
    "external_references",
    "mutation",
    "occurrence_snapshots",
    "ticket_id"
  ],
  "title": "TicketBugFactsRequest",
  "type": "object"
}

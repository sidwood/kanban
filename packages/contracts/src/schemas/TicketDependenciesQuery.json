{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Request payload for the `ticket.dependencies` query.",
  "properties": {
    "ticket_id": {
      "description": "The Ticket whose dependencies and blockers are read.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "ticket_id"
  ],
  "title": "TicketDependenciesQuery",
  "type": "object"
}

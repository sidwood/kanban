{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Request payload for the `ticket.graph.list` query.",
  "properties": {
    "spec_id": {
      "description": "The Spec whose proposed graphs are listed, every state included.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "spec_id"
  ],
  "title": "TicketGraphListQuery",
  "type": "object"
}
